# cloud_power 合并系统部署文档

> 适用范围：`main` 分支当前版本。本文档用于在 Windows 或 Linux 服务器部署算力系统、集采系统、华为云业务和公共基础功能组成的 Next.js 前后端一体应用。文档更新日期：2026-09-03。

## 1. 部署架构

- 应用：Next.js 16、React、TypeScript、Node.js。
- 数据库：MySQL。生产环境连接远程 `139.9.122.9:3306` 的 `cloud` 数据库；本地开发验证默认使用 `merge` 数据库。
- 数据表：生产数据库中的物理表按业务域使用 `merge_power_`、`merge_po_`、`merge_cloud_`、`merge_common_` 四类前缀。
- 生产数据库结构状态：远程 `cloud` 数据库已完成结构同步、`InternalId` 迁移和基线数据补全。库内共 153 张表，其中 `merge_*` 业务表 90 张，`internalId` 均为 `INT UNSIGNED AUTO_INCREMENT` 主键；原 UUID、业务编号和原有关联字段均已保留。结构与开发库比对为 0 差异，详见第 5 节。
- 文件存储：合同、发票等上传文件保存在应用服务器的 `uploads/` 目录。
- 运行方式：`next start`，建议使用 PM2 或服务器进程管理器守护。

生产部署只更新代码和应用进程，不自动迁移现有业务数据。远程数据库已经完成当前版本的表结构更新，日常发布不要执行建库、建表、迁移、导入或清空数据命令。

表前缀约定：

- `merge_power_`：算力系统需求、采购、物流、合同、月账单、预付款、服务费和结差。
- `merge_po_`：集采客户 PO、报价单、项目结算、产品主档、产品类别和发票汇总。
- `merge_cloud_`：华为云对账、账号映射、供应商付款和附件。
- `merge_common_`：供应商、客户、承接单位、联系人、银行账户、用户、权限、文档和操作日志等公共数据。

供应商、客户和承接单位只维护在 `merge_common_` 公共表中；三个业务域的业务单据表仍然相互隔离。

## 2. 环境要求

| 软件 | 建议版本 | 说明 |
| --- | --- | --- |
| Node.js | 20.9+、22 LTS 或 24 LTS | Node.js 18 不再建议使用 |
| npm | 随 Node.js 安装 | 使用 `npm ci` 按锁文件安装 |
| Git | 最新稳定版 | 拉取与更新代码 |
| MySQL | 8.0 或兼容版本 | 数据库已部署在独立服务器 |
| PM2 | 最新稳定版，可选 | 生产进程守护与重启 |

部署服务器需要能够访问代码仓库和远程 MySQL 服务 `139.9.122.9:3306`，远程数据库还需要在安全组、防火墙和 MySQL 账号权限中允许部署服务器的来源 IP，并开放应用对外服务端口，例如 `3000`。

## 3. 获取代码

### 3.1 华为 CodeHub 代码仓（生产部署）

生产部署使用华为 CodeHub 的 `cloud_purchase` 仓库：

```bash
git clone --branch main --single-branch https://codehub.devcloud.cn-north-4.huaweicloud.com/2fc6809e8fe540f68c39743bc923d349/cloud_purchase.git merge
cd merge
```

代码仓账号和密码应通过安全方式提供，不要写入命令历史、脚本或文档。

### 3.2 GitHub 代码仓（备用）

```bash
git clone --branch main --single-branch https://github.com/Never-mind/cloud.git merge
cd merge
```

也可以使用 SSH：

```bash
git clone --branch main --single-branch git@github.com:Never-mind/cloud.git merge
cd merge
```

如服务器上已有部署目录，先确认工作区没有人工修改，再更新。生产服务器必须确认远程地址指向 `cloud_purchase.git`：

```bash
git status
git remote -v
git switch main
git pull --ff-only
```

不要在存在未提交修改的部署目录直接执行覆盖操作。

## 4. 配置环境变量

在项目根目录创建 `.env.local`：

```env
DB_HOST=139.9.122.9
DB_PORT=3306
DB_USER=<远程数据库账号>
DB_PASSWORD=<通过安全渠道获取的数据库密码>
DB_NAME=cloud
DB_CONNECTION_LIMIT=5
AUTH_SESSION_TOKEN=<随机且不可公开的长字符串>
ADMIN_EMAIL=admin@luzcorp.com
ADMIN_INITIAL_PASSWORD=<初始化后立即修改>
FRAPPE_API_TOKEN=<Frappe 接口密钥，格式 key:secret>
```

说明：

- `.env.local` 已被 Git 忽略，禁止提交真实密码。
- 生产环境必须配置 `DB_HOST=139.9.122.9`、`DB_PORT=3306`、`DB_NAME=cloud`；不要把生产连接配置写入代码。
- `DB_NAME=merge` 仅是本地开发默认值。切换数据库只修改部署服务器的 `.env.local`，不要修改业务代码。
- 生产环境不要使用 `root` 或弱密码账号，建议创建仅能访问 `cloud` 数据库的专用 MySQL 账号，并限制来源 IP。
- `DB_CONNECTION_LIMIT` 建议从 `5` 开始，避免多个应用进程造成数据库连接数过高。
- `AUTH_SESSION_TOKEN` 用于登录会话，生产环境必须设置为随机值，不要使用代码中的开发默认值。
- `ADMIN_EMAIL` 和 `ADMIN_INITIAL_PASSWORD` 只在初始化公共用户时使用；初始化后应在系统“账户管理”中修改管理员密码。
- 应用通过 `src/lib/db.ts` 将逻辑表名映射为 `merge_power_` 物理表名；集采、华为云和公共服务使用已定义的 `merge_po_`、`merge_cloud_`、`merge_common_` 物理表名。

### 4.1 读取远端 Frappe 数据的配置（需求单同步 / 物料同步）

需求单同步、物料同步、物流快照取数读的都是同一台远端 Frappe，**地址与密钥只解析一次**（见 `src/lib/frappe-config.ts`），配置项写在 `.env.local`：

| 变量 | 是否必填 | 说明 |
| --- | --- | --- |
| `FRAPPE_API_TOKEN` | **必填** | Frappe API 密钥，格式 `<api_key>:<api_secret>`。两个同步服务共用这一个 |
| `FRAPPE_API_BASE_URL` | 可选 | 远端地址，默认 `http://192.168.3.153:1337`（代码内默认值，见 `src/lib/frappe-config.ts`） |
| `FRAPPE_DEMAND_SYNC_PAGE_SIZE` | 可选 | 需求单同步分页大小，默认 200，上限 500 |
| `FRAPPE_DEMAND_SYNC_TIMEOUT_MS` | 可选 | 需求单同步单次请求超时，默认 30000，夹在 1000~120000 |
| `MATERIAL_SYNC_PAGE_SIZE` | 可选 | 物料同步分页大小，默认 100，上限 1000 |
| `MATERIAL_SYNC_TIMEOUT_MS` | 可选 | 物料同步单次请求超时，默认 30000 |
| `FRAPPE_DEMAND_SYNC_STATUSES` | 可选 | 只在需要**收窄**建档范围时配置，逗号分隔；留空则放开除取消外全部状态（远端新增状态自动进入范围，不用改代码） |
| `FRAPPE_DEMAND_CANCELLED_STATUSES` | 可选 | 视为取消的状态，默认 `Cancelled`；取消状态只写台账、不建档 |

历史变量名继续兼容，**不需要改也不用再新配**：`MATERIAL_API_TOKEN`、`MATERIAL_API_BASE_URL`、`FRAPPE_DEMAND_API_TOKEN`、`FRAPPE_DEMAND_API_BASE_URL`。解析顺序是"新名 → 历史名 → 代码默认值"。

示例（远端地址与端口和默认值一致时，只需要填令牌）：

```env
FRAPPE_API_TOKEN=xxxxxxxxxxxxxxxx:yyyyyyyyyyyyyyyy
```

注意事项：

- **部署服务器必须能访问 `192.168.3.153:1337`**（内网地址）。如果服务器不在该内网，需要在网络侧打通，或把地址换成对外可达的入口后配置 `FRAPPE_API_BASE_URL`。
- **已部署服务器如果曾在 `.env.local` 里写死过旧地址（`http://192.168.2.27:1337`），必须同步改成 `http://192.168.3.153:1337`**。代码内默认值只在未配置该变量时生效，不会覆盖 `.env.local` 里的旧值。
- **建档范围的默认口径是"除取消外全部放开"**（黑名单）：远端已有的 7 种履约状态和以后新增的状态都会建档，`Cancelled` 只写台账不建档，状态为空的明细按未知处理、不建档。只有显式配置 `FRAPPE_DEMAND_SYNC_STATUSES` 才会收窄成白名单。历史变量 `FRAPPE_DEMAND_ELIGIBLE_STATUS` 已不再读取，`.env.local` 里留着也不影响范围，可以顺手删掉。
- 密钥只在服务端读取，不会下发到浏览器。
- 修改 `.env.local` 后需要**重启应用进程**（`pm2 restart cloud_power --update-env`）才生效。

Linux 建议限制环境文件权限：

```bash
chmod 600 .env.local
```

## 5. 生产数据库结构状态与变更规范

生产数据库 `cloud` 已完成结构同步与基线数据补全，**本次代码发布不需要再执行任何数据库操作**。当前状态如下：

- 远程数据库：`cloud`（MySQL 8.0，`139.9.122.9:3306`）
- 表数量：153 张，其中 `merge_*` 业务表 90 张（`merge_power_` 53 张、`merge_common_` 17 张、`merge_po_` 14 张、`merge_cloud_` 6 张）
- `merge_*` 表数据总行数：20,034 行
- 所有表均为 InnoDB
- 所有表均已增加 `internalId INT UNSIGNED AUTO_INCREMENT`，并设置为主键
- 原 UUID、业务单号和原业务关联字段均保留，并保留唯一约束
- 结构与开发库逐列比对为 **0 差异**（列、类型、列顺序均已对齐）
- `receivableDate` 已补齐到华为云、供应商付款、服务费对账单和项目结算发票相关表
- 项目结算发票的承接单位、供应商/客户字段已补齐

复核结构是否仍然一致（只读，不会修改任何数据）：

```bash
TARGET_DB_HOST=139.9.122.9 TARGET_DB_PORT=3306 \
TARGET_DB_USER=cloud_wenjian TARGET_DB_PASSWORD='******' TARGET_DB_NAME=cloud \
npm run schema:sync-merge
```

该命令默认是 dry-run，输出 `新建表 0 张，新增列 0 个，类型调整 0 个，列顺序调整 0 个` 即表示结构一致。只有追加 `--apply` 才会执行变更。

## 5.1 首次初始化说明（仅适用于全新空数据库）

只有部署全新的空数据库时，才由数据库管理员使用仓库根目录的 `merge-schema-only.sql`。该文件包含当前版本的表结构、字段、索引、约束及中文备注，不包含业务数据、`INSERT` 和 `DROP TABLE`。

首次初始化全新数据库时：

1. 连接远程 MySQL `139.9.122.9:3306`，选中目标数据库 `cloud`。
2. 先确认数据库备份、字符集和账号建表权限。
3. 打开 `merge-schema-only.sql` 并执行。
4. 执行后确认 `merge_power_`、`merge_po_`、`merge_cloud_`、`merge_common_` 前缀的表均已生成。
5. 再按本文档第 4 节配置应用服务器的 `.env.local`。

如果目标数据库已经存在业务数据或同名表，不要直接导入该文件，不要删除或覆盖现有表。应先备份数据和附件，再导出结构并生成经过审核的增量 SQL。

## 5.2 当前远程数据库日常发布规则

远程 `cloud` 已完成 `InternalId` 迁移、结构对齐、国家默认值与 B6 规则等基线数据补全，以及派生的应付金额与算力价快照回填（明细见 5.3）。**日常代码发布只需要更新应用，不需要再次同步数据库结构。**

日常发布禁止执行：

```bash
npm run schema:init-merge
npm run schema:internal-id:apply
npm run migrate
npm run seed
npm run schema:apply
npm run schema:sync-remote
npm run schema:sync-merge -- --apply
npm run data:backfill-remote-baseline -- --apply
npm run data:backfill-remote-derived-data -- --apply
```

上述命令可能重复建表、修改结构、迁移主键或写入初始化数据。后续确有数据库变更时，必须单独制作增量 SQL，先在测试数据库验证，经负责人批准并完成备份后再执行。

本地开发库初始化仍可使用项目脚本：

```bash
npm run schema:init-merge
```

初始化注意事项：

- `merge-schema-only.sql` 只用于全新空数据库的首次或受控结构初始化，不属于普通发布步骤。
- `scripts/initialize-merge-schema.mjs` 面向本地开发库，不要在生产 `cloud` 数据库上直接执行。
- 对已有数据库执行任何结构 SQL 前必须完成数据库和 `uploads/` 备份，并先在测试库验证。
- 不要把 `npm run schema:init-merge`、`npm run migrate` 或 `npm run seed` 放入每次启动脚本。
- 结构 SQL 不会导入业务单据，也不会替代 Excel 业务导入流程；但执行前仍需确认目标数据库和表名。
- 只有确认目标数据库、代码版本、备份和维护窗口后，才允许执行初始化或结构变更。

## 5.3 本轮已完成的数据库变更记录

以下变更**已经在远程 `cloud` 执行完毕**，属于历史记录，发布时不需要重复执行。保留此清单是为了避免后续维护人员重复操作。

### 5.3.1 结构变更

| 对象 | 变更内容 |
| --- | --- |
| `merge_po_settlement_projects` | 新增 `acceptanceCompletedAt`（验收完成时间），位于 `acceptanceStartedAt` 之后 |
| `merge_cloud_supplier_payments` | 新增 `supplierPayableCurrency`、`supplierPayableNetAmount`、`supplierTaxRate`、`supplierTaxAmount`、`supplierPayableTotalAmount` |
| `merge_power_demand_sync_runs` / `_items` / `_mappings`、`merge_power_material_sync_runs` | 新建 4 张同步台账表 |
| 早期批次（23 列 + 5 处类型放宽） | 需求单远端状态、物流远端字段、实例型号类型、算力价测算字段等已补齐 |
| 列顺序 | 103 处按开发库顺序对齐（新增列曾落在表末尾，Navicat 中不易查找） |

### 5.3.2 基线数据

| 表 | 变更内容 |
| --- | --- |
| `merge_power_countries` | BR / CL / MX 三个国家的默认承接单位与默认客户，按 `undertakingUnitCode` 与名称映射到远程自身 UUID（两侧 UUID 不同，不能直接复制） |
| `merge_power_b6typeconfigs` | 补入 B61、B62-A7、B62-A8 (HT)、B63 (HT) 四条 B6 规则 |

### 5.3.3 派生数据

| 列 | 处理方式 | 影响行数 |
| --- | --- | --- |
| `merge_power_purchaseorderitems.currency` | 按采购单币种回填，兜底 USD | 279 |
| `merge_power_purchaseorderitems.powerPricingJson` / `powerFirst24VatIncluded` / `powerNext36VatIncluded` | 用现成的算力服务费测算函数重算 | 279 |
| `merge_power_billinginstanceledgers.customerId` | 按 `requestitems` 反查 | 274 |
| `merge_power_billinginstanceledgers.requestType` | 按请求单反查，兜底「整机」 | 2 |
| `merge_power_monthlybillingwriteoffs.customerId` | 按台账与请求明细反查 | 16,440 |
| `merge_power_monthlybillingwriteoffs.requestType` | 同上 | 120 |
| `merge_power_servicefeesnapshotitems.requestType` | 按月度账单/预付款来源反查 | 8 |
| `merge_cloud_supplier_payments` 应付字段 | 按 `merge_cloud_rows` 实时聚合口径落库 | 1 |

### 5.3.4 仍未回填、且不应手工回填的列

| 列 | 原因 |
| --- | --- |
| `merge_power_monthlybillingwriteoffs.selfCalculatedUnitPrice` / `differenceUnitPrice` / `differenceTotalPrice` | 由月账单生成流程计算，需在远程重新生成月账单，不能硬写 |
| `merge_power_monthlybillingwriteoffs.adjustmentNo` | 结差来源标记，流程产物 |
| `merge_power_requests.remoteStatus` / `remoteStatusUpdatedAt` | 需求单同步再次执行时自动写入 |
| `merge_power_shipments` 的 `dcCode`、`crd`、`apdAt` 等节点时间 | 物流同步或采购确认时写入 |
| 各类 `createdByUserId` 等审计字段 | 业务操作时写入，历史行无值属正常 |

### 5.3.5 巡检脚本

以下四个脚本默认都是 dry-run，只打印计划，只有显式追加 `--apply` 才写库。执行前必须完成备份并取得审批。

| 命令 | 用途 |
| --- | --- |
| `npm run schema:sync-merge` | 全量比对 90 张 `merge_*` 表的结构（早期脚本只比对 50 张纳管表，曾因此漏掉项目结算字段） |
| `npm run data:backfill-remote-baseline` | 国家默认值、B6 规则等基线数据，幂等 |
| `npm run data:backfill-remote-derived-data` | 币种、算力价快照、customerId、requestType 等派生数据，幂等；`--validate` 可用本地数据自检测算管线 |
| `npm run data:check-remote-gaps` | 只读巡检本地有值、远程为空的列，输出缺口清单 |

## 5.4 最近一次发布范围（2026-09-20 物流时间按 Release 落地与批量刷新入口）

本次发布是**代码 + 数据库结构变更**，涉及 `merge_power_shipments` 新增两列。**这两列已经在生产库执行完毕**，上线时不要再跑 `npm run migrate`。

### 5.4.1 需求

1. 物流列表的时间字段口径按业务确认重定：CRD、供应商反馈ETA（新增）、APD交单、ASD提货、起飞/开船、到港、清关完成、派送。
2. 运输方式拉取不对：远端是海运的单，本地显示空运或待安排。
3. 需要确认机房、收货地址、收件人、收件电话是否真的来自远端。
4. 存量数据要能批量刷，且前端要有入口。

### 5.4.2 字段映射（已落地）

| 本地列 | 显示名 | 远端来源 | 说明 |
| --- | --- | --- | --- |
| `crd` | CRD | `Demand Order Item.requested_delivery_date` | 客户需求到货时间 |
| `supplierEtaAt` | 供应商反馈ETA | `EDI Order.estimated_delivery_date` | **新增列** |
| `apdAt` | APD交单 | `SL Shipment.actual_ready_at` | 供应商确认备货完成 |
| `pickupAt` | ASD提货 | `SL Shipment.actual_shipped_at` | 供应商确认发货 |
| `departedAt` | 起飞/开船 | `SL Shipment.actual_departed_at` | |
| `arrivedAt` | 到港 | `SL Shipment.actual_arrived_at` | |
| `customsClearedAt` | 清关完成 | `SL Shipment.customs_cleared_date` | |
| `deliveredAt` | 派送 | `SL Shipment.signed_date` | 签收 |
| `transportMode` | 运输方式 | `Demand Order.transport_mode` | `Sea→海运`、`Air→空运`，新取值原样带回 |

映射表定义在 `src/lib/remote-shipment-timeline.ts` 的 `REMOTE_SHIPMENT_TIMELINE_FIELDS`，每条带 `source`（`demandItem` / `release` / `shipment`）。

### 5.4.3 时间按 Release 落地（不是按需求单号）

一个需求单可以跨多个 Release，每个 Release 的时间和物流单不同。实测 DO-00049 的 7 条明细分属 4 个 Release，其中两个有时间且不一样（SHP-00021 签收 2026-03-02、SHP-00035 签收 2026-02-27）。按需求单号写时间会把别条明细的签收日套上去，所以：

- 新增 `releaseId` 列，本地物流行记录所属 Release，时间从该 Release 读。
- 归属链路：`shipment.deviceCode → 实例型号映射 → MAT-xxxxx`，与 `shipment` 的需求单号一起定位唯一 DOI，再经 `EDI Order Item.demand_order_item` 找到 Release。
- 依据：实测非取消状态下 `(需求单, 物料)` 247 个组合**零重复**；`requested_delivery_date` 在 163 个 Release 内**完全一致**，所以 CRD 在 Release 级也良定义。
- 定位不到 Release 时留空，不写别的明细的时间。

### 5.4.4 修了一个关联 bug

批量刷新原来对大部分物流行报"未关联需求单"，原因是这两处 SQL 的关联键不成立：

```sql
LEFT JOIN purchaseorderitems poi ON poi.id = s.purchaseOrderItemId   -- 对不上
```

物流行存的是 `POI-<poNo><purchaseOrderId>-<序号>`，采购明细的 id 是 `POI-<purchaseOrderId>-<序号>`，中间多一段 poNo 前缀，本地库 283 行里有 **277 行 join 落空**。已改成"id 相等 **或** 物流 id 以明细 id 去掉 `POI-` 前缀后的内容结尾"（`SHIPMENT_TO_PO_ITEM_JOIN`），两种格式都能命中：本地 277/277、生产 279/279，且都不产生重复行。

### 5.4.5 批量刷新入口

原来的按钮「补齐待补全物流」只扫 `remoteLogisticsSourceStatus = 'pending'`，也就是"采购确认时远端不可用、先留空生成"的行；历史导入的 `legacy` 行永远进不了范围，所以点了没反应。现在：

- 按钮改名 **「批量刷新远端物流」**，扫描范围放宽到**所有非 `remote` 来源**（`pending` + `legacy` + 状态为空）。
- 成功即写入并把来源改成 `remote`；远端确实没有该需求单的保持原样并回报原因，不会把 `legacy` 误标成 `pending`。
- 单行「重新拉取」保留，用于只刷某一条。
- 命名一并理顺：`synchronizePendingRemoteLogistics` → `synchronizeRemoteLogistics`，接口 `…/shipments/pending-logistics` → `…/shipments/refresh-logistics`。

### 5.4.6 数据库变更（**已在生产执行**）

| 列 | 定义 | 位置 |
| --- | --- | --- |
| `releaseId` | `VARCHAR(64) NULL COMMENT 'remote EDI Order release id'` | `merge_power_shipments`，`logisticsSnapshotAt` 之后 |
| `supplierEtaAt` | `DATE NULL COMMENT 'supplier committed ETA from remote EDI Order'` | `merge_power_shipments`，`deliveredAt` 之后 |

`scripts/migrate.ts` 里已按 `addColumnIfMissing` 幂等写入，**新环境初始化时会自动创建，生产库不需要再执行**。

### 5.4.7 存量数据回填结果（2026-09-20 实测）

| 指标 | 生产库刷新前 | 生产库刷新后 |
| --- | --- | --- |
| 来源 `remote` | 0 | **142** |
| 来源 `legacy` | 279 | 137 |
| 运输方式「待安排」 | 279 | 137 |
| 运输方式「空运」 | 0 | 74 |
| 运输方式「海运」 | 0 | 68 |
| 机房填充 | 0 | 142 |
| Release ID 填充 | — | 126 |
| 供应商反馈ETA 填充 | — | 46 |

扫描 279 条、成功 142 条、远端无数据 137 条，耗时 8 秒；**重跑 0 变化**，幂等。剩余 137 条的需求单在远端系统里确实不存在（远端只有 155 张需求单，本地有一批更早的历史单）。

操作前已把 279 行完整备份到执行机器的系统临时目录（`prod-shipments-backup-<时间戳>.json`）。

### 5.4.8 改动文件

| 文件 | 说明 |
| --- | --- |
| `src/lib/remote-shipment-timeline.ts` | 重写：带来源实体的映射表、`pickReleaseTimeline()` 归属规则、`REMOTE_ONLY_SHIPMENT_COLUMNS` |
| `src/lib/frappe-demand-sync-service.ts` | 新增 `loadRemoteShipmentTimelines()`（DOI → Release → Shipment 关联）、`localTransportMode()`，远端快照补 `transportMode` |
| `src/lib/procurement-service.ts` | 关联修复、按 Release 落库、三条链路（采购确认生成 / 批量刷新 / 单行重新拉取）接通、差异提示日期格式 |
| `src/app/api/procurement/shipments/refresh-logistics/route.ts` | 新接口（原 `pending-logistics` 删除） |
| `src/components/entity-page.tsx`、`order-*.tsx` | 按钮与提示文案 |
| `src/lib/modules.ts` | 列表新增「供应商反馈ETA」，表单新增该字段与隐藏的「远端ReleaseID」 |
| `scripts/migrate.ts` | 两个 `addColumnIfMissing` |

### 5.4.9 上线步骤

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

**不要执行 `npm run migrate`**，两列已经在生产库加好。上线后让用户 `Ctrl + Shift + R` 强制刷新。

## 5.5 上一轮发布范围（2026-09-20 远端 Frappe 地址切换与同步配置合并）

本次发布是**代码与配置改动，不涉及数据库结构变更**，上线时不要执行任何数据库命令。

**需求**：远端 Frappe（物流 / 需求单系统）从 `192.168.2.27` 换成 `192.168.3.153`（端口仍是 `1337`）。借这次切换把原先分散在两个同步服务里的地址 / 密钥解析合并到一处，避免以后再换 IP 时改漏位置。

**改动**：

| 位置 | 说明 |
| --- | --- |
| `src/lib/frappe-config.ts`（新增） | 远端 Frappe 地址与密钥的唯一解析入口：`resolveFrappeEndpoint()` / `resolvePageSize()` / `resolveTimeoutMs()` |
| `src/lib/material-sync-service.ts` | 删除自带的 `DEFAULT_MATERIAL_API_BASE_URL` 与内联解析，改用公共配置 |
| `src/lib/frappe-demand-sync-service.ts` | 删除自带的 `DEFAULT_API_BASE_URL` 与内联解析，改用公共配置；顺带删掉与 `sourceTypes` 完全重复的 `activeMappingSourceTypes` 及其类型断言 |
| `.env.example` | 合并为 `FRAPPE_API_BASE_URL` + `FRAPPE_API_TOKEN` 两项；删除代码里从未读取的 `FRAPPE_DEMAND_REQUEST_PREFIX`；补上 `AUTH_SESSION_TOKEN` / `ADMIN_EMAIL` / `ADMIN_INITIAL_PASSWORD` |
| `docs/superpowers/specs/*` | 设计稿里的远端地址同步更新 |

变量解析顺序：`FRAPPE_API_*`（新名） → `MATERIAL_API_*`、`FRAPPE_DEMAND_API_*`（历史名，继续兼容，不必改名） → 代码内默认值。空白值按未配置处理。

**需求单建档状态改为黑名单口径**：默认除 `Cancelled` 外全部放开，远端以后新增状态会自动进入同步范围；`Cancelled` 只写台账不建档，状态为空按未知处理。同时**移除了历史变量 `FRAPPE_DEMAND_ELIGIBLE_STATUS` 的兼容读取** —— 此前它一旦被设置（旧模板里是 `=Committed`）就只对 `Committed` 建档，会把放开范围静默收窄回单一状态。仍想收窄请显式配置 `FRAPPE_DEMAND_SYNC_STATUSES`。

**升级动作（重要）**：

1. 部署服务器的 `.env.local` 若写死过 `http://192.168.2.27:1337`，改成 `http://192.168.3.153:1337`。
2. `.env.local` 里若残留 `FRAPPE_DEMAND_ELIGIBLE_STATUS=Committed`，本次升级后它**不再被读取**，可以保留也可以删除，都不会影响建档范围。
3. 改完 `.env.local` 必须重启进程（`pm2 restart cloud_power --update-env`）。

**实测**：

| 检查项 | 结果 |
| --- | --- |
| `http://192.168.3.153:1337/api/method/ping` | `200 {"message":"pong"}` |
| 带密钥读取 `Demand Order` | `200`，正常返回记录 |
| `npx tsx scripts/sync-frappe-demands.ts --dry-run` | `fetchedItems: 277`、`eligibleItems: 247`、`errors: []` |
| `npx tsc --noEmit` / `npm test` / `npm run build` | 通过（75 个测试文件 336 项、145 个路由） |

远端真实状态分布（277 条明细）：`Received` 148、`Committed` 96、`Cancelled` 30、`Handed Over` 2、`Shipped` 1；没有空状态，也没有白名单外的状态。所以改成黑名单口径后**存量数据的同步结果完全不变**（277 − 30 取消 = 247，与试运行一致），变化只体现在"以后远端新增状态会自动进入范围"。

新增 `src/lib/frappe-config.test.ts` 守住配置回退链（新变量名优先、历史变量名可用、两个同步服务解析到同一地址、空白值按未配置处理）；`src/lib/frappe-demand-sync-service.test.ts` 补了 7 项状态范围用例（默认放开全部、未知状态放开、空状态不建档、白名单收窄、历史变量失效、取消状态可配置）。

已核对：**没有 `*.sql`、没有 `src/lib/db.ts`、没有 `package-lock.json` 改动**，不需要执行任何数据库命令。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

## 5.6 上一轮发布范围（2026-09-20 预付款合同实例明细支持单独/批量退回）

本次发布为**纯前端改动，不涉及数据库，也没有接口行为变化**，上线时不要执行任何数据库命令。

**需求**：从「待生成预付款实例」勾选生成预付款合同后，如果勾错了，原来只能整单删掉重建，不能单独退回某一项。

**改动**（`src/components/prepayment-contract-detail-page.tsx`）：

| 能力 | 说明 |
| --- | --- |
| 单项退回 | 实例明细表新增"操作"列，每行一个「退回」按钮，把该条实例移出本合同 |
| 批量退回 | 行首新增勾选框 + 表头全选，配合标题栏的「批量退回」按钮；标题栏显示"已选 N 项 / 共 N 项"，未勾选时按钮置灰 |
| 空状态 | 实例明细清空后显示"暂无实例明细 / 请到「待生成预付款实例」勾选后生成合同" |
| 只读态 | 已确认的合同不显示勾选框与操作列，保持原样 |

勾选框使用已有的 `table-select-cell` 紧凑列样式（宽 `2.5rem`、左右 padding `0.5rem`），不占用过多横向空间。

**不需要改后端**：「待生成预付款实例」的口径本身就是"未被**草稿/已确认**合同占用的采购明细"，所以退回后一保存，实例会自动回到待生成列表。合同总金额本就按明细实时计算，退回后立刻跟着变。

**实测**（本地草稿合同 `PPC-20260912`）：

| 步骤 | 该采购明细是否在待生成列表 |
| --- | --- |
| 在合同中 | 否 |
| 退回并保存 | **是**（合同明细变为空） |
| 还原后 | 否（又被占用） |

**交互约定**：退回是改本地状态、点「保存草稿」才落库，与费用明细的"删除"一致；退回后需要保存，直接关闭标签页会丢失改动。

**未改动**：费用明细表已有单行「删除」，本次未给它加批量（业务确认不需要）。

已核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**，也没有改动任何 API 路由。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。构建后必须重启，并让用户 `Ctrl + Shift + R` 强制刷新。

## 5.7 上一轮发布范围（2026-09-20 预付款合同号可修改、统一滚动条与下拉样式定稿）

本次发布为**纯代码改动，不涉及数据库结构变更**，上线时不要执行任何数据库命令。

### 5.7.1 预付款合同草稿可修改合同号

**需求**：预付款合同退回草稿后有时需要改合同号，原来该字段写死为只读。

**改动**：

- 前端 `prepayment-contract-detail-page.tsx`：`预付款合同号` 在草稿状态下可编辑；保存时随其他字段一起提交；若改了号，保存成功后自动把详情路由切到新单号（否则后续操作仍指向旧号）。
- 接口 `PUT /api/prepayments/contracts/{合同号}`：接受 `contractNo` 字段作为目标合同号。
- 服务 `updatePrepaymentDraft`：
  - 新增重名校验，改成已存在的合同号会提示"预付款合同号 xxx 已存在，请换一个"；
  - 合同号是**唯一键**，且下游 4 张表靠它关联（`prepaymentcontractitems`、`monthlyprepaymentwriteoffs`、`prepaymentwriteoffadjustments`、`prepaymentwriteoffadjustmentitems`），表间**没有外键约束**，所以改名时自带这些表，并且**与明细替换放在同一个事务**里，避免"名字改了但保存失败"的半截状态；
  - "采购明细已被占用"的归属校验改用**库里当前的合同号**比对，否则改名会被自己挡住。

**实测**：改名前 `PPC-20260912`（草稿、1 条明细、总额 12,340）→ 改名后新号存在、旧号消失、明细跟迁 → 同号保存不误报 → 改回原名与初始状态完全一致（测试数据已还原）。

**限制**：只有**草稿**可改；改名后旧明细行的 `id`（形如 `PPCI-旧合同号-001`）保留原样，它只是内部标识，不影响功能。

### 5.7.2 全站滚动条统一

原来只有表格容器自定义过滚动条，其余都是浏览器默认——**左侧深色导航上压着一条浅灰粗滚动条**，最扎眼。现在一处定义全站生效：

```css
* { scrollbar-width: thin; scrollbar-color: var(--color-scroll-thumb) transparent; }
*::-webkit-scrollbar-thumb {
  background-color: var(--color-scroll-thumb);   /* rgb(144 147 153 / .45) */
  background-clip: content-box;                  /* 滑块比轨道窄，留呼吸空间 */
  border: 2px solid transparent;
  border-radius: 9999px;
}
.app-sidebar nav::-webkit-scrollbar-thumb { background-color: rgb(255 255 255 / .28); }
```

| 维度 | 之前 | 现在 |
| --- | --- | --- |
| 宽度 | 17px（系统默认） | 10px，滑块实宽 6px |
| 轨道 | 实心浅灰 / 系统灰 | 透明 |
| 滑块 | 方形带箭头 | 圆角胶囊、半透明灰，悬停加深 |
| 深色导航 | 浅灰滑块 | 白色半透明（悬停加深） |
| Firefox | 未处理 | `scrollbar-width: thin` 同步 |

原 `.table-scroll` 的独立滚动条定义已删除，全站只有一处滚动条样式。

### 5.7.3 搜索下拉样式定稿

按业务确认的样式调整 `SearchSelect`（详细排查与迁移见 5.7 节）：

| 项 | 定稿 |
| --- | --- |
| 输入框右侧 | 保留下拉箭头（有值时换清除按钮） |
| 展开位置 | 输入框下方 4px |
| 面板宽度 | 按内容自适应，保证全称完整显示，最宽不超出视口 |
| 圆角 / 阴影 | rounded（4px） / 偏重阴影 |
| 左右内边距 | px-3（12px） |
| 选项行 | **两行**：主值加粗 14px；补充文字 12px 灰色、不加粗；两行间距 4px |
| 关键词命中 | 加粗 |

### 5.7.4 改动文件

- `src/lib/prepayment-service.ts`、`src/app/api/prepayments/contracts/[contractNo]/route.ts`、`src/components/prepayment-contract-detail-page.tsx`
- `src/app/globals.css`、`src/components/search-select.tsx` 及 4 个调用页（`request-order-form-page`、`cloud-reconciliation-page`、`settlement-project-detail-workspace`、`customer-po-page`）
- `docs/preview/`（三份可直接打开的样式预览页，纯参考）

已核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。构建后必须重启，并让用户 `Ctrl + Shift + R` 强制刷新。

## 5.8 上一轮发布范围（2026-09-18 统一搜索选择控件并补充调整单列表退回入口）

本次发布为**纯前端改动，不涉及数据库，也没有接口行为变化**，上线时不要执行任何数据库命令。

### 5.8.1 新增 `SearchSelect`，统一"搜索 + 选择"控件

系统里这类控件原有 **16 处、两套完全不同的实现**：

- 11 个浏览器原生 `<datalist>`（样式不可控、无空态/加载态/键盘操作，各浏览器表现不一）
- 5 处自研"输入 + 下拉面板"（面板定位、圆角、阴影、hover 色、z-index、选项文案各不相同）

新增 `src/components/search-select.tsx` 统一为：

| 维度 | 统一后 |
| --- | --- |
| 选项 | **两行显示**：第一行主值（编码），第二行灰色说明（机型 / 英文名 / 规格） |
| 外观 | 复用 `Input` 的高度 / 圆角 / 边框 / 聚焦环，右侧固定图标（有值时显示清除按钮） |
| 面板 | `fixed` 定位（不被表格 `overflow` 裁切）、统一圆角 / 阴影 / 滚动条 / `z-[120]` |
| 键盘 | ↑↓ 移动、Enter 选中、Esc 关闭、Tab 关闭（原来 16 处全都没有） |
| 条数 | 默认最多渲染 50 条，超出提示"共匹配 N 条，仅显示前 50 条" |
| 空态 / 加载态 | 统一文案与样式 |
| 自由输入 | **不支持**，只允许从选项中选择 |

已迁移的 11 处：需求单表单（4 处，删除旧 `SearchPicker`）、华为云对账 `PartnerSelect`、项目结算 `PartnerLookup`、客户PO 的伙伴筛选与产品匹配、月账单调整单实例编码、账单生成实例合同号、价格版本设备编码与 B6 类型、伙伴档案自定义可选字段。另删除了需求单表单里一个**没有任何输入框绑定的死 datalist**（原来白白渲染全部实例型号）。

**行为变化，需要知会使用方**：

1. 需求单的实例编码不再允许手输系统里没有的编码，只能从实例档案里选（未建档需先建档）。
2. 原生下拉换成自研面板：以前点击可能不弹出、只能靠输入过滤；现在点击即弹出全量（前 50 条）列表。
3. 下拉选项新增第二行说明。

**尚未迁移**：`entity-page.tsx` 的 5 个 datalist（实例合同-实例编码、实例型号-B6类型、客户PO/报价明细-产品编码、产品档案-产品分类、账单台账-合同号）。原因：它们同处一个通用档案表单，且这些字段用的是 `value` + `defaultValue` 混写的**非受控**写法，直接换成受控组件会导致"编辑已有档案时回显为空"，需要先改造该表单，属独立一轮。

### 5.8.2 两个调整单列表补充「退回草稿」入口

实例合同调整单、预付款核销调整单的**退回草稿功能本身早已实现**（服务、API、详情页按钮都在，提交 `4838ebc`），但入口只在详情页——列表页操作列只有"查看/编辑、确认、删除"，从列表看会以为没有这个功能。

本次在两个列表页的操作列补上「退回草稿」按钮（已确认时可点，草稿时置灰），顺序为 `查看/编辑 → 确认 → 退回草稿 → 删除`，走同一套 API 并弹确认提示，成功后自动刷新列表。

**还原逻辑已实测验证**（本地实跑）：

| 对象 | 验证结果 |
| --- | --- |
| 实例合同调整单 | 已确认 → 退回：受影响台账明细按要求重算（58 行调整单标记消失、金额合计回到未调整值）；再确认后与退回前**完全一致** |
| 预付款核销调整单 | 确认后金额 +100、来源标记为"调整单"；退回后金额与来源标记**完全还原**成调整前状态 |

已知边角问题（本次未改）：预付款核销调整单退回时把来源标记硬编码为"首次生成"并清空调整单号；若同一行被两张调整单先后调整过，退回后一张时金额正确，但来源标记会被清成"首次生成"。仅影响"来源"列显示，不影响金额。

### 5.8.3 改动文件

- `src/components/search-select.tsx`（新增）
- `request-order-form-page.tsx`、`cloud-reconciliation-page.tsx`、`settlement-project-detail-workspace.tsx`、`customer-po-page.tsx`、`billing-adjustment-detail-page.tsx`、`billing-available-page.tsx`、`capex-pricing-page.tsx`、`party-archive-detail-page.tsx`
- `billing-adjustments-page.tsx`、`prepayment-writeoff-adjustments-page.tsx`（列表退回入口）
- `DESIGN.md`、`docs/superpowers/specs/2026-09-18-search-select-unification-design.md`

已核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。构建后必须重启，并让用户 `Ctrl + Shift + R` 强制刷新。

## 5.9 上一轮发布范围（2026-09-18 修复预付款合同承接单位/供应商/客户名称口径不一致）

本次发布为**单文件前端改动，不涉及数据库，也没有接口行为变化**，上线时不要执行任何数据库命令。

**现象**：预付款合同编辑页，上方"实例明细"表格里的承接单位、供应商、客户显示**全称**，同一页下方"费用明细"里同样这三列却是**简称**。

**根因**：`src/components/prepayment-contract-detail-page.tsx` 里两处调用同一个 `partyShortName` 辅助函数，但传入的字段优先级不同：

```ts
// 上方实例表格：只给全称字段，没有传 shortName
partyShortName(..., ["entityName", "name"])   // 承接单位
partyShortName(..., ["nameCn"])               // 供应商
partyShortName(..., ["nameCn", "name"])       // 客户

// 下方费用明细下拉框：简称优先
partyOptionLabel(unit, [...], ["shortName", "entityName", "name"])
```

而 `partyShortName` 只按传入顺序取值、不会自己兜 `shortName`，所以上方必然取到全称。

**修复**：上方实例表格的三处取值顺序改成与下方一致——**简称优先，其次全称，最后回退原始 id**。

**全系统排查结论**（本次一并核对）：

| 检查项 | 结果 |
| --- | --- |
| 后端 SQL 里取人名的 `COALESCE` 取值顺序 | 68 处，全部"简称优先"，0 异常 |
| 前端内联写字段优先级的数组 | 3 处异常，即本次修复的 |
| 前端按 id 反查名称的写法 | 全系统仅此一页 |
| 档案列表、档案详情页标题 | 有意以全称为主（档案列表同时带简称副标题），按业务确认保持现状 |

实测对照（本地数据）：

| 主数据 | 修复前 | 修复后 |
| --- | --- | --- |
| 承接单位 BRGS01 | `LUZ BRAZIL LTDA` | `Luz 巴西` |
| 承接单位 MXGS01 | `LUZ NEWMEDIA, S.A. DE C.V.` | `LUZ NEWMEDIA` |
| 供应商 supply01 | `XFUSION INTERNATIONAL PTE. LTD.` | `HT98` |
| 供应商 supply02 | `Da Chuang Precision Intelligent Manufacturing (Kunshan) Co.,Ltd.` | `DC` |

改动文件：`src/components/prepayment-contract-detail-page.tsx`（4 行）。

已核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。构建后必须重启，并让用户 `Ctrl + Shift + R` 强制刷新。

## 5.10 上一轮发布范围（2026-09-17 修复调整单明细重复主键并统一吸顶表头）

本次发布为**代码改动，不涉及数据库结构变更**，上线时不要执行任何数据库命令。

### 5.10.1 实例合同调整单明细「重复主键」修复

**现象**：录入调整单后回退，删掉其中一条明细又在末尾新增一条，保存时一直报 `Duplicate entry '...' for key '...'`；报错里的 id 在数据库里查不到对应数据。

**根因**：明细主键是按「明细在数组里的第几条」拼出来的：

```ts
id: item.id?.trim() || `BAI-${adjustmentNo}-${String(index + 1).padStart(3, "0")}`
```

已有 id 会沿用，新增行则按下标生成。**删掉中间一条再在末尾新增**时，新行落在已被占用的序号上，直接撞主键唯一索引 `uk_internal_legacy_merge_power_billingadjustmentitems(id)`。只有"删的不是最后一条"才会触发，所以表现为偶发。

**为什么数据库看着"没有这条数据"**：保存不是事务——先 `DELETE` 本调整单全部明细、再逐条 `INSERT`，中途报错就停在半路，留下"删了一半、插了一半"的数据。

**修复**：

| 项 | 内容 |
| --- | --- |
| 明细 id | 新增 `src/lib/draft-line-ids.ts` 的 `createLineId(prefix)`，用随机后缀彻底摆脱数组下标 |
| 保存事务 | `DELETE` + 逐条 `INSERT` 放进 `withTransaction`，失败整体回滚，不再产生半截数据 |
| 重复自检 | 新增 `assertUniqueLineIds`，同一批明细 id 重复时提示"第 X 条与第 Y 条的明细编号重复（...），请删除其中一条后重新添加"，不再把 MySQL 的报错直接抛给用户 |
| 采购订单表单 | 采购明细保存由"无脑 POST"改为 upsert（先查后 PUT/POST），修掉"首次保存中途失败后重试必然撞主键"的问题 |
| 预付款核销调整单 | 同样的"删后插"写法补上事务 |

**刻意未改动**：预付款合同明细、月账单/内部服务费/预付款核销的 `MBW-{台账}-{月份}`、`ISF-{台账}-{月份}`、`MWO-{明细}-{月份}` 等 id 保持确定性——它们的"确定性"正是幂等的前提（同一台账同一月只能有一行），改成随机值会造出重复月份数据。

**已卡住的单据**：代码更新后用原操作再保存一次即可；若明细已被半截保存弄得不完整，重存会用界面上的当前明细覆盖回去。

### 5.10.2 吸顶表头

长表格滚动时表头常驻。做法是集中两行 CSS，不在各页面重复写类名：

```css
.table-viewport { max-height: calc(100dvh - 210px); overscroll-behavior: contain; }
.table-scroll > table > thead th { position: sticky; top: 0; z-index: 20; background-color: var(--color-canvas); }
```

要点：

- `position: sticky` 只在**最近的滚动容器**内生效，所以容器必须有确定高度（`.table-viewport`）并自己滚动，单纯给表头加 `sticky` 是无效的。
- 选择器用 `.table-scroll > table > thead th`（直接子级），展开行里的嵌套小表不受影响。
- 应用范围 50 个列表容器；华为云 3 张表本来就是 `h-full` 自滚动，无需再加。
- 去掉华为云对账、首页看板表头上原来行内写的 `sticky top-0`，避免两套写法并存。

### 5.10.3 改动文件与本轮范围

- `src/lib/draft-line-ids.ts`（新增）、`src/lib/draft-line-ids.test.ts`（新增）
- `src/lib/billing-service.ts`、`src/lib/prepayment-adjustment-service.ts`
- `src/components/purchase-order-form-page.tsx`
- `src/app/globals.css`、50 个列表组件（加 `table-viewport`）
- `DESIGN.md`、`docs/superpowers/specs/2026-09-17-ui-consistency-audit.md`

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**。明细 id 规则变化只影响新建明细，存量数据不受影响。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。构建后必须重启；样式部分需让用户 `Ctrl + Shift + R` 强制刷新。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 调整单明细 id | 新建明细的 id 变为 `BAI-{调整单号}-{8位随机}`；删中间一条再新增可正常保存 |
| 重复 id | 提交前自检并给出可读提示，不再出现 `Duplicate entry` |
| 保存失败 | 明细整体替换进事务，失败后数据保持原样 |
| 采购订单表单 | 明细保存幂等，保存失败后可直接重试 |
| 吸顶表头 | 50 个列表容器内滚动时表头常驻；嵌套小表不受影响 |

## 5.11 上一轮发布范围（2026-09-17 统一空状态、色值与分页）

本次发布为**纯前端样式改动，不涉及数据库，也没有接口行为变化**，上线时不要执行任何数据库命令。

本次发布的改动（33 个文件）：

- **空状态统一**：15 处手写的纯文本空状态（`<td>暂无数据</td>`，无图标、`py-10`/`py-12` 不一致）全部改为公共组件 `EmptyState`，`py-10` 归一为 `py-12`；业务文案保留（"暂无映射""暂无供应商付款"等）。至此全系统空状态 55 处全部走 `TableStateContent` / `EmptyState`。
- **硬编码色值归到设计令牌**：`src/app/globals.css` 新增 6 个确实缺失的令牌（取值与原来完全一致，无视觉变化）：`--color-skeleton`、`--color-nav-ink`、`--color-nav-ink-soft`、`--color-info-ink`、`--color-danger-border-strong`、`--color-warning-border-soft`；其余 66 处硬编码色值归到已有语义令牌。**结果：全系统硬编码色值 0 处。**
- **分页统一**：`customer-po-page` 的自绘分页条替换为 `PaginationBar`，至此 16 个列表页分页完全一致。
- **查询按钮配色**：同步台账/映射、华为云对账的"查询"按钮补 `tone="secondary"`，与其它页面一致。
- `docs/superpowers/specs/2026-09-17-ui-consistency-audit.md`（排查报告与修复记录）。

其中色值合并是本次最实质的变化，修掉了几组"同色系不同深浅"的历史遗留：

| 原硬编码 | 处数 | 归到 |
| --- | --- | --- |
| `#f4f9ff` / `#f5fbff` / `#e6f4ff` / `#f4faff` / `#f0f5ff` | 10 | `bg-info-soft`（原来有 5 种不同的浅蓝底） |
| `#d9ecff` / `#c6e2ff` / `#b8d8f8` | 8 | `border-info-border` |
| `#fff7e6` / `#fff8e6` / `#fffdf5` | 5 | `bg-warning-soft` |
| `#f0fff7` / `#f0fff4` | 2 | `bg-success-soft` |
| `#13a85a` / `#13a561` / `#13a65b` | 3 | `text-success-dark` |
| `#e5e7eb` / `#e4e7ed` / `#d9e2ec` | 4 | `border-line` |
| 其余 | 34 | 对应语义令牌或本次新增令牌 |

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**，也没有改动任何 API 路由。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。**纯样式改动，构建后必须重启并让用户 `Ctrl + Shift + R` 强制刷新**，否则浏览器会继续用旧样式。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 空状态 | 所有列表的空状态统一为"图标 + 文案"；手写纯文本空状态 15 处 → 0 处 |
| 颜色 | 全系统不再有 `bg-[#...]` / `text-[#...]` / `border-[#...]` 硬编码；几组近似色合并为同一语义令牌 |
| 分页 | 客户 PO 列表的分页样式与其它列表一致（共 X 条 / 当前 X-X 条 / 条每页 / 上一页下一页 / 跳转） |
| 查询按钮 | 同步台账、同步映射、华为云对账的"查询"按钮由默认样式改为次级样式 |

## 5.12 上一轮发布范围（2026-09-17 统一下拉框、表头与表格容器样式）

本次发布为**纯前端样式与组件改动，不涉及数据库，也没有接口行为变化**，上线时不要执行任何数据库命令。

本次发布的改动（43 个源码文件 + `DESIGN.md`）：

- `src/components/ui.tsx`：新增 `Select` 公共组件，与 `Input` 使用同一套高度、圆角、边框和聚焦样式。
- 34 个页面把 **84 个裸 `<select>`** 全部替换为 `<Select>`（其中十几个原来完全没有 className，用的是浏览器默认样式），基础样式类收敛到组件内部，页面只传 `w-full` / `min-w-*` 等布局类。
- 全部 `<th>` 规范化：补齐 `whitespace-nowrap`、`font-medium` 和缺失的对齐类，`py-2` 归到 `py-3`（共修正 68 处）。
- 表格容器统一为 `table-scroll overflow-auto`：清除 3 处写死的 `max-h-[calc(100vh-XXXpx)]`；华为云对账 3 张表补上 `table-scroll`（保留 `h-full` 填满视口的行为）；导入中心 2 张表补上 `table-scroll`。
- `DESIGN.md`：修正 6.2 节的时间格式（原写 `YYYY-MM-DD HH:mm:ss`，与现行"到分"规范冲突）；新增 6.4 节「组件与类名约定」。
- `docs/superpowers/specs/2026-09-17-ui-consistency-audit.md`（本轮排查报告）。

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**，也没有改动任何 API 路由的返回结构。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。**本次是纯样式改动，构建后必须重启并让用户 `Ctrl + Shift + R` 强制刷新**，否则浏览器会继续用旧的样式 chunk。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 下拉框 | 全系统 87 个下拉框统一为 `Select` 组件：高度统一 `h-9`，聚焦时统一描边+渐变环，不再出现浏览器默认样式；原来固定宽度（`w-24` / `w-28` / `w-[104px]` 等）的保留宽度 |
| 表头 | 所有表头统一加粗、不折行、行高一致；原来不加粗的 37 处、会折行的 24 处、行高偏矮的 10 处全部修正 |
| 表格容器 | 滚动条样式统一（细滚动条、圆角滑块）；同步台账/映射、项目结算、集采发票汇总三页不再有写死的视口高度，列表随内容增长、整页滚动 |
| 华为云对账 | 列表仍然填满视口（保留 `h-full`），滚动条样式与其它模块统一 |

## 5.13 上一轮发布范围（2026-09-17 统一日期时间与列表显示规范）

本次发布为**纯代码改动，不涉及任何数据库结构变更**，上线时不要执行任何数据库命令。

本次发布的改动（47 个文件，其中 4 个页面文件删除）：

- `src/lib/display-format.ts`：新增 `normalizeTemporalFields`（接口返回前统一格式化 DATE / DATETIME），导出 `formatMoneyValue` / `formatNumberValue`；日期时间统一 `YYYY-MM-DD HH:mm`，日期统一 `YYYY-MM-DD`。
- 详情接口统一收口（7 个）：`order-detail-service`、`settlement-project-service`、`api/po/customer-pos`、`listCloudMappings`、`normalizeCloudDateFields`、`listImportJobs`、`getDocumentItems`。
- 前端删除 4 处按 UTC 字符串截取的私有日期格式化：`settlement-project-page`（死代码）、`settlement-project-detail-workspace`、`document-manager-page`、`po-invoice-summary-service`。
- `src/components/ui.tsx`：`AuditInfoBar` 内部统一格式化，去掉 `truncate`（客户PO、需求单详情、采购订单详情、报价单、项目结算详情 5 个页面同时生效）。
- 列名统一：创建日期/更新日期/确认日期 → 创建时间/更新时间/确认时间，共 17 个文件；对应的 33 处 `type: "date"` 改为 `type: "datetime"`（这三个字段在库里都是 DATETIME）。
- 列表单元格补 `whitespace-nowrap`；通用实体列表的备注列改为可折行。
- `src/lib/crud.ts`：采购订单的"国家"改为按明细回查来源需求单（主表没有该字段），修复 `GET /api/entities/purchase-orders` 500。
- 删除 4 个孤立路由：`/master-data/customers`、`/master-data/suppliers`、`/master-data/undertaking-units`（与 `/customers`、`/suppliers`、`/undertaking-units` 重复）、`/tariff-rates`，以及对应的权限前缀和导航分组判断。
- `开发者文档.md`、`docs/superpowers/specs/2026-09-17-*.md`。

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**。
本次只改接口返回格式与前端展示，**不改表结构、不改字段、不需要加索引**。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。构建后必须重启进程；已打开页面的用户建议 `Ctrl + Shift + R` 强制刷新。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 日期时间格式 | 统一 `YYYY-MM-DD HH:mm`（到分）；需要秒的排查场景保留 `datetime-seconds` 类型 |
| 修复时间偏差 | 原来项目结算、文档管理、集采发票汇总、详情页审计栏按 UTC 截取字符串，时间差 8 小时、日期可能差一天；现按本地时间换算 |
| 详情接口 | 需求单/采购订单详情、项目结算、客户PO、服务映射、供应商付款、导入中心、文档管理全部返回已格式化时间，前端不再做字符串截取 |
| 列表详情一致 | 同一条记录在列表与详情显示同一时间（实测 `/api/orders` 与 `/api/order-details` 一致） |
| 列名 | 创建日期/更新日期/确认日期 全部改为 创建时间/更新时间/确认时间（含导出列） |
| 单元格 | 数据单元格不再折行；备注等超长文本列允许折行 |
| 接口修复 | `GET /api/entities/purchase-orders` 由 500 恢复 200（国家按明细回查来源需求单） |
| 路由清理 | 删除 4 个未挂导航的重复页面，路由数 61 → 57；如有人收藏这些 URL 会 404 |
| 数字格式 | 结差单明细"数量"列恢复千分位；6 个页面的金额格式化统一委托公共实现 |

## 5.14 上一轮发布范围（2026-09-16 修复非管理员登录 502）

本次发布为**纯代码改动，不涉及任何数据库结构变更**，上线时不要执行任何数据库命令。

本次发布的改动文件（共 6 个源码 + 2 个文档）：

- `src/lib/permission-definitions.ts`：新增 `encodePermissionPayload` / `decodePermissionPayload`，权限按掩码分组编码（相同掩码只写一次掩码），并保留对旧 JSON 格式的解析。
- `src/lib/permission-cookie.ts`、`src/lib/permission-middleware.ts`：改为调用上面这组共用编解码，去掉两处重复实现，避免 Node 与 Middleware 两侧格式漂移。
- `src/lib/module-feature-definitions.ts`：模块开关 Cookie 只保存与默认值不同的开关，默认值读取时补回。
- `src/lib/permission-definitions.test.ts`、`src/lib/module-feature-definitions.test.ts`：新增 Cookie 体积上限断言和新旧格式往返用例。
- `部署文档.md`、`开发者文档.md`。

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**。
本次只改 Cookie 编解码，**不改表结构、不改字段、不需要加索引**，`common_user_permissions` 表数据不做任何处理。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。构建后必须重启进程。

**本次问题还有一半在反向代理上**：应用侧把登录响应头压到 2 KB 以内只是消除诱因，反向代理的响应头缓冲仍需按第 7.2 节放宽，否则以后往 Cookie 里增加字段会再次复现同样的 502。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 问题现象 | 非管理员账号登录报"登录服务返回异常（HTTP 502）"，管理员账号正常，业务页面全部正常 |
| 根因 | 登录成功时下发 4 个 Cookie，权限 Cookie 1511 字节、模块开关 Cookie 2060 字节，使登录响应头达到 **4234 字节**，超过 Nginx 默认 `proxy_buffer_size 4k`，Nginx 记录 `upstream sent too big header while reading response header from upstream` 并返回 502。管理员权限 Cookie 很小，响应头只有 2921 字节，所以同一份代码只有非管理员复现 |
| 权限 Cookie 编码 | 由"逐条 `[token, mask]` 的 JSON + base64"改为"按掩码分组 + base64"：非管理员 **1511 → 574 字节**，管理员 210 → 186 字节 |
| 模块开关 Cookie | 只写入与默认值不同的开关：**2060 → 477 字节**（本机 46 个开关全部为默认值） |
| 登录响应头 | 非管理员 **4234 → 1714 字节**，管理员 **2921 → 1314 字节**，均已留出约 2.4 KB 余量 |
| 兼容性 | 旧格式 Cookie 仍能解析，已用旧格式 Cookie 实测通过，不需要强制用户重新登录；下次登录自动换成新格式 |
| 权限校验 | Node 侧与 `middleware.ts` 共用同一套编解码，已端到端验证：有权限 200、无权限 403 |
| 反向代理 | 第 7.2 节新增 `proxy_buffer_size`、`proxy_buffers`、`proxy_busy_buffers_size`、`large_client_header_buffers` 配置与原因说明；第 13 节新增对应排错条目 |

## 5.15 历史发布范围（2026-09-14 优化系统数据导出功能）

本次发布为**代码与查询语句改动，不涉及任何数据库结构变更**，上线时不要执行任何数据库命令。

本次发布的改动文件（共 10 个）：

- `src/components/**`（2 个）：月账单每月明细、预付款每月核销明细两个页面，导出按钮改为下载服务端生成的文件。
- `src/lib/**`（5 个）：`billing-service.ts` / `prepayment-service.ts`（导出变慢的关联写法与统计开销修复）、`csv-export.ts` 与 `writeoff-export-columns.ts`（新增：CSV 生成与列定义）、`display-format.ts`（数字格式化复用 `Intl.NumberFormat` 实例）。
- `src/app/api/**`（2 个，新增）：`billing/monthly-writeoffs/export/`、`prepayments/monthly-writeoffs/export/`。
- `部署文档.md`。

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`**。
`billing-service.ts` / `prepayment-service.ts` 只调整查询里的关联与聚合写法（联合查询不再放大行数、导出时跳过全表统计），**不改表结构、不改字段、不需要加索引**。

因此本次上线步骤与常规代码发布完全相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。**唯一容易漏的一步是构建后必须重启**（详见第 6 节），
并且要让已打开页面的用户按 `Ctrl + Shift + R` 强制刷新，否则浏览器会继续使用旧样式。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 导出变慢修复 | 月账单每月明细、预付款每月核销明细的导出恢复速度：关联由"逐行子查询"改为"分组派生表 + 主键关联"，并去掉导出时不需要的全表 COUNT/SUM。月账单明细导出服务端耗时 **4.07 秒 → 1.3~1.5 秒**，列表第一页 **0.32 秒 → 0.13 秒** |
| 导出改为服务端生成文件 | 两个页面的"导出"改为服务端直接生成 CSV 并作为文件下载：浏览器收到的响应 **15.98 MB JSON → 4.48 MB CSV**，不再在浏览器里解析大 JSON、拼接 CSV；导出列与页面表格共用同一份定义（`writeoff-export-columns.ts`），不会再出现"界面有列、导出没有" |
| 数字格式化 | `formatDisplayValue` 改为复用 `Intl.NumberFormat` 实例（原来每次调用都新建格式化配置，导出几万行 × 几十列会产生几十万次创建） |
| 数据一致性 | 导出与列表使用同一套查询与格式化：月账单明细 16,620 行 = 16,620 个不重复 id；导出文件 16,621 行（表头 + 明细），表头与页面列顺序一致，带 UTF-8 BOM，Excel 直接打开不乱码 |

## 6. 安装、测试与编译

首次部署或 `package-lock.json` 发生变化时执行：

```bash
npm ci
npm test
npm run build
```

成功标准：

- `npm ci` 正常完成。
- 自动化测试全部通过。
- `npm run build` 完成 TypeScript、页面和 API 路由构建。
- 项目根目录生成 `.next/`。

**重要：`npm run build` 会整体替换 `.next/`，必须在构建完成后重启应用进程。** `next start` 在运行期间持有启动时的构建清单，如果在服务运行中重建 `.next/`，浏览器会拿到已经不存在的旧 chunk，表现为页面能打开但列表空白、按钮点击无反应。正确的顺序是「先构建、再重启」，不要省略重启。

构建阶段的 `NODE_ENV` 也要注意：如果服务器环境变量里设了 `NODE_ENV=production`，`npm ci` 会跳过 devDependencies，而 `next build` 需要 `typescript`、`tailwindcss` 等构建期依赖，会直接失败。安装和构建前先清掉该变量（Linux：`unset NODE_ENV`；Windows：`$env:NODE_ENV=""`），构建完成后再用生产模式启动。

不建议生产部署使用 `npm install`，因为它可能更新锁文件中的依赖版本。

## 7. 启动应用

默认使用 `3000` 端口：

```bash
npm run start
```

指定其他端口，例如 `3001`：

```bash
npm run start -- -p 3001
```

本地开发热更新通常使用 `5174` 端口：

```bash
npm run dev:hot -- -p 5174
```

如果出现 `EADDRINUSE`，表示端口已被其他进程占用。应检查现有进程或改用未占用端口，不要直接结束未知业务进程。

### 7.1 PM2 守护运行

```bash
npm install -g pm2
pm2 start npm --name cloud_power -- run start
pm2 save
pm2 startup
```

更新版本后，生产环境按以下顺序执行：

```bash
git pull --ff-only
npm ci
npm test
npm run build
pm2 restart cloud_power --update-env
```

如果服务器使用的 PM2 进程名称不是 `cloud_power`，以实际名称为准。确认 `.env.local` 已配置远程 `cloud` 数据库后再执行重启。

### 7.2 Nginx 反向代理示例

```nginx
server {
    listen 80;
    server_name cloud-power.example.com;

    client_max_body_size 100m;
    proxy_buffer_size 16k;
    proxy_buffers 8 16k;
    proxy_busy_buffers_size 32k;
    large_client_header_buffers 4 16k;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

登录成功时应用会一次性下发 4 个 Cookie，反向代理必须留出足够的响应头缓冲。Nginx 默认 `proxy_buffer_size 4k`，当响应头超过该值时 Nginx 会记录 `upstream sent too big header while reading response header from upstream` 并对浏览器返回 **502**。该现象只在登录响应头上出现，业务页面正常，因此容易被误判为账号或权限问题。按上面的配置把 `proxy_buffer_size`、`proxy_buffers`、`proxy_busy_buffers_size` 和 `large_client_header_buffers` 放宽即可；同时应用侧只写入必要的 Cookie 数据，保证登录响应头稳定在 2 KB 以内。

### 7.3 定时同步远端数据（可选）

远端 Frappe 数据既可以由业务人员在页面上点「同步」手动触发，也可以注册计划任务定时抓取。两个同步任务相互独立：

| 任务 | 执行命令 | 注册脚本（Windows） | 默认频率 |
| --- | --- | --- | --- |
| 需求单同步 | `npm run sync:frappe-demands` | `scripts/register-frappe-demand-sync-task.ps1` | 每小时 |
| 物料同步 | `npm run sync:materials` | `scripts/register-material-sync-task.ps1` | 每小时 |

注册（需要**本机登录当前用户**，脚本使用交互式登录类型）：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\register-frappe-demand-sync-task.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\register-material-sync-task.ps1
```

要点：

- 脚本默认把 `ProjectPath` 当作当前项目目录，会自动 `Set-Location` 到该目录后再执行 `npm run`，**所以读的还是该目录下的 `.env.local`**；换目录部署时用 `-ProjectPath <部署目录>` 指定。
- 任务名默认 `Suanli Frappe Demand Sync` / `Suanli Material Sync`，可用 `-TaskName` 修改；重复注册会覆盖同名任务。
- 计划任务的**运行账号必须能访问远端 `192.168.3.153:1337`**，否则任务会以"读取远端失败"告终。
- 手动验证配置是否可用：

```bash
npm run sync:frappe-demands -- --dry-run
```

`--dry-run` 只计算不写库，用于正式同步前核对影响面。

Linux 环境没有对应的注册脚本，可自行用 cron 调用同样的命令，例如每小时一次：

```cron
0 * * * * cd /path/to/cloud_power && /usr/bin/npm run sync:frappe-demands >> /var/log/suanli-demand-sync.log 2>&1
```

## 8. 文件存储与备份

生产服务器必须保证以下目录可写：

```text
uploads/
  documents/
  service-fee-invoices/
```

要求：

- 将 `uploads/` 纳入定期备份。
- 容器部署时将 `uploads/` 挂载到持久化卷。
- 多实例部署时应改用共享文件存储，否则每个实例只能看到本机上传的文件。
- 更新代码时不要删除 `uploads/`。

数据库备份与文件备份需要同时保留，单独恢复数据库不能恢复已上传文件。

## 9. 部署后验证

1. 打开 `http://<服务器IP>:<端口>/login`。
2. 使用已配置账号登录，确认登录接口返回正常。
3. 打开首页、基础信息、需求单、采购订单、物流和财务模块。
4. 检查国家、客户、供应商和承接单位等基础资料能否读取。
5. 检查分页、筛选、标签页和左侧目录拖动是否正常。
6. 打开月账单台账、预付款合同、需求单列表等带批量选择的列表，确认勾选框显示正常、列宽紧凑。
7. 检查预付款合同详情页已确认状态下有“退回草稿”按钮，草稿状态下有“删除草稿”按钮。
8. **界面样式抽查**（本次界面优化重点）：
   - 列表行悬浮时整行（含最右侧固定列、锁定列）统一变淡蓝，中间不应出现白色空白段。
   - 打开任一列表的"新增/编辑"弹窗，确认弹窗为圆角卡片、标题区与按钮区分隔线一致、按 Esc 可以关闭。
   - 触发删除或退回操作，确认弹出的是系统内确认框（带标题和按钮文案），而不是浏览器原生对话框。
   - 保存成功后右上角出现轻提示，几秒后自动消失。
   - 表格首次加载显示灰条骨架屏，无数据时显示"图标 + 文案"空状态。
   - 分页区"跳至"输入框内填写的数字可以正常看到。
9. 进入“数据工具 - 功能模块管理”，确认模块状态能读取。
10. 在文档库上传并下载测试文件，确认 `uploads/` 权限正常。
11. 检查服务日志中没有 `Unknown column`、`Too many connections` 或持续 500 错误。

首页和列表页出现空白时，先在浏览器按 `Ctrl + Shift + R` 强制刷新一次，排除浏览器缓存的旧前端资源。

需要只读验证数据库结构时，推荐显式加载环境文件：

```bash
node --env-file=.env.local scripts/verify-deploy-schema.mjs
```

该脚本只读检查关键 `merge_power_` 表，不写入业务数据。当前远程数据库已经完成 `InternalId` 迁移，完整验证还应确认 90 张 `merge_*` 表均存在、`internalId` 均为主键且为自增、原业务键唯一约束仍存在，并确认业务数据行数没有异常变化。

## 10. 模块启停

成本与锚定价格、结差、内部服务费等附加模块默认可以停用。管理员可在“数据工具 - 功能模块管理”中启用或停用：

- 停用后，左侧目录和首页入口隐藏。
- 直接访问对应页面或 API 时会被拦截。
- 停用不会删除代码、数据表或历史业务数据。
- 修改状态后其他已打开页面会同步刷新模块状态。

生产上线前应确认计划开放的模块状态，避免把尚未投入使用的功能展示给业务用户。

## 11. 数据库变更限制

正常部署禁止执行：

```bash
npm run seed
npm run migrate
npm run schema:apply
npm run schema:sync-remote
npm run schema:sync-merge -- --apply
npm run data:backfill-remote-baseline -- --apply
npm run data:backfill-remote-derived-data -- --apply
```

以上命令不会作为普通发布的一部分执行。`npm run schema:init-merge` 和 `npm run schema:internal-id:apply` 同样属于受控的初始化/结构变更命令，不能在生产发布脚本中自动执行。远程 `cloud` 已完成本轮结构更新，除非有新的经审核增量 SQL，否则不要重复执行。

相关命令可能创建、修改表结构或写入初始化数据。只有在以下条件全部满足时才能执行数据库结构变更：

1. 已完成数据库结构和数据备份。
2. 已在测试数据库验证迁移脚本。
3. 已审查变更 SQL，不包含业务数据清空或错误表名。
4. 已安排维护窗口和回退方案。
5. 已由项目负责人明确批准。

## 12. 回退方案

部署前记录当前提交：

```bash
git rev-parse HEAD
```

如果新版本编译或运行异常，可在干净的部署目录切换到已验证提交后重新安装、构建并重启：

```bash
git switch --detach <已验证提交号>
npm ci
npm run build
pm2 restart cloud_power --update-env
```

回退代码不会自动回退数据库。若版本包含数据库结构变化，必须使用部署前备份和单独审核过的数据库回退方案。

## 13. 常见部署问题

| 问题 | 处理方法 |
| --- | --- |
| `ENOENT package.json` | 当前目录不是项目根目录，先检查 `Get-ChildItem` 或 `ls` |
| `EADDRINUSE` | 端口被占用，检查监听进程或使用 `-p 3001` |
| `Unknown column` | 应用代码与数据库结构不一致，停止写操作并由开发人员比较结构 |
| `Too many connections` | 检查重复应用进程，降低 `DB_CONNECTION_LIMIT`，确认连接池正常释放 |
| 登录页能打开但无法登录 | 检查 `common_users`、数据库连接和会话环境变量 |
| 登录报 `登录服务返回异常（HTTP 502）`，且只有非管理员账号复现 | 反向代理响应头缓冲不足。查看 Nginx 错误日志是否出现 `upstream sent too big header`，按第 7.2 节放宽 `proxy_buffer_size`、`proxy_buffers`、`proxy_busy_buffers_size`、`large_client_header_buffers` 后重试，再确认应用已升级到收敛 Cookie 体积的版本 |
| 上传失败 | 检查 `uploads/` 权限、磁盘空间和反向代理上传大小 |
| 页面仍是旧版本 | 确认拉取分支、提交号，重新执行 `npm ci`、`npm run build` 并重启进程 |
| 页面能打开但列表空白、按钮点击无反应 | 服务运行期间执行过 `npm run build` 导致前端 chunk 失效。重启应用进程，并让浏览器 `Ctrl + Shift + R` 强制刷新 |
| `sh: next: command not found` 之类启动失败 | 先确认 `npm ci` 与 `npm run build` 都已在本目录完成，再启动 |
| 列表数据为空但接口返回 200 且有数据 | 属于浏览器缓存旧资源，强制刷新即可；仍为空时退出重新登录，确认会话未过期 |
| 样式和别的页面不一样（如悬浮颜色、圆角、按钮颜色） | 浏览器仍在使用旧样式文件。`Ctrl + Shift + R` 强制刷新；确认已重启应用进程 |
| 弹窗仍显示成浏览器原生的"确定/取消" | 同上，属于前端资源未更新；确认构建产物已重启加载 |
