# cloud_power 部署文档

本文档用于指导部署人员将 `cloud_power` 系统部署到新的服务器和新的 MySQL 数据库。当前要求是：**新数据库只创建表结构，不同步现有业务数据**。

## 1. 部署范围

- 部署前后端一体的 Next.js 应用。
- 创建新的 MySQL 数据库 `cloud_power`。
- 同步当前项目的表结构、字段和索引。
- 不导入本地数据库已有业务数据。
- 不执行测试数据种子脚本。
- 文档上传文件保存在部署服务器本地 `uploads/` 目录。

## 2. 环境要求

| 软件 | 建议版本 | 说明 |
| --- | --- | --- |
| Node.js | 20 LTS 或更高 | 运行 Next.js 应用 |
| npm | 随 Node.js 安装 | 安装依赖、执行脚本 |
| MySQL | 8.0 或兼容版本 | 业务数据库 |
| Git | 最新稳定版 | 拉取代码 |

## 3. 获取代码

```bash
git clone <代码仓库地址> cloud_power
cd cloud_power
npm install
```

## 4. 数据库连接信息

目标数据库服务器：

```text
DB_HOST=139.9.122.9
DB_PORT=3306
DB_USER=cloud_wenjian
DB_NAME=cloud_power
```

数据库密码请由项目负责人通过安全渠道单独提供，不建议写入代码仓库或部署文档。

## 5. 创建数据库与授权

如果执行建库时报错类似下面内容，说明当前账号没有创建 `cloud_power` 数据库的权限：

```text
ERROR 1044 (42000): Access denied for user 'cloud_wenjian'@'%' to database 'cloud_power'
```

需要 DBA 或具备管理员权限的账号先执行：

```sql
CREATE DATABASE IF NOT EXISTS `cloud_power`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

GRANT ALL PRIVILEGES ON `cloud_power`.* TO 'cloud_wenjian'@'%';
FLUSH PRIVILEGES;
```

如果数据库用户尚未创建，可由管理员执行：

```sql
CREATE USER IF NOT EXISTS 'cloud_wenjian'@'%' IDENTIFIED BY '<数据库密码>';
GRANT ALL PRIVILEGES ON `cloud_power`.* TO 'cloud_wenjian'@'%';
FLUSH PRIVILEGES;
```

## 6. 表名大小写要求

当前代码内部仍使用逻辑表名，例如 `requests`、`purchaseorders`、`monthlybillingwriteoffs`；数据库层会统一映射到带前缀的物理表名，例如 `power_requests`、`power_purchaseorders`、`power_monthlybillingwriteoffs`。

远程 MySQL 如果是 Linux 常见配置，`lower_case_table_names=0` 时表名大小写敏感。此时如果直接导入 `schema.sql` 中的 `Requests`、`PurchaseOrders` 等大写表名，应用运行时会出现“表存在但页面读不到数据”的问题。

因此部署时请使用项目提供的脚本生成部署专用 SQL。该 SQL 会：

- 将数据库名从 `suanli` 改为 `cloud_power`。
- 将表名统一转换为小写，并增加 `power_` 前缀，匹配应用数据库访问规则。
- 只包含建库、建表、字段和索引，不包含业务数据。

生成命令：

```bash
npm run schema:deploy
```

生成文件：

```text
cloud_power_schema.sql
```

该生成文件已加入 `.gitignore`，不会提交到代码仓库。

## 7. 只同步表结构，不同步数据

项目数据库结构同步分两步：

1. 导入 `cloud_power_schema.sql`，创建基础空表。
2. 执行 `npm run migrate`，补齐增量字段、索引和新增表。

### 7.1 生成空表 SQL

```bash
npm run schema:deploy
```

### 7.2 导入空表结构

Linux/macOS：

```bash
mysql -h 139.9.122.9 -P 3306 -u cloud_wenjian -p cloud_power < cloud_power_schema.sql
```

Windows PowerShell：

```powershell
mysql -h 139.9.122.9 -P 3306 -u cloud_wenjian -p cloud_power < .\cloud_power_schema.sql
```

### 7.3 执行结构迁移

Linux/macOS：

```bash
export DB_HOST=139.9.122.9
export DB_PORT=3306
export DB_USER=cloud_wenjian
export DB_PASSWORD='<数据库密码>'
export DB_NAME=cloud_power
export DB_CONNECTION_LIMIT=5

npm run migrate
```

Windows PowerShell：

```powershell
$env:DB_HOST='139.9.122.9'
$env:DB_PORT='3306'
$env:DB_USER='cloud_wenjian'
$env:DB_PASSWORD='<数据库密码>'
$env:DB_NAME='cloud_power'
$env:DB_CONNECTION_LIMIT='5'

npm run migrate
```

注意：不要执行下面命令，否则会写入测试或初始化数据：

```bash
npm run seed
```

`npm run migrate` 会创建登录用户表 `power_appusers`，并在账号不存在时写入初始管理员账号：

```text
账号：admin@luzcorp.com
初始密码：由项目负责人确认
```

密码会以加盐哈希形式保存到数据库，不会在 `power_appusers` 表中保存明文密码。

## 8. 数据库验证

建库和迁移完成后，检查表是否存在：

```sql
USE `cloud_power`;
SHOW TABLES;
```

确认没有同步旧数据，可抽查关键业务表行数。新库应为 0：

```sql
SELECT 'power_requests' AS tableName, COUNT(*) AS rowCount FROM power_requests
UNION ALL SELECT 'power_requestitems', COUNT(*) FROM power_requestitems
UNION ALL SELECT 'power_purchaseorders', COUNT(*) FROM power_purchaseorders
UNION ALL SELECT 'power_purchaseorderitems', COUNT(*) FROM power_purchaseorderitems
UNION ALL SELECT 'power_shipments', COUNT(*) FROM power_shipments
UNION ALL SELECT 'power_billinginstanceledgers', COUNT(*) FROM power_billinginstanceledgers
UNION ALL SELECT 'power_monthlybillingwriteoffs', COUNT(*) FROM power_monthlybillingwriteoffs
UNION ALL SELECT 'power_prepaymentcontracts', COUNT(*) FROM power_prepaymentcontracts
UNION ALL SELECT 'power_prepaymentcontractitems', COUNT(*) FROM power_prepaymentcontractitems
UNION ALL SELECT 'power_servicefeesnapshots', COUNT(*) FROM power_servicefeesnapshots
UNION ALL SELECT 'power_documentfolders', COUNT(*) FROM power_documentfolders
UNION ALL SELECT 'power_documentfiles', COUNT(*) FROM power_documentfiles
UNION ALL SELECT 'power_appusers', COUNT(*) FROM power_appusers;
```

如果基础信息也要求空库状态，则 `power_countries`、`power_instancemodels`、`power_suppliers` 等基础表也应为 0。`power_appusers` 表通常会有 1 条初始管理员账号。后续可通过系统页面或数据导入中心导入正式资料。

## 9. 应用环境变量

在服务器项目根目录创建 `.env.local` 或配置生产环境变量：

```env
DB_HOST=139.9.122.9
DB_PORT=3306
DB_USER=cloud_wenjian
DB_PASSWORD=<数据库密码>
DB_NAME=cloud_power
DB_CONNECTION_LIMIT=5
```

不要将包含真实密码的 `.env.local` 提交到代码仓库。

## 10. 构建和启动

构建生产版本：

```bash
npm run build
```

启动服务：

```bash
npm run start
```

默认访问地址：

```text
http://服务器IP:3000/
```

如果需要后台常驻运行，建议使用 `pm2`、`systemd`、Docker 或服务器平台自带的进程管理能力。

pm2 示例：

```bash
npm install -g pm2
pm2 start npm --name cloud_power -- run start
pm2 save
```

## 11. 上传文件目录

文档管理模块上传的合同、发票、图片、Word、PDF 等文件默认保存在项目根目录下的 `uploads/` 文件夹。

生产部署时建议：

- 确认应用进程对 `uploads/` 目录有读写权限。
- 将 `uploads/` 纳入服务器备份策略。
- 如果使用容器部署，需要将 `uploads/` 挂载为持久化卷，避免容器重建导致文件丢失。

## 12. 登录账号

系统当前内置初始登录信息：

```text
账号：admin@luzcorp.com
初始密码：由项目负责人确认
```

上线后建议尽快增加账号管理或修改默认密码策略。当前初始账号由 `npm run migrate` 写入 `power_appusers` 表，密码以加盐哈希形式保存。

## 13. 常见问题

### 13.1 数据库无法连接

检查：

- 服务器安全组是否放行 3306。
- MySQL 是否允许远程连接。
- `cloud_wenjian` 是否允许从应用服务器 IP 登录。
- `.env.local` 中 `DB_HOST`、`DB_PORT`、`DB_USER`、`DB_PASSWORD`、`DB_NAME` 是否正确。

### 13.2 建库没有权限

使用 DBA 账号先执行本文档第 5 节的建库和授权 SQL。

### 13.3 表已存在但字段缺失

确认已执行：

```bash
npm run migrate
```

并确认执行迁移时环境变量 `DB_NAME=cloud_power`。

### 13.4 页面提示表不存在

先检查 MySQL 表名大小写：

```sql
SHOW VARIABLES LIKE 'lower_case_table_names';
SHOW TABLES;
```

如果 `lower_case_table_names=0`，表名必须使用小写且带 `power_` 前缀。请使用 `npm run schema:deploy` 生成的 `cloud_power_schema.sql` 重新初始化空库。

### 13.5 不小心执行了 seed

`npm run seed` 会写入测试或初始化数据。若新库要求完全不带数据，需由 DBA 清空数据库后重新执行第 7 节表结构同步流程。

## 14. 上线检查清单

- 已创建 `cloud_power` 数据库。
- 已授权 `cloud_wenjian` 用户访问 `cloud_power`。
- 已执行 `npm run schema:deploy`。
- 已导入 `cloud_power_schema.sql` 空表结构。
- 已执行 `npm run migrate`。
- 未执行 `npm run seed`。
- 已配置生产环境变量。
- 已执行 `npm run build`。
- 已启动 `npm run start` 或进程管理服务。
- 已确认登录页面可访问。
- 已确认关键表为空。
- 已确认 `uploads/` 目录可写并已纳入备份。
