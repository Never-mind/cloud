# cloud_power 合并系统部署文档

> 适用范围：`main` 分支当前版本。本文档用于在 Windows 或 Linux 服务器部署算力系统、集采系统、华为云业务和公共基础功能组成的 Next.js 前后端一体应用。文档更新日期：2026-09-03。

## 1. 部署架构

- 应用：Next.js 16、React、TypeScript、Node.js。
- 数据库：MySQL。生产环境连接远程 `139.9.122.9:3306` 的 `cloud` 数据库；本地开发验证默认使用 `merge` 数据库。
- 数据表：生产数据库中的物理表按业务域使用 `merge_power_`、`merge_po_`、`merge_cloud_`、`merge_common_` 四类前缀。
- 生产数据库结构状态：远程 `cloud` 数据库已完成结构同步、`InternalId` 迁移和基线数据补全。库内共 153 张表，其中 `merge_*` 业务表 90 张，`internalId` 均为 `INT UNSIGNED AUTO_INCREMENT` 主键；原 UUID、业务编号和原有关联字段均已保留。结构与开发库比对为 0 差异，详见第 5 节。
- 文件存储：合同、发票等上传文件保存在应用服务器的 `uploads/` 目录。
- 运行方式：`next start`，建议使用 PM2 或服务器进程管理器守护。

生产部署只更新代码和应用进程，不自动迁移现有业务数据。远程数据库已经完成当前版本的表结构更新，日常发布不要执行建库、建表、迁移、导入或清空数据命令。

表前缀约定：

- `merge_power_`：算力系统需求、采购、物流、合同、月账单、预付款、服务费和结差。
- `merge_po_`：集采客户 PO、报价单、项目结算、产品主档、产品类别和发票汇总。
- `merge_cloud_`：华为云对账、账号映射、供应商付款和附件。
- `merge_common_`：供应商、客户、承接单位、联系人、银行账户、用户、权限、文档和操作日志等公共数据。

供应商、客户和承接单位只维护在 `merge_common_` 公共表中；三个业务域的业务单据表仍然相互隔离。

## 2. 环境要求

| 软件 | 建议版本 | 说明 |
| --- | --- | --- |
| Node.js | 20.9+、22 LTS 或 24 LTS | Node.js 18 不再建议使用 |
| npm | 随 Node.js 安装 | 使用 `npm ci` 按锁文件安装 |
| Git | 最新稳定版 | 拉取与更新代码 |
| MySQL | 8.0 或兼容版本 | 数据库已部署在独立服务器 |
| PM2 | 最新稳定版，可选 | 生产进程守护与重启 |

部署服务器需要能够访问代码仓库和远程 MySQL 服务 `139.9.122.9:3306`，远程数据库还需要在安全组、防火墙和 MySQL 账号权限中允许部署服务器的来源 IP，并开放应用对外服务端口，例如 `3000`。

## 3. 获取代码

### 3.1 华为 CodeHub 代码仓（生产部署）

生产部署使用华为 CodeHub 的 `cloud_purchase` 仓库：

```bash
git clone --branch main --single-branch https://codehub.devcloud.cn-north-4.huaweicloud.com/2fc6809e8fe540f68c39743bc923d349/cloud_purchase.git merge
cd merge
```

代码仓账号和密码应通过安全方式提供，不要写入命令历史、脚本或文档。

### 3.2 GitHub 代码仓（备用）

```bash
git clone --branch main --single-branch https://github.com/Never-mind/cloud.git merge
cd merge
```

也可以使用 SSH：

```bash
git clone --branch main --single-branch git@github.com:Never-mind/cloud.git merge
cd merge
```

如服务器上已有部署目录，先确认工作区没有人工修改，再更新。生产服务器必须确认远程地址指向 `cloud_purchase.git`：

```bash
git status
git remote -v
git switch main
git pull --ff-only
```

不要在存在未提交修改的部署目录直接执行覆盖操作。

## 4. 配置环境变量

在项目根目录创建 `.env.local`：

```env
DB_HOST=139.9.122.9
DB_PORT=3306
DB_USER=<远程数据库账号>
DB_PASSWORD=<通过安全渠道获取的数据库密码>
DB_NAME=cloud
DB_CONNECTION_LIMIT=5
AUTH_SESSION_TOKEN=<随机且不可公开的长字符串>
ADMIN_EMAIL=admin@luzcorp.com
ADMIN_INITIAL_PASSWORD=<初始化后立即修改>
```

说明：

- `.env.local` 已被 Git 忽略，禁止提交真实密码。
- 生产环境必须配置 `DB_HOST=139.9.122.9`、`DB_PORT=3306`、`DB_NAME=cloud`；不要把生产连接配置写入代码。
- `DB_NAME=merge` 仅是本地开发默认值。切换数据库只修改部署服务器的 `.env.local`，不要修改业务代码。
- 生产环境不要使用 `root` 或弱密码账号，建议创建仅能访问 `cloud` 数据库的专用 MySQL 账号，并限制来源 IP。
- `DB_CONNECTION_LIMIT` 建议从 `5` 开始，避免多个应用进程造成数据库连接数过高。
- `AUTH_SESSION_TOKEN` 用于登录会话，生产环境必须设置为随机值，不要使用代码中的开发默认值。
- `ADMIN_EMAIL` 和 `ADMIN_INITIAL_PASSWORD` 只在初始化公共用户时使用；初始化后应在系统“账户管理”中修改管理员密码。
- 应用通过 `src/lib/db.ts` 将逻辑表名映射为 `merge_power_` 物理表名；集采、华为云和公共服务使用已定义的 `merge_po_`、`merge_cloud_`、`merge_common_` 物理表名。

Linux 建议限制环境文件权限：

```bash
chmod 600 .env.local
```

## 5. 生产数据库结构状态与变更规范

生产数据库 `cloud` 已完成结构同步与基线数据补全，**本次代码发布不需要再执行任何数据库操作**。当前状态如下：

- 远程数据库：`cloud`（MySQL 8.0，`139.9.122.9:3306`）
- 表数量：153 张，其中 `merge_*` 业务表 90 张（`merge_power_` 53 张、`merge_common_` 17 张、`merge_po_` 14 张、`merge_cloud_` 6 张）
- `merge_*` 表数据总行数：20,034 行
- 所有表均为 InnoDB
- 所有表均已增加 `internalId INT UNSIGNED AUTO_INCREMENT`，并设置为主键
- 原 UUID、业务单号和原业务关联字段均保留，并保留唯一约束
- 结构与开发库逐列比对为 **0 差异**（列、类型、列顺序均已对齐）
- `receivableDate` 已补齐到华为云、供应商付款、服务费对账单和项目结算发票相关表
- 项目结算发票的承接单位、供应商/客户字段已补齐

复核结构是否仍然一致（只读，不会修改任何数据）：

```bash
TARGET_DB_HOST=139.9.122.9 TARGET_DB_PORT=3306 \
TARGET_DB_USER=cloud_wenjian TARGET_DB_PASSWORD='******' TARGET_DB_NAME=cloud \
npm run schema:sync-merge
```

该命令默认是 dry-run，输出 `新建表 0 张，新增列 0 个，类型调整 0 个，列顺序调整 0 个` 即表示结构一致。只有追加 `--apply` 才会执行变更。

## 5.1 首次初始化说明（仅适用于全新空数据库）

只有部署全新的空数据库时，才由数据库管理员使用仓库根目录的 `merge-schema-only.sql`。该文件包含当前版本的表结构、字段、索引、约束及中文备注，不包含业务数据、`INSERT` 和 `DROP TABLE`。

首次初始化全新数据库时：

1. 连接远程 MySQL `139.9.122.9:3306`，选中目标数据库 `cloud`。
2. 先确认数据库备份、字符集和账号建表权限。
3. 打开 `merge-schema-only.sql` 并执行。
4. 执行后确认 `merge_power_`、`merge_po_`、`merge_cloud_`、`merge_common_` 前缀的表均已生成。
5. 再按本文档第 4 节配置应用服务器的 `.env.local`。

如果目标数据库已经存在业务数据或同名表，不要直接导入该文件，不要删除或覆盖现有表。应先备份数据和附件，再导出结构并生成经过审核的增量 SQL。

## 5.2 当前远程数据库日常发布规则

远程 `cloud` 已完成 `InternalId` 迁移、结构对齐、国家默认值与 B6 规则等基线数据补全，以及派生的应付金额与算力价快照回填（明细见 5.3）。**日常代码发布只需要更新应用，不需要再次同步数据库结构。**

日常发布禁止执行：

```bash
npm run schema:init-merge
npm run schema:internal-id:apply
npm run migrate
npm run seed
npm run schema:apply
npm run schema:sync-remote
npm run schema:sync-merge -- --apply
npm run data:backfill-remote-baseline -- --apply
npm run data:backfill-remote-derived-data -- --apply
```

上述命令可能重复建表、修改结构、迁移主键或写入初始化数据。后续确有数据库变更时，必须单独制作增量 SQL，先在测试数据库验证，经负责人批准并完成备份后再执行。

本地开发库初始化仍可使用项目脚本：

```bash
npm run schema:init-merge
```

初始化注意事项：

- `merge-schema-only.sql` 只用于全新空数据库的首次或受控结构初始化，不属于普通发布步骤。
- `scripts/initialize-merge-schema.mjs` 面向本地开发库，不要在生产 `cloud` 数据库上直接执行。
- 对已有数据库执行任何结构 SQL 前必须完成数据库和 `uploads/` 备份，并先在测试库验证。
- 不要把 `npm run schema:init-merge`、`npm run migrate` 或 `npm run seed` 放入每次启动脚本。
- 结构 SQL 不会导入业务单据，也不会替代 Excel 业务导入流程；但执行前仍需确认目标数据库和表名。
- 只有确认目标数据库、代码版本、备份和维护窗口后，才允许执行初始化或结构变更。

## 5.3 本轮已完成的数据库变更记录

以下变更**已经在远程 `cloud` 执行完毕**，属于历史记录，发布时不需要重复执行。保留此清单是为了避免后续维护人员重复操作。

### 5.3.1 结构变更

| 对象 | 变更内容 |
| --- | --- |
| `merge_po_settlement_projects` | 新增 `acceptanceCompletedAt`（验收完成时间），位于 `acceptanceStartedAt` 之后 |
| `merge_cloud_supplier_payments` | 新增 `supplierPayableCurrency`、`supplierPayableNetAmount`、`supplierTaxRate`、`supplierTaxAmount`、`supplierPayableTotalAmount` |
| `merge_power_demand_sync_runs` / `_items` / `_mappings`、`merge_power_material_sync_runs` | 新建 4 张同步台账表 |
| 早期批次（23 列 + 5 处类型放宽） | 需求单远端状态、物流远端字段、实例型号类型、算力价测算字段等已补齐 |
| 列顺序 | 103 处按开发库顺序对齐（新增列曾落在表末尾，Navicat 中不易查找） |

### 5.3.2 基线数据

| 表 | 变更内容 |
| --- | --- |
| `merge_power_countries` | BR / CL / MX 三个国家的默认承接单位与默认客户，按 `undertakingUnitCode` 与名称映射到远程自身 UUID（两侧 UUID 不同，不能直接复制） |
| `merge_power_b6typeconfigs` | 补入 B61、B62-A7、B62-A8 (HT)、B63 (HT) 四条 B6 规则 |

### 5.3.3 派生数据

| 列 | 处理方式 | 影响行数 |
| --- | --- | --- |
| `merge_power_purchaseorderitems.currency` | 按采购单币种回填，兜底 USD | 279 |
| `merge_power_purchaseorderitems.powerPricingJson` / `powerFirst24VatIncluded` / `powerNext36VatIncluded` | 用现成的算力服务费测算函数重算 | 279 |
| `merge_power_billinginstanceledgers.customerId` | 按 `requestitems` 反查 | 274 |
| `merge_power_billinginstanceledgers.requestType` | 按请求单反查，兜底「整机」 | 2 |
| `merge_power_monthlybillingwriteoffs.customerId` | 按台账与请求明细反查 | 16,440 |
| `merge_power_monthlybillingwriteoffs.requestType` | 同上 | 120 |
| `merge_power_servicefeesnapshotitems.requestType` | 按月度账单/预付款来源反查 | 8 |
| `merge_cloud_supplier_payments` 应付字段 | 按 `merge_cloud_rows` 实时聚合口径落库 | 1 |

### 5.3.4 仍未回填、且不应手工回填的列

| 列 | 原因 |
| --- | --- |
| `merge_power_monthlybillingwriteoffs.selfCalculatedUnitPrice` / `differenceUnitPrice` / `differenceTotalPrice` | 由月账单生成流程计算，需在远程重新生成月账单，不能硬写 |
| `merge_power_monthlybillingwriteoffs.adjustmentNo` | 结差来源标记，流程产物 |
| `merge_power_requests.remoteStatus` / `remoteStatusUpdatedAt` | 需求单同步再次执行时自动写入 |
| `merge_power_shipments` 的 `dcCode`、`crd`、`apdAt` 等节点时间 | 物流同步或采购确认时写入 |
| 各类 `createdByUserId` 等审计字段 | 业务操作时写入，历史行无值属正常 |

### 5.3.5 巡检脚本

以下四个脚本默认都是 dry-run，只打印计划，只有显式追加 `--apply` 才写库。执行前必须完成备份并取得审批。

| 命令 | 用途 |
| --- | --- |
| `npm run schema:sync-merge` | 全量比对 90 张 `merge_*` 表的结构（早期脚本只比对 50 张纳管表，曾因此漏掉项目结算字段） |
| `npm run data:backfill-remote-baseline` | 国家默认值、B6 规则等基线数据，幂等 |
| `npm run data:backfill-remote-derived-data` | 币种、算力价快照、customerId、requestType 等派生数据，幂等；`--validate` 可用本地数据自检测算管线 |
| `npm run data:check-remote-gaps` | 只读巡检本地有值、远程为空的列，输出缺口清单 |

## 5.4 最近一次发布范围（2026-09-17 统一空状态、色值与分页）

本次发布为**纯前端样式改动，不涉及数据库，也没有接口行为变化**，上线时不要执行任何数据库命令。

本次发布的改动（33 个文件）：

- **空状态统一**：15 处手写的纯文本空状态（`<td>暂无数据</td>`，无图标、`py-10`/`py-12` 不一致）全部改为公共组件 `EmptyState`，`py-10` 归一为 `py-12`；业务文案保留（"暂无映射""暂无供应商付款"等）。至此全系统空状态 55 处全部走 `TableStateContent` / `EmptyState`。
- **硬编码色值归到设计令牌**：`src/app/globals.css` 新增 6 个确实缺失的令牌（取值与原来完全一致，无视觉变化）：`--color-skeleton`、`--color-nav-ink`、`--color-nav-ink-soft`、`--color-info-ink`、`--color-danger-border-strong`、`--color-warning-border-soft`；其余 66 处硬编码色值归到已有语义令牌。**结果：全系统硬编码色值 0 处。**
- **分页统一**：`customer-po-page` 的自绘分页条替换为 `PaginationBar`，至此 16 个列表页分页完全一致。
- **查询按钮配色**：同步台账/映射、华为云对账的"查询"按钮补 `tone="secondary"`，与其它页面一致。
- `docs/superpowers/specs/2026-09-17-ui-consistency-audit.md`（排查报告与修复记录）。

其中色值合并是本次最实质的变化，修掉了几组"同色系不同深浅"的历史遗留：

| 原硬编码 | 处数 | 归到 |
| --- | --- | --- |
| `#f4f9ff` / `#f5fbff` / `#e6f4ff` / `#f4faff` / `#f0f5ff` | 10 | `bg-info-soft`（原来有 5 种不同的浅蓝底） |
| `#d9ecff` / `#c6e2ff` / `#b8d8f8` | 8 | `border-info-border` |
| `#fff7e6` / `#fff8e6` / `#fffdf5` | 5 | `bg-warning-soft` |
| `#f0fff7` / `#f0fff4` | 2 | `bg-success-soft` |
| `#13a85a` / `#13a561` / `#13a65b` | 3 | `text-success-dark` |
| `#e5e7eb` / `#e4e7ed` / `#d9e2ec` | 4 | `border-line` |
| 其余 | 34 | 对应语义令牌或本次新增令牌 |

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**，也没有改动任何 API 路由。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。**纯样式改动，构建后必须重启并让用户 `Ctrl + Shift + R` 强制刷新**，否则浏览器会继续用旧样式。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 空状态 | 所有列表的空状态统一为"图标 + 文案"；手写纯文本空状态 15 处 → 0 处 |
| 颜色 | 全系统不再有 `bg-[#...]` / `text-[#...]` / `border-[#...]` 硬编码；几组近似色合并为同一语义令牌 |
| 分页 | 客户 PO 列表的分页样式与其它列表一致（共 X 条 / 当前 X-X 条 / 条每页 / 上一页下一页 / 跳转） |
| 查询按钮 | 同步台账、同步映射、华为云对账的"查询"按钮由默认样式改为次级样式 |

## 5.5 上一轮发布范围（2026-09-17 统一下拉框、表头与表格容器样式）

本次发布为**纯前端样式与组件改动，不涉及数据库，也没有接口行为变化**，上线时不要执行任何数据库命令。

本次发布的改动（43 个源码文件 + `DESIGN.md`）：

- `src/components/ui.tsx`：新增 `Select` 公共组件，与 `Input` 使用同一套高度、圆角、边框和聚焦样式。
- 34 个页面把 **84 个裸 `<select>`** 全部替换为 `<Select>`（其中十几个原来完全没有 className，用的是浏览器默认样式），基础样式类收敛到组件内部，页面只传 `w-full` / `min-w-*` 等布局类。
- 全部 `<th>` 规范化：补齐 `whitespace-nowrap`、`font-medium` 和缺失的对齐类，`py-2` 归到 `py-3`（共修正 68 处）。
- 表格容器统一为 `table-scroll overflow-auto`：清除 3 处写死的 `max-h-[calc(100vh-XXXpx)]`；华为云对账 3 张表补上 `table-scroll`（保留 `h-full` 填满视口的行为）；导入中心 2 张表补上 `table-scroll`。
- `DESIGN.md`：修正 6.2 节的时间格式（原写 `YYYY-MM-DD HH:mm:ss`，与现行"到分"规范冲突）；新增 6.4 节「组件与类名约定」。
- `docs/superpowers/specs/2026-09-17-ui-consistency-audit.md`（本轮排查报告）。

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**，也没有改动任何 API 路由的返回结构。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。**本次是纯样式改动，构建后必须重启并让用户 `Ctrl + Shift + R` 强制刷新**，否则浏览器会继续用旧的样式 chunk。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 下拉框 | 全系统 87 个下拉框统一为 `Select` 组件：高度统一 `h-9`，聚焦时统一描边+渐变环，不再出现浏览器默认样式；原来固定宽度（`w-24` / `w-28` / `w-[104px]` 等）的保留宽度 |
| 表头 | 所有表头统一加粗、不折行、行高一致；原来不加粗的 37 处、会折行的 24 处、行高偏矮的 10 处全部修正 |
| 表格容器 | 滚动条样式统一（细滚动条、圆角滑块）；同步台账/映射、项目结算、集采发票汇总三页不再有写死的视口高度，列表随内容增长、整页滚动 |
| 华为云对账 | 列表仍然填满视口（保留 `h-full`），滚动条样式与其它模块统一 |

## 5.6 上一轮发布范围（2026-09-17 统一日期时间与列表显示规范）

本次发布为**纯代码改动，不涉及任何数据库结构变更**，上线时不要执行任何数据库命令。

本次发布的改动（47 个文件，其中 4 个页面文件删除）：

- `src/lib/display-format.ts`：新增 `normalizeTemporalFields`（接口返回前统一格式化 DATE / DATETIME），导出 `formatMoneyValue` / `formatNumberValue`；日期时间统一 `YYYY-MM-DD HH:mm`，日期统一 `YYYY-MM-DD`。
- 详情接口统一收口（7 个）：`order-detail-service`、`settlement-project-service`、`api/po/customer-pos`、`listCloudMappings`、`normalizeCloudDateFields`、`listImportJobs`、`getDocumentItems`。
- 前端删除 4 处按 UTC 字符串截取的私有日期格式化：`settlement-project-page`（死代码）、`settlement-project-detail-workspace`、`document-manager-page`、`po-invoice-summary-service`。
- `src/components/ui.tsx`：`AuditInfoBar` 内部统一格式化，去掉 `truncate`（客户PO、需求单详情、采购订单详情、报价单、项目结算详情 5 个页面同时生效）。
- 列名统一：创建日期/更新日期/确认日期 → 创建时间/更新时间/确认时间，共 17 个文件；对应的 33 处 `type: "date"` 改为 `type: "datetime"`（这三个字段在库里都是 DATETIME）。
- 列表单元格补 `whitespace-nowrap`；通用实体列表的备注列改为可折行。
- `src/lib/crud.ts`：采购订单的"国家"改为按明细回查来源需求单（主表没有该字段），修复 `GET /api/entities/purchase-orders` 500。
- 删除 4 个孤立路由：`/master-data/customers`、`/master-data/suppliers`、`/master-data/undertaking-units`（与 `/customers`、`/suppliers`、`/undertaking-units` 重复）、`/tariff-rates`，以及对应的权限前缀和导航分组判断。
- `开发者文档.md`、`docs/superpowers/specs/2026-09-17-*.md`。

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**。
本次只改接口返回格式与前端展示，**不改表结构、不改字段、不需要加索引**。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。构建后必须重启进程；已打开页面的用户建议 `Ctrl + Shift + R` 强制刷新。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 日期时间格式 | 统一 `YYYY-MM-DD HH:mm`（到分）；需要秒的排查场景保留 `datetime-seconds` 类型 |
| 修复时间偏差 | 原来项目结算、文档管理、集采发票汇总、详情页审计栏按 UTC 截取字符串，时间差 8 小时、日期可能差一天；现按本地时间换算 |
| 详情接口 | 需求单/采购订单详情、项目结算、客户PO、服务映射、供应商付款、导入中心、文档管理全部返回已格式化时间，前端不再做字符串截取 |
| 列表详情一致 | 同一条记录在列表与详情显示同一时间（实测 `/api/orders` 与 `/api/order-details` 一致） |
| 列名 | 创建日期/更新日期/确认日期 全部改为 创建时间/更新时间/确认时间（含导出列） |
| 单元格 | 数据单元格不再折行；备注等超长文本列允许折行 |
| 接口修复 | `GET /api/entities/purchase-orders` 由 500 恢复 200（国家按明细回查来源需求单） |
| 路由清理 | 删除 4 个未挂导航的重复页面，路由数 61 → 57；如有人收藏这些 URL 会 404 |
| 数字格式 | 结差单明细"数量"列恢复千分位；6 个页面的金额格式化统一委托公共实现 |

## 5.7 上一轮发布范围（2026-09-16 修复非管理员登录 502）

本次发布为**纯代码改动，不涉及任何数据库结构变更**，上线时不要执行任何数据库命令。

本次发布的改动文件（共 6 个源码 + 2 个文档）：

- `src/lib/permission-definitions.ts`：新增 `encodePermissionPayload` / `decodePermissionPayload`，权限按掩码分组编码（相同掩码只写一次掩码），并保留对旧 JSON 格式的解析。
- `src/lib/permission-cookie.ts`、`src/lib/permission-middleware.ts`：改为调用上面这组共用编解码，去掉两处重复实现，避免 Node 与 Middleware 两侧格式漂移。
- `src/lib/module-feature-definitions.ts`：模块开关 Cookie 只保存与默认值不同的开关，默认值读取时补回。
- `src/lib/permission-definitions.test.ts`、`src/lib/module-feature-definitions.test.ts`：新增 Cookie 体积上限断言和新旧格式往返用例。
- `部署文档.md`、`开发者文档.md`。

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`、没有 `package-lock.json`**。
本次只改 Cookie 编解码，**不改表结构、不改字段、不需要加索引**，`common_user_permissions` 表数据不做任何处理。

上线步骤与常规代码发布相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。构建后必须重启进程。

**本次问题还有一半在反向代理上**：应用侧把登录响应头压到 2 KB 以内只是消除诱因，反向代理的响应头缓冲仍需按第 7.2 节放宽，否则以后往 Cookie 里增加字段会再次复现同样的 502。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 问题现象 | 非管理员账号登录报"登录服务返回异常（HTTP 502）"，管理员账号正常，业务页面全部正常 |
| 根因 | 登录成功时下发 4 个 Cookie，权限 Cookie 1511 字节、模块开关 Cookie 2060 字节，使登录响应头达到 **4234 字节**，超过 Nginx 默认 `proxy_buffer_size 4k`，Nginx 记录 `upstream sent too big header while reading response header from upstream` 并返回 502。管理员权限 Cookie 很小，响应头只有 2921 字节，所以同一份代码只有非管理员复现 |
| 权限 Cookie 编码 | 由"逐条 `[token, mask]` 的 JSON + base64"改为"按掩码分组 + base64"：非管理员 **1511 → 574 字节**，管理员 210 → 186 字节 |
| 模块开关 Cookie | 只写入与默认值不同的开关：**2060 → 477 字节**（本机 46 个开关全部为默认值） |
| 登录响应头 | 非管理员 **4234 → 1714 字节**，管理员 **2921 → 1314 字节**，均已留出约 2.4 KB 余量 |
| 兼容性 | 旧格式 Cookie 仍能解析，已用旧格式 Cookie 实测通过，不需要强制用户重新登录；下次登录自动换成新格式 |
| 权限校验 | Node 侧与 `middleware.ts` 共用同一套编解码，已端到端验证：有权限 200、无权限 403 |
| 反向代理 | 第 7.2 节新增 `proxy_buffer_size`、`proxy_buffers`、`proxy_busy_buffers_size`、`large_client_header_buffers` 配置与原因说明；第 13 节新增对应排错条目 |

## 5.8 历史发布范围（2026-09-14 优化系统数据导出功能）

本次发布为**代码与查询语句改动，不涉及任何数据库结构变更**，上线时不要执行任何数据库命令。

本次发布的改动文件（共 10 个）：

- `src/components/**`（2 个）：月账单每月明细、预付款每月核销明细两个页面，导出按钮改为下载服务端生成的文件。
- `src/lib/**`（5 个）：`billing-service.ts` / `prepayment-service.ts`（导出变慢的关联写法与统计开销修复）、`csv-export.ts` 与 `writeoff-export-columns.ts`（新增：CSV 生成与列定义）、`display-format.ts`（数字格式化复用 `Intl.NumberFormat` 实例）。
- `src/app/api/**`（2 个，新增）：`billing/monthly-writeoffs/export/`、`prepayments/monthly-writeoffs/export/`。
- `部署文档.md`。

已逐条核对：**没有 `*.sql`、没有 `scripts/` 下的任何文件、没有 `src/lib/db.ts`**。
`billing-service.ts` / `prepayment-service.ts` 只调整查询里的关联与聚合写法（联合查询不再放大行数、导出时跳过全表统计），**不改表结构、不改字段、不需要加索引**。

因此本次上线步骤与常规代码发布完全相同（第 7.1 节）：

```bash
git pull --ff-only
npm run build
pm2 restart cloud_power --update-env
```

`package-lock.json` 未变化，`npm ci` 可以跳过。**唯一容易漏的一步是构建后必须重启**（详见第 6 节），
并且要让已打开页面的用户按 `Ctrl + Shift + R` 强制刷新，否则浏览器会继续使用旧样式。

本次变化清单（供测试核对）：

| 变化 | 说明 |
| --- | --- |
| 导出变慢修复 | 月账单每月明细、预付款每月核销明细的导出恢复速度：关联由"逐行子查询"改为"分组派生表 + 主键关联"，并去掉导出时不需要的全表 COUNT/SUM。月账单明细导出服务端耗时 **4.07 秒 → 1.3~1.5 秒**，列表第一页 **0.32 秒 → 0.13 秒** |
| 导出改为服务端生成文件 | 两个页面的"导出"改为服务端直接生成 CSV 并作为文件下载：浏览器收到的响应 **15.98 MB JSON → 4.48 MB CSV**，不再在浏览器里解析大 JSON、拼接 CSV；导出列与页面表格共用同一份定义（`writeoff-export-columns.ts`），不会再出现"界面有列、导出没有" |
| 数字格式化 | `formatDisplayValue` 改为复用 `Intl.NumberFormat` 实例（原来每次调用都新建格式化配置，导出几万行 × 几十列会产生几十万次创建） |
| 数据一致性 | 导出与列表使用同一套查询与格式化：月账单明细 16,620 行 = 16,620 个不重复 id；导出文件 16,621 行（表头 + 明细），表头与页面列顺序一致，带 UTF-8 BOM，Excel 直接打开不乱码 |

## 6. 安装、测试与编译

首次部署或 `package-lock.json` 发生变化时执行：

```bash
npm ci
npm test
npm run build
```

成功标准：

- `npm ci` 正常完成。
- 自动化测试全部通过。
- `npm run build` 完成 TypeScript、页面和 API 路由构建。
- 项目根目录生成 `.next/`。

**重要：`npm run build` 会整体替换 `.next/`，必须在构建完成后重启应用进程。** `next start` 在运行期间持有启动时的构建清单，如果在服务运行中重建 `.next/`，浏览器会拿到已经不存在的旧 chunk，表现为页面能打开但列表空白、按钮点击无反应。正确的顺序是「先构建、再重启」，不要省略重启。

构建阶段的 `NODE_ENV` 也要注意：如果服务器环境变量里设了 `NODE_ENV=production`，`npm ci` 会跳过 devDependencies，而 `next build` 需要 `typescript`、`tailwindcss` 等构建期依赖，会直接失败。安装和构建前先清掉该变量（Linux：`unset NODE_ENV`；Windows：`$env:NODE_ENV=""`），构建完成后再用生产模式启动。

不建议生产部署使用 `npm install`，因为它可能更新锁文件中的依赖版本。

## 7. 启动应用

默认使用 `3000` 端口：

```bash
npm run start
```

指定其他端口，例如 `3001`：

```bash
npm run start -- -p 3001
```

本地开发热更新通常使用 `5174` 端口：

```bash
npm run dev:hot -- -p 5174
```

如果出现 `EADDRINUSE`，表示端口已被其他进程占用。应检查现有进程或改用未占用端口，不要直接结束未知业务进程。

### 7.1 PM2 守护运行

```bash
npm install -g pm2
pm2 start npm --name cloud_power -- run start
pm2 save
pm2 startup
```

更新版本后，生产环境按以下顺序执行：

```bash
git pull --ff-only
npm ci
npm test
npm run build
pm2 restart cloud_power --update-env
```

如果服务器使用的 PM2 进程名称不是 `cloud_power`，以实际名称为准。确认 `.env.local` 已配置远程 `cloud` 数据库后再执行重启。

### 7.2 Nginx 反向代理示例

```nginx
server {
    listen 80;
    server_name cloud-power.example.com;

    client_max_body_size 100m;
    proxy_buffer_size 16k;
    proxy_buffers 8 16k;
    proxy_busy_buffers_size 32k;
    large_client_header_buffers 4 16k;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

登录成功时应用会一次性下发 4 个 Cookie，反向代理必须留出足够的响应头缓冲。Nginx 默认 `proxy_buffer_size 4k`，当响应头超过该值时 Nginx 会记录 `upstream sent too big header while reading response header from upstream` 并对浏览器返回 **502**。该现象只在登录响应头上出现，业务页面正常，因此容易被误判为账号或权限问题。按上面的配置把 `proxy_buffer_size`、`proxy_buffers`、`proxy_busy_buffers_size` 和 `large_client_header_buffers` 放宽即可；同时应用侧只写入必要的 Cookie 数据，保证登录响应头稳定在 2 KB 以内。

## 8. 文件存储与备份

生产服务器必须保证以下目录可写：

```text
uploads/
  documents/
  service-fee-invoices/
```

要求：

- 将 `uploads/` 纳入定期备份。
- 容器部署时将 `uploads/` 挂载到持久化卷。
- 多实例部署时应改用共享文件存储，否则每个实例只能看到本机上传的文件。
- 更新代码时不要删除 `uploads/`。

数据库备份与文件备份需要同时保留，单独恢复数据库不能恢复已上传文件。

## 9. 部署后验证

1. 打开 `http://<服务器IP>:<端口>/login`。
2. 使用已配置账号登录，确认登录接口返回正常。
3. 打开首页、基础信息、需求单、采购订单、物流和财务模块。
4. 检查国家、客户、供应商和承接单位等基础资料能否读取。
5. 检查分页、筛选、标签页和左侧目录拖动是否正常。
6. 打开月账单台账、预付款合同、需求单列表等带批量选择的列表，确认勾选框显示正常、列宽紧凑。
7. 检查预付款合同详情页已确认状态下有“退回草稿”按钮，草稿状态下有“删除草稿”按钮。
8. **界面样式抽查**（本次界面优化重点）：
   - 列表行悬浮时整行（含最右侧固定列、锁定列）统一变淡蓝，中间不应出现白色空白段。
   - 打开任一列表的"新增/编辑"弹窗，确认弹窗为圆角卡片、标题区与按钮区分隔线一致、按 Esc 可以关闭。
   - 触发删除或退回操作，确认弹出的是系统内确认框（带标题和按钮文案），而不是浏览器原生对话框。
   - 保存成功后右上角出现轻提示，几秒后自动消失。
   - 表格首次加载显示灰条骨架屏，无数据时显示"图标 + 文案"空状态。
   - 分页区"跳至"输入框内填写的数字可以正常看到。
9. 进入“数据工具 - 功能模块管理”，确认模块状态能读取。
10. 在文档库上传并下载测试文件，确认 `uploads/` 权限正常。
11. 检查服务日志中没有 `Unknown column`、`Too many connections` 或持续 500 错误。

首页和列表页出现空白时，先在浏览器按 `Ctrl + Shift + R` 强制刷新一次，排除浏览器缓存的旧前端资源。

需要只读验证数据库结构时，推荐显式加载环境文件：

```bash
node --env-file=.env.local scripts/verify-deploy-schema.mjs
```

该脚本只读检查关键 `merge_power_` 表，不写入业务数据。当前远程数据库已经完成 `InternalId` 迁移，完整验证还应确认 90 张 `merge_*` 表均存在、`internalId` 均为主键且为自增、原业务键唯一约束仍存在，并确认业务数据行数没有异常变化。

## 10. 模块启停

成本与锚定价格、结差、内部服务费等附加模块默认可以停用。管理员可在“数据工具 - 功能模块管理”中启用或停用：

- 停用后，左侧目录和首页入口隐藏。
- 直接访问对应页面或 API 时会被拦截。
- 停用不会删除代码、数据表或历史业务数据。
- 修改状态后其他已打开页面会同步刷新模块状态。

生产上线前应确认计划开放的模块状态，避免把尚未投入使用的功能展示给业务用户。

## 11. 数据库变更限制

正常部署禁止执行：

```bash
npm run seed
npm run migrate
npm run schema:apply
npm run schema:sync-remote
npm run schema:sync-merge -- --apply
npm run data:backfill-remote-baseline -- --apply
npm run data:backfill-remote-derived-data -- --apply
```

以上命令不会作为普通发布的一部分执行。`npm run schema:init-merge` 和 `npm run schema:internal-id:apply` 同样属于受控的初始化/结构变更命令，不能在生产发布脚本中自动执行。远程 `cloud` 已完成本轮结构更新，除非有新的经审核增量 SQL，否则不要重复执行。

相关命令可能创建、修改表结构或写入初始化数据。只有在以下条件全部满足时才能执行数据库结构变更：

1. 已完成数据库结构和数据备份。
2. 已在测试数据库验证迁移脚本。
3. 已审查变更 SQL，不包含业务数据清空或错误表名。
4. 已安排维护窗口和回退方案。
5. 已由项目负责人明确批准。

## 12. 回退方案

部署前记录当前提交：

```bash
git rev-parse HEAD
```

如果新版本编译或运行异常，可在干净的部署目录切换到已验证提交后重新安装、构建并重启：

```bash
git switch --detach <已验证提交号>
npm ci
npm run build
pm2 restart cloud_power --update-env
```

回退代码不会自动回退数据库。若版本包含数据库结构变化，必须使用部署前备份和单独审核过的数据库回退方案。

## 13. 常见部署问题

| 问题 | 处理方法 |
| --- | --- |
| `ENOENT package.json` | 当前目录不是项目根目录，先检查 `Get-ChildItem` 或 `ls` |
| `EADDRINUSE` | 端口被占用，检查监听进程或使用 `-p 3001` |
| `Unknown column` | 应用代码与数据库结构不一致，停止写操作并由开发人员比较结构 |
| `Too many connections` | 检查重复应用进程，降低 `DB_CONNECTION_LIMIT`，确认连接池正常释放 |
| 登录页能打开但无法登录 | 检查 `common_users`、数据库连接和会话环境变量 |
| 登录报 `登录服务返回异常（HTTP 502）`，且只有非管理员账号复现 | 反向代理响应头缓冲不足。查看 Nginx 错误日志是否出现 `upstream sent too big header`，按第 7.2 节放宽 `proxy_buffer_size`、`proxy_buffers`、`proxy_busy_buffers_size`、`large_client_header_buffers` 后重试，再确认应用已升级到收敛 Cookie 体积的版本 |
| 上传失败 | 检查 `uploads/` 权限、磁盘空间和反向代理上传大小 |
| 页面仍是旧版本 | 确认拉取分支、提交号，重新执行 `npm ci`、`npm run build` 并重启进程 |
| 页面能打开但列表空白、按钮点击无反应 | 服务运行期间执行过 `npm run build` 导致前端 chunk 失效。重启应用进程，并让浏览器 `Ctrl + Shift + R` 强制刷新 |
| `sh: next: command not found` 之类启动失败 | 先确认 `npm ci` 与 `npm run build` 都已在本目录完成，再启动 |
| 列表数据为空但接口返回 200 且有数据 | 属于浏览器缓存旧资源，强制刷新即可；仍为空时退出重新登录，确认会话未过期 |
| 样式和别的页面不一样（如悬浮颜色、圆角、按钮颜色） | 浏览器仍在使用旧样式文件。`Ctrl + Shift + R` 强制刷新；确认已重启应用进程 |
| 弹窗仍显示成浏览器原生的"确定/取消" | 同上，属于前端资源未更新；确认构建产物已重启加载 |
