# cloud_power 合并系统部署文档

> 适用范围：`main` 分支当前版本。本文档用于在 Windows 或 Linux 服务器部署算力系统、集采系统、华为云业务和公共基础功能组成的 Next.js 前后端一体应用。文档更新日期：2026-08-31。

## 1. 部署架构

- 应用：Next.js 16、React、TypeScript、Node.js。
- 数据库：MySQL，合并系统默认数据库名为 `merge`。
- 数据表：按业务域使用 `power_`、`po_`、`cloud_`、`common_` 四类前缀。
- 文件存储：合同、发票等上传文件保存在应用服务器的 `uploads/` 目录。
- 运行方式：`next start`，建议使用 PM2 或服务器进程管理器守护。

生产部署只更新代码和应用进程，不自动迁移现有业务数据。日常发布不要执行建库、建表、迁移、导入或清空数据命令。

表前缀约定：

- `power_`：算力系统需求、采购、物流、合同、月账单、预付款、服务费和结差。
- `po_`：集采客户 PO、报价单、项目结算、产品主档、产品类别和发票汇总。
- `cloud_`：华为云对账、账号映射、供应商付款和附件。
- `common_`：供应商、客户、承接单位、联系人、银行账户、用户、权限、文档和操作日志等公共数据。

供应商、客户和承接单位只维护在 `common_` 公共表中；三个业务域的业务单据表仍然相互隔离。

## 2. 环境要求

| 软件 | 建议版本 | 说明 |
| --- | --- | --- |
| Node.js | 20.9+、22 LTS 或 24 LTS | Node.js 18 不再建议使用 |
| npm | 随 Node.js 安装 | 使用 `npm ci` 按锁文件安装 |
| Git | 最新稳定版 | 拉取与更新代码 |
| MySQL | 8.0 或兼容版本 | 数据库已部署在独立服务器 |
| PM2 | 最新稳定版，可选 | 生产进程守护与重启 |

部署服务器需要能够访问 GitHub 代码仓库和目标 MySQL 服务，并开放应用对外服务端口，例如 `3000`。

## 3. 获取代码

### 3.1 GitHub 代码仓

```bash
git clone --branch main --single-branch https://github.com/Never-mind/cloud.git merge
cd merge
```

也可以使用 SSH：

```bash
git clone --branch main --single-branch git@github.com:Never-mind/cloud.git merge
cd merge
```

如服务器上已有部署目录，先确认工作区没有人工修改，再更新：

```bash
git status
git switch main
git pull --ff-only
```

不要在存在未提交修改的部署目录直接执行覆盖操作。

## 4. 配置环境变量

在项目根目录创建 `.env.local`：

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=<通过安全渠道获取的数据库密码>
DB_NAME=merge
DB_CONNECTION_LIMIT=5
AUTH_SESSION_TOKEN=<随机且不可公开的长字符串>
ADMIN_EMAIL=admin@luzcorp.com
ADMIN_INITIAL_PASSWORD=<初始化后立即修改>
```

说明：

- `.env.local` 已被 Git 忽略，禁止提交真实密码。
- `DB_NAME=merge` 是合并系统默认值；生产环境如使用其他数据库名，必须确认该库已完整初始化且与代码版本匹配。
- `root/root` 只适合本地开发验证，不应作为生产数据库账号密码。
- `DB_CONNECTION_LIMIT` 建议从 `5` 开始，避免多个应用进程造成数据库连接数过高。
- `AUTH_SESSION_TOKEN` 用于登录会话，生产环境必须设置为随机值，不要使用代码中的开发默认值。
- `ADMIN_EMAIL` 和 `ADMIN_INITIAL_PASSWORD` 只在初始化公共用户时使用；初始化后应在系统“账户管理”中修改管理员密码。
- 应用通过 `src/lib/db.ts` 将算力逻辑表映射为 `power_` 物理表名；集采、华为云和公共服务使用已定义的带前缀表名。

Linux 建议限制环境文件权限：

```bash
chmod 600 .env.local
```

## 5. 合并数据库初始化

初始化脚本会读取 `schema.sql` 创建算力表，再创建公共、集采和华为云表，补齐合并系统历史字段，写入初始管理员和基础模块记录，并添加中文数据库注释。脚本要求目标数据库已经存在，默认目标为 `merge`。

首次初始化空的 `merge` 数据库时：

```bash
# 先由数据库管理员创建数据库并确认账号有建表权限
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS merge CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 已将 DB_* 环境变量加载到当前 shell 后执行
npm run schema:init-merge
```

如果使用 Node.js 20+ 且不希望把变量写入当前 shell，可以显式加载环境文件：

```bash
node --env-file=.env.local scripts/initialize-merge-schema.mjs
```

初始化注意事项：

- 该脚本包含 `CREATE TABLE`、`ALTER TABLE`、字段整理和初始账号写入，不属于普通发布步骤。
- 对已有数据库执行前必须完成数据库和 `uploads/` 备份，并先在测试库验证。
- 不要把 `npm run schema:init-merge`、`npm run migrate` 或 `npm run seed` 放入每次启动脚本。
- 脚本不会导入业务单据，也不会替代 Excel 业务导入流程；但仍可能变更结构和初始化公共配置。
- 只有确认目标数据库、代码版本、备份和维护窗口后，才允许执行初始化或结构变更。

## 6. 安装、测试与编译

首次部署或 `package-lock.json` 发生变化时执行：

```bash
npm ci
npm test
npm run build
```

成功标准：

- `npm ci` 正常完成。
- 自动化测试全部通过。
- `npm run build` 完成 TypeScript、页面和 API 路由构建。
- 项目根目录生成 `.next/`。

不建议生产部署使用 `npm install`，因为它可能更新锁文件中的依赖版本。

## 7. 启动应用

默认使用 `3000` 端口：

```bash
npm run start
```

指定其他端口，例如 `3001`：

```bash
npm run start -- -p 3001
```

本地开发热更新通常使用 `5174` 端口：

```bash
npm run dev:hot -- -p 5174
```

如果出现 `EADDRINUSE`，表示端口已被其他进程占用。应检查现有进程或改用未占用端口，不要直接结束未知业务进程。

### 7.1 PM2 守护运行

```bash
npm install -g pm2
pm2 start npm --name cloud_power -- run start
pm2 save
pm2 startup
```

更新版本后：

```bash
git pull --ff-only
npm ci
npm test
npm run build
pm2 restart cloud_power --update-env
```

### 7.2 Nginx 反向代理示例

```nginx
server {
    listen 80;
    server_name cloud-power.example.com;

    client_max_body_size 100m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## 8. 文件存储与备份

生产服务器必须保证以下目录可写：

```text
uploads/
  documents/
  service-fee-invoices/
```

要求：

- 将 `uploads/` 纳入定期备份。
- 容器部署时将 `uploads/` 挂载到持久化卷。
- 多实例部署时应改用共享文件存储，否则每个实例只能看到本机上传的文件。
- 更新代码时不要删除 `uploads/`。

数据库备份与文件备份需要同时保留，单独恢复数据库不能恢复已上传文件。

## 9. 部署后验证

1. 打开 `http://<服务器IP>:<端口>/login`。
2. 使用已配置账号登录，确认登录接口返回正常。
3. 打开首页、基础信息、需求单、采购订单、物流和财务模块。
4. 检查国家、客户、供应商和承接单位等基础资料能否读取。
5. 检查分页、筛选、标签页和左侧目录拖动是否正常。
6. 进入“数据工具 - 功能模块管理”，确认模块状态能读取。
7. 在文档库上传并下载测试文件，确认 `uploads/` 权限正常。
8. 检查服务日志中没有 `Unknown column`、`Too many connections` 或持续 500 错误。

需要只读验证数据库结构时，推荐显式加载环境文件：

```bash
node --env-file=.env.local scripts/verify-deploy-schema.mjs
```

该脚本只读检查关键 `power_` 表，不写入业务数据；如需完整检查合并库，还应由数据库管理员只读核对 `common_`、`po_`、`cloud_` 表是否存在及版本是否一致。

## 10. 模块启停

成本与锚定价格、结差、内部服务费等附加模块默认可以停用。管理员可在“数据工具 - 功能模块管理”中启用或停用：

- 停用后，左侧目录和首页入口隐藏。
- 直接访问对应页面或 API 时会被拦截。
- 停用不会删除代码、数据表或历史业务数据。
- 修改状态后其他已打开页面会同步刷新模块状态。

生产上线前应确认计划开放的模块状态，避免把尚未投入使用的功能展示给业务用户。

## 11. 数据库变更限制

正常部署禁止执行：

```bash
npm run seed
npm run migrate
npm run schema:apply
npm run schema:sync-remote
```

以上命令不会作为普通发布的一部分执行。`npm run schema:init-merge` 同样属于受控的初始化/结构变更命令，不能在生产发布脚本中自动执行。

相关命令可能创建、修改表结构或写入初始化数据。只有在以下条件全部满足时才能执行数据库结构变更：

1. 已完成数据库结构和数据备份。
2. 已在测试数据库验证迁移脚本。
3. 已审查变更 SQL，不包含业务数据清空或错误表名。
4. 已安排维护窗口和回退方案。
5. 已由项目负责人明确批准。

## 12. 回退方案

部署前记录当前提交：

```bash
git rev-parse HEAD
```

如果新版本编译或运行异常，可在干净的部署目录切换到已验证提交后重新安装、构建并重启：

```bash
git switch --detach <已验证提交号>
npm ci
npm run build
pm2 restart cloud_power --update-env
```

回退代码不会自动回退数据库。若版本包含数据库结构变化，必须使用部署前备份和单独审核过的数据库回退方案。

## 13. 常见部署问题

| 问题 | 处理方法 |
| --- | --- |
| `ENOENT package.json` | 当前目录不是项目根目录，先检查 `Get-ChildItem` 或 `ls` |
| `EADDRINUSE` | 端口被占用，检查监听进程或使用 `-p 3001` |
| `Unknown column` | 应用代码与数据库结构不一致，停止写操作并由开发人员比较结构 |
| `Too many connections` | 检查重复应用进程，降低 `DB_CONNECTION_LIMIT`，确认连接池正常释放 |
| 登录页能打开但无法登录 | 检查 `common_users`、数据库连接和会话环境变量 |
| 上传失败 | 检查 `uploads/` 权限、磁盘空间和反向代理上传大小 |
| 页面仍是旧版本 | 确认拉取分支、提交号，重新执行 `npm ci`、`npm run build` 并重启进程 |
