# cloud_power 部署文档

本文档用于将应用部署到服务器，并连接已经完成结构初始化的远程 MySQL 数据库。当前远程数据库为 `cloud`，其 `power_` 前缀表、索引、初始管理员和默认文档目录均已就绪。

## 1. 部署范围

- 部署 Next.js 前后端一体应用。
- 使用远程 MySQL `cloud` 数据库。
- 不执行建库、建表、结构迁移、测试数据写入或数据同步。
- 合同、发票等上传文件保存于应用服务器的 `uploads/` 目录。

## 2. 环境要求

| 软件 | 建议版本 | 用途 |
| --- | --- | --- |
| Node.js | 20 LTS 或更高 | 运行应用 |
| npm | 随 Node.js 安装 | 安装依赖与构建 |
| Git | 最新稳定版 | 拉取代码 |

## 3. 获取代码

```bash
git clone <代码仓库地址> cloud_power
cd cloud_power
npm install
```

## 4. 配置远程数据库

在项目根目录创建 `.env.local`：

```env
DB_HOST=139.9.122.9
DB_PORT=3306
DB_USER=cloud_wenjian
DB_PASSWORD=<通过安全渠道获取的数据库密码>
DB_NAME=cloud
DB_CONNECTION_LIMIT=5
```

代码通过 `DB_NAME` 选择数据库，因此不需要修改业务代码。应用内部使用逻辑表名，例如 `requests`；访问数据库时会自动映射到 `power_requests` 等小写物理表名。

不要提交包含真实密码的 `.env.local`。

## 5. 构建与启动

```bash
npm run build
npm run start
```

默认监听端口为 `3000`。可通过反向代理将域名转发到该端口。

使用 PM2 的示例：

```bash
npm install -g pm2
pm2 start npm --name cloud_power -- run start
pm2 save
```

## 6. 部署后验证

1. 打开 `http://<服务器IP>:3000/login`，确认登录页可访问。
2. 使用初始管理员账号登录：`admin@luzcorp.com`。初始密码由项目负责人通过安全渠道提供。
3. 访问基础信息、需求单、采购订单和财务模块，确认接口可以读取远程数据库。
4. 上传一个测试文件到文档管理模块，确认 `uploads/` 目录具备写入权限。

仅需检查数据库结构时，可执行：

```bash
npm run schema:verify
```

该命令只读检查 `power_` 表数量及关键业务表行数，不会修改数据库。

## 7. 文件存储

生产服务器必须为 `uploads/` 提供读写权限并纳入备份策略。容器部署时，应将该目录挂载到持久化卷；否则容器重建会导致已上传文件丢失。

## 8. 禁止操作

远程 `cloud` 数据库已完成结构配置。正常部署时不要执行以下命令：

```bash
npm run seed
npm run schema:deploy
npm run schema:apply
npm run migrate
```

如后续版本包含数据库结构变更，应先备份远程数据库，再由开发人员评估并单独安排迁移。
