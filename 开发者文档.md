# cloud_power 合并系统开发者文档

> 本文档描述当前 `main` 分支的技术架构、数据库约定、核心业务规则和 API。合并数据库的结构来源是 `scripts/initialize-merge-schema.mjs`；算力基础表的历史结构基线仍见 `schema.sql`，关系图见 `ER图.md`。
>
> 文档更新日期：2026-08-31。当前系统由算力系统、集采系统、华为云业务和公共基础功能组成。

## 1. 系统定位

cloud_power 是合并业务系统，覆盖：

- 客户需求、采购订单、要货计划和物流交付。
- 实例合同、预付款合同、月账单和实际服务费。
- CAPEX/OPEX 锚定价格、实例及非实例结差。
- 内部服务费分摊、区间调整和月度归档。
- 集采客户 PO、报价单、项目结算和发票汇总。
- 华为云对账、客户实收、客户开票和供应商付款。
- 供应商、客户、承接单位、银行账户和联系人等公共基础信息。
- 文档库、统一数据导入、模块启停、用户权限和用户界面偏好。

## 2. 技术架构

| 层次 | 技术与位置 |
| --- | --- |
| Web 框架 | Next.js 16 App Router |
| UI | React、TypeScript、Tailwind CSS、Lucide React |
| API | `src/app/api/**/route.ts` |
| 页面 | `src/app/**/page.tsx` |
| 复用组件 | `src/components/` |
| 业务服务 | `src/lib/*-service.ts` |
| 纯业务算法 | `src/lib/*-workflow.ts` |
| 通用实体配置 | `src/lib/modules.ts` |
| 数据访问 | `src/lib/db.ts`、`mysql2/promise` |
| Excel | ExcelJS、SheetJS (`xlsx`) |
| 测试 | Vitest、Testing Library |

系统是前后端一体应用。浏览器只访问 Next.js 页面和 `/api/*`，数据库连接仅存在于服务端。

## 3. 目录说明

```text
src/app/                 页面与 API 路由；集采、华为云页面也在这里
src/components/          页面级和通用 UI 组件
src/lib/                 业务服务、算法、实体配置和数据库访问
scripts/                 建库、迁移、结构验证与维护脚本
schema.sql               算力 `power_` 表的结构基线
scripts/initialize-merge-schema.mjs  `merge` 数据库完整初始化脚本
uploads/                 用户上传文件，Git 忽略
ER图.md                  数据库关系图
部署文档.md              生产部署说明
系统操作手册.md          业务用户操作说明
```

## 4. 环境变量

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=<数据库密码>
DB_NAME=merge
DB_CONNECTION_LIMIT=5
AUTH_SESSION_TOKEN=<会话随机值>
```

开发和合并系统默认数据库为 `merge`。生产环境数据库名由部署环境决定，不应在代码中写死。`.env.local` 不得提交到 Git。

可选认证变量：

- `ADMIN_EMAIL`：初始化管理员邮箱，默认 `admin@luzcorp.com`。
- `ADMIN_INITIAL_PASSWORD`：初始化管理员密码，默认 `admin@luzcorp.com`；生产环境应在初始化后立即修改。
- `AUTH_SESSION_TOKEN`：登录 Cookie 的服务端校验值，生产环境必须显式设置。

## 5. 数据库访问约定

### 5.1 表名前缀

业务 SQL 使用逻辑表名，例如：

```sql
SELECT * FROM requests WHERE requestNo = :requestNo
```

`src/lib/db.ts` 会根据逻辑表名映射到物理表。算力逻辑表映射为 `power_`，集采表使用 `po_`，华为云表使用 `cloud_`，公共表使用 `common_`。新增算力逻辑表时必须同步加入 `LOGICAL_TABLE_NAMES`；集采、华为云和公共表应直接使用已定义的带前缀表名。业务代码不要重复拼接前缀；明确使用 `queryRowsRaw`、`executeRaw` 时才直接写物理表名。

### 5.2 连接池与事务

- 全局连接池由 `getDb()` 复用。
- 默认连接上限由 `DB_CONNECTION_LIMIT` 控制。
- 查询使用命名参数，避免拼接用户输入。
- 涉及主单、明细和下游数据同时写入时使用 `withTransaction()`。
- `queryRowsInTransaction()` 和 `executeInTransaction()` 会继续执行表名映射。

### 5.3 关系约定

多数关系由业务字段和服务层维护，没有依赖数据库外键级联删除。删除主单前必须显式检查下游占用，并在事务内删除允许删除的明细。

## 6. 数据表目录

当前系统共维护 49 张 `power_` 前缀表。

### 6.1 基础资料

| 物理表 | 主键/关键字段 | 用途 |
| --- | --- | --- |
| `power_countries` | `code`、`vatRate` | 国家及增值税税率 |
| `power_deliverylocations` | `locationId`、`countryCode` | 收货地点与详细地址 |
| `power_deliverycontacts` | `contactId`、`locationId` | 收件人、电话、邮箱 |
| `power_datacenters` | `dcCode`、`locationId` | 机房及物理地址 |
| `power_instancemodels` | `deviceCode`、`b6Type` | 设备编码、机型、名称、默认 B6 类型 |
| `power_suppliers` | `supplierId`、`supplierCode` | 供应商 |
| `power_undertakingunits` | `undertakingUnitId`、`undertakingUnitCode` | 承接单位 |
| `power_customers` | `customerId`、`customerCode` | 客户 |

设备编码是实例型号唯一键；机型、xxll 编码和中英文名称允许重复。

### 6.2 需求、采购与物流

| 物理表 | 主键/关联字段 | 用途 |
| --- | --- | --- |
| `power_requests` | `requestNo` | 需求主单 |
| `power_requestitems` | `id`、`requestNo` | 需求明细，保存供应商、承接单位和客户 |
| `power_purchaseorders` | `purchaseOrderId`、`poNo` | 采购主单，业务关联优先使用 PO 单号 |
| `power_purchaseorderitems` | `id`、`poNo`、`requestItemId` | 采购明细及含税、不含税、CAPEX/OPEX 单价 |
| `power_purchaseordersnitems` | `id`、`purchaseOrderId` | 要货计划 SN 明细 |
| `power_purchaseorderplanitems` | `id`、`purchaseOrderId` | 要货计划子明细 |
| `power_shipments` | `shipmentId`、`purchaseOrderItemId` | 物流交付和地址、联系人快照 |

### 6.3 合同、月账单与预付款

| 物理表 | 主键/关联字段 | 用途 |
| --- | --- | --- |
| `power_instancecontracts` | `id`，唯一组合 `contractNo + countryCode + deviceCode` | 实例合同价格 |
| `power_contractitems` | `id`、`contractNo` | 历史合同明细兼容表 |
| `power_billinginstanceledgers` | `ledgerId`、`purchaseOrderItemId` | 月账单台账 |
| `power_monthlybillingwriteoffs` | `id`、`ledgerId`、`writeOffMonth` | 60 个月月账单明细 |
| `power_billingadjustments` | `adjustmentNo` | 实例合同调整主单 |
| `power_billingadjustmentitems` | `id`、`adjustmentNo` | 实例合同调整明细 |
| `power_billingstatementsnapshots` | `snapshotNo` | 月账单对账单主单 |
| `power_billingstatementsnapshotitems` | `id`、`snapshotNo` | 月账单对账单冻结明细 |
| `power_prepaymentcontracts` | `contractNo` | 预付款合同主单 |
| `power_prepaymentcontractitems` | `id`、`contractNo` | 实例或非实例费用明细 |
| `power_monthlyprepaymentwriteoffs` | `id`、`contractLineId` | 24 个月预付款核销明细 |
| `power_prepaymentwriteoffadjustments` | `adjustmentNo` | 预付款指定月份调整主单 |
| `power_prepaymentwriteoffadjustmentitems` | `id`、`adjustmentNo` | 调整明细 |

### 6.4 服务费与内部服务费

| 物理表 | 主键/关联字段 | 用途 |
| --- | --- | --- |
| `power_servicefeesnapshots` | `snapshotNo` | 服务费对账单、确认、开票和回款信息 |
| `power_servicefeesnapshotitems` | `id`、`snapshotNo` | 服务费对账单冻结明细 |
| `power_internalserviceledgers` | `ledgerId` | 内部服务费实例台账 |
| `power_monthlyinternalservicefees` | `id`、`ledgerId` | 60 个月内部服务费明细 |
| `power_internalservicefeeadjustments` | `adjustmentNo`、`ledgerId` | 人工区间调整 |
| `power_internalservicefeesnapshots` | `snapshotNo` | 月度归档主单 |
| `power_internalservicefeesnapshotitems` | `id`、`snapshotNo` | 已冻结归档明细 |

服务费表的物理名称保留历史 `snapshot` 命名，前端业务名称已经统一为“服务费对账单”。

### 6.5 锚定价格与结差

| 物理表 | 主键/关联字段 | 用途 |
| --- | --- | --- |
| `power_b6typeconfigs` | `b6Type` | B6 类型规则和默认参数 |
| `power_capexpricingversions` | `versionId`、`versionNo` | CAPEX/OPEX 锚定价格版本 |
| `power_capexpricingitems` | `id`、`versionId`、`deviceCode` | 设备锚定价格参数和计算快照 |
| `power_balancesettlements` | `settlementNo` | 实例或非实例结差来源单 |
| `power_balancesettlementitems` | `id`、`settlementNo` | 结差明细及计算结果 |
| `power_balancesettlementfinals` | `finalSettlementNo` | 结差结算单主单 |
| `power_balancesettlementfinalsources` | `id`、`finalSettlementNo` | 结算单引用的来源单 |
| `power_writeoffitems` | `id` | 历史统一核销兼容表 |

### 6.6 文档、导入和系统配置

| 物理表 | 主键/关联字段 | 用途 |
| --- | --- | --- |
| `power_documentfolders` | `folderId`、`parentId` | 多级文件夹 |
| `power_documentfiles` | `fileId`、`folderId` | 文件元数据，文件本体在 `uploads/` |
| `power_importjobs` | `jobId` | 导入历史和错误摘要 |
| `common_users` | `email` | 登录账号、角色、状态和密码哈希 |
| `power_userpreferences` | `id`、`email`、`preferenceKey` | 用户左侧目录排序 |
| `power_modulefeatures` | `moduleKey` | 模块启停状态与修改人 |

完整字段类型、默认值和索引请查阅 `schema.sql`；可视化关系请查阅 `ER图.md`。

### 6.7 合并系统表

合并数据库 `merge` 使用业务域前缀隔离同名表。供应商、客户和承接单位只保留公共表，集采、华为云和算力的业务表彼此独立。

| 前缀 | 主要表 | 业务归属 |
| --- | --- | --- |
| `common_` | `common_customers`、`common_suppliers`、`common_undertaking_units`、各自联系人和银行账户 | 三个系统共用的基础资料、附件、用户、权限、文档和操作日志 |
| `po_` | `po_customer_pos`、`po_customer_po_items`、`po_quotations`、`po_quotation_items`、`po_settlement_projects`、`po_settlement_items`、`po_settlement_expenses`、`po_settlement_sales`、`po_settlement_invoices`、`po_settlement_attachments`、`po_product_masters`、`po_tariff_rates` | 集采系统客户 PO、报价、项目结算、发票汇总、产品主档和产品类别 |
| `cloud_` | `cloud_rows`、`cloud_mappings`、`cloud_mapping_accounts`、`cloud_import_batches`、`cloud_supplier_payments`、`cloud_attachments` | 华为云对账、账号映射、供应商付款和附件 |
| `power_` | 本文 6.1 至 6.6 所列算力业务表 | 算力系统需求、采购、物流、合同、财务和结差 |

产品主档当前实际使用 `po_product_masters`。其中 `tariffRate`、`hsCodeMx` 和 `needNom` 按产品类别规则自动带出，报价单明细可保存产品编码匹配结果。`po_product_models` 和 `po_product_specifications` 为历史/扩展结构，新增集采业务不要把它们当作客户 PO 明细必填来源。

`scripts/initialize-merge-schema.mjs` 会创建公共、集采和华为云表，并补充历史字段；执行前必须确认目标数据库和备份状态。

## 7. 核心业务链路

### 7.1 需求到物流

1. 需求单草稿保存主单与多条需求明细。
2. 确认需求后状态变为“待下单”，并自动生成采购草稿。
3. 采购订单可组合多个需求单明细，系统采购 ID 与用户 PO 单号并存。
4. 采购确认后需求状态更新为“已下单”，并按采购明细生成或补齐物流记录。
5. 数据导入中心导入采购订单时也执行物流同步逻辑。
6. 物流前端的实例英文名称按设备编码实时关联实例型号；地址、联系人和机房支持按 ID 或关键字匹配。

采购计算：

```text
含税单价 = 不含税单价 + 税费加成
含税总价 = 数量 × 含税单价
设备采购总成本 = 数量 × (不含税单价 + 税费加成)
```

### 7.2 月账单

每个台账生成 60 个月明细：前 24 个月使用 `first24MonthPrice`，后 36 个月使用 `next36MonthPrice`。

```text
月账单金额（含税） = 数量 × 月账单实例价格（含税）
24个月实例单价（含税自算）
  = (采购不含税单价 / 88495.58 × 3978.4 + 税费加成 / 24)
    × (1 + 国家增值税税率)
结差差额单价 = 月账单实例价格（含税） - 含税自算单价
结差差额总价 = 数量 × 结差差额单价
```

实例合同调整单规则：

- 仅使用已确认调整单。
- 对每个月查找生效月份不晚于该月的调整单。
- 在所有有效调整单中，确认时间最近的调整单优先；确认时间相同时按调整单号排序决定。
- 调整单可同时覆盖实例合同号、币种、前 24 个月价格和后 36 个月价格。
- 台账重新生成月度明细时仍须重新应用调整单。

### 7.3 预付款

- 同一 `purchaseOrderItemId` 不能同时被多个预付款合同草稿或已确认合同占用。
- 预付款合同支持实例行和非实例费用行。
- 确认合同后，每条合同明细按合同总价生成 24 个月核销明细，最后一个月吸收四舍五入差额。
- 指定月份调整单只修改目标月份，其他月份保持不变。
- 已确认合同回退草稿时删除其月度核销；存在已确认核销调整单时禁止回退。

### 7.4 实际服务费与对账单

```text
月度服务费（含税） = 月账单金额（含税） - 预付款核销金额（含税）
月度服务费（未税） = 月度服务费（含税） / (1 + 国家增值税税率)
```

- 实例行按月份、国家、批次、需求单、PO 和设备编码合并。
- 预付款非实例费用单独成行，月账单金额为 `0`。
- 服务费对账单按单一国家、单一核销月份生成草稿。
- 确认后主单与明细冻结，不受后续月账单或预付款变化影响。
- 发票附件保存在 `uploads/service-fee-invoices/`，开票状态可以独立手动切换。
- 回款登记保存收款单位、付款客户、币种、金额和日期。

### 7.5 内部服务费

内部服务费是独立管理口径，不改变月账单、预付款或实际服务费。

```text
60个月合同未税收入 = 60个月月账单含税总收入 / (1 + VAT)
采购成本 = 数量 × (采购不含税单价 + 税费加成)
内部服务费总额 = 60个月合同未税收入 - 采购成本
```

初始分摊将 50% 分配到前 24 个月，50% 分配到后 36 个月。合同价格调整后，系统保留已归档月份和人工区间调整月份，只把修订后的剩余金额分摊到其余未归档月份。归档快照一经生成，不再被自动重算。

### 7.6 CAPEX/OPEX 锚定价格

```text
资金占用比例 = 资金年利率 × 资金月数 / 12
资金占用金额 = 整机价格 × 资金占用比例
CAPEX合计 = 整机价格 × (1 + Onsite/RMA费率) + 资金占用金额
DDP价格 = CAPEX合计 × (1 + 运保清关费率)
          × (1 + 总代过手费率 + 其他税费率)
OPEX金额 = DDP价格 - CAPEX合计
原始CAPEX锚定 = CAPEX合计 × 换算汇率
原始OPEX锚定 = OPEX金额 × 换算汇率
巴西最终锚定 = 原始锚定 / (1 + 巴西服务税率)
```

价格版本为草稿时可编辑，确认后参数和计算结果锁定。新版本通过复制历史版本产生，不覆盖旧版本。

### 7.7 结差

实例结差：

```text
结算CAPEX单价 = 采购CAPEX单价 / 采购币种兑USD结差汇率
CAPEX差额单价 = 结算CAPEX单价 - CAPEX锚定单价
CAPEX差额总额 = CAPEX差额单价 × 数量
OPEX按相同规则计算
结差合计 = CAPEX差额总额 + OPEX差额总额
```

非实例费用支持备件、清关费 OPEX、跨境业务金融税、人力及行政成本和其他非实例 OPEX。灰底派生字段由服务端按规则重新计算，不能以导入值覆盖。

已确认的实例结差与非实例来源单进入“结差结算单”候选池；最终结算单记录来源单号、期间、国家、币种和汇总金额。

### 7.8 集采客户 PO、报价与项目结算

- 客户 PO 是主从单据，主表为 `po_customer_pos`，明细表为 `po_customer_po_items`。
- PO 主单可填写项目名称，项目名称沿 PO -> 报价单 -> 项目结算传递。
- PO 明细只有产品名称、品牌、规格和数量为业务必填；行号由服务端自动生成。目标单价为空按 `0` 保存，币种为空按 `USD` 保存，产品主档匹配为可选。
- `poDate` 和 `deliveryDate` 是 MySQL `DATE`，保存前必须规范化为 `YYYY-MM-DD`，禁止将纯日期序列化成 UTC `Date` 字符串。
- 报价单从 PO 生成时按产品编码匹配产品主档，带出产品名称、品牌、规格、品类、关税率和 NOM 信息；历史报价只按当前报价之前最近一条匹配记录展示，不覆盖当前报价。
- 报价成本根据采购数量、币种、原币不含税单价、运输方式、清关、NOM、CIF 分摊和加价率计算。海外增值税和坏账率不进入最终报价成本。
- 项目结算关联报价单，已采购商品、其他费用成本、销售收入和发票均为项目明细；非 USD 金额转换为 USD 时使用报价单汇率。
- PO、报价单和项目结算的创建/更新/确认人由当前登录用户自动写入主单审计字段，明细不重复记录人员。

## 8. API 约定

### 8.1 通用响应与分页

分页列表统一使用：

```text
GET ?page=1&pageSize=20&keyword=...&countryCode=...
```

响应结构：

```json
{
  "rows": [],
  "total": 0,
  "page": 1,
  "pageSize": 20,
  "totalPages": 0
}
```

大列表在数据库执行 `COUNT`、`LIMIT` 和 `OFFSET`。导出接口忽略当前页，导出筛选条件命中的全部数据；专用接口通常使用 `export=1`。

错误响应使用：

```json
{ "error": "可展示给用户的错误信息" }
```

### 8.2 认证、首页与系统配置

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/auth/login` | 登录并写入 HttpOnly Cookie |
| `POST` | `/api/auth/logout` | 清除登录 Cookie |
| `GET` | `/api/dashboard/overview` | 首页图表 |
| `GET/PUT` | `/api/user-preferences/sidebar-order` | 读取或保存目录顺序 |
| `GET/PUT` | `/api/system/module-features` | 查询模块状态；管理员修改启停 |

### 8.3 通用实体

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET/POST` | `/api/entities/{entity}` | 分页查询或新建 |
| `GET/PUT/DELETE` | `/api/entities/{entity}/{id}` | 详情、更新、删除 |
| `GET` | `/api/entities/{entity}/export` | 导出筛选后的全部数据 |
| `GET` | `/api/entities/{entity}/template` | 下载导入模板 |
| `POST` | `/api/entities/{entity}/import` | 导入并返回成功、失败明细 |

实体键由 `src/lib/modules.ts` 定义。

### 8.4 需求、采购与物流

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/orders` | 订单主单分页列表 |
| `GET` | `/api/order-details/{type}/{id}` | 按主单业务编号读取主从详情 |
| `POST` | `/api/procurement/from-request` | 从需求单生成采购草稿 |
| `POST` | `/api/procurement/{poNo}/confirm` | 确认采购并同步物流 |
| `POST` | `/api/procurement/shipments/sync` | 批量补齐采购对应物流 |
| `GET` | `/api/requests/product-lines` | 需求明细一览分页 |
| `GET` | `/api/purchase/product-lines` | 采购明细一览分页 |
| `GET` | `/api/purchase/product-lines/export` | 导出采购明细全部筛选结果 |

### 8.5 月账单与预付款

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/billing/available` | 待生成月账单实例 |
| `POST` | `/api/billing/confirm` | 创建或更新台账并生成 60 个月明细 |
| `GET` | `/api/billing/monthly-writeoffs` | 月账单每月明细分页 |
| `GET/POST` | `/api/billing/adjustments` | 调整单列表、新建草稿 |
| `GET/PUT/DELETE` | `/api/billing/adjustments/{adjustmentNo}` | 调整单详情、保存、删除 |
| `POST` | `/api/billing/adjustments/{adjustmentNo}/confirm` | 确认调整单并重算 |
| `POST` | `/api/billing/adjustments/{adjustmentNo}/import` | 导入调整明细 |
| `GET` | `/api/billing/adjustments/{adjustmentNo}/template` | 调整单模板 |
| `GET/POST` | `/api/billing-statements` | 对账单列表、预览或创建 |
| `GET/DELETE` | `/api/billing-statements/{snapshotNo}` | 详情、删除未确认单据 |
| `POST` | `/api/billing-statements/{snapshotNo}/confirm` | 确认对账单 |
| `GET` | `/api/billing-statements/{snapshotNo}/export` | 导出格式化 Excel |
| `GET` | `/api/prepayments/available` | 待生成预付款实例 |
| `POST` | `/api/prepayments/drafts` | 创建预付款草稿 |
| `GET/PUT/DELETE` | `/api/prepayments/contracts/{contractNo}` | 合同详情、保存草稿、删除草稿 |
| `POST` | `/api/prepayments/contracts/{contractNo}/confirm` | 确认合同 |
| `POST` | `/api/prepayments/contracts/{contractNo}/rollback` | 已确认合同回退草稿 |
| `GET` | `/api/prepayments/monthly-writeoffs` | 预付款每月核销分页 |
| `GET/POST` | `/api/prepayment-adjustments` | 预付款调整单列表、新建 |
| `GET/PUT/DELETE` | `/api/prepayment-adjustments/{adjustmentNo}` | 调整单详情、保存、删除 |
| `POST` | `/api/prepayment-adjustments/{adjustmentNo}/confirm` | 确认指定月份调整 |

### 8.6 实际服务费

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/service-fees/calculate` | 服务费分页、汇总或全部导出数据 |
| `GET/POST` | `/api/service-fees/snapshots` | 服务费对账单列表、创建草稿 |
| `PATCH/DELETE` | `/api/service-fees/snapshots/{snapshotNo}` | 修改开票状态或删除未确认单据 |
| `POST` | `/api/service-fees/snapshots/{snapshotNo}/confirm` | 确认冻结 |
| `GET/POST/DELETE` | `/api/service-fees/snapshots/{snapshotNo}/invoice` | 下载、上传、删除发票 |
| `PATCH` | `/api/service-fees/snapshots/{snapshotNo}/repayment` | 保存回款信息 |

### 8.7 内部服务费

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/internal-service-fees/available` | 待初始化台账分页 |
| `GET/POST` | `/api/internal-service-fees` | 查询明细、初始化或重算 |
| `GET` | `/api/internal-service-fees/adjustments/list` | 调整单分页列表 |
| `POST` | `/api/internal-service-fees/adjustments` | 创建区间调整并重算剩余月份 |
| `DELETE` | `/api/internal-service-fees/adjustments/{adjustmentNo}` | 撤销未涉及归档月的调整 |
| `POST` | `/api/internal-service-fees/archive` | 归档指定月份 |
| `GET` | `/api/internal-service-fees/snapshots` | 查询归档快照 |

### 8.8 锚定价格与结差

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/capex-pricing/b6-types` | 启用的 B6 规则 |
| `GET` | `/api/capex-pricing/defaults` | 按国家和 B6 类型取得默认参数 |
| `GET/POST` | `/api/capex-pricing/versions` | 版本列表、新建版本 |
| `GET/PUT` | `/api/capex-pricing/versions/{versionId}` | 版本详情、保存草稿 |
| `POST` | `/api/capex-pricing/versions/{versionId}/confirm` | 确认锁定版本 |
| `POST` | `/api/capex-pricing/versions/{versionId}/clone` | 复制新版本 |
| `GET` | `/api/capex-pricing/versions/{versionId}/calculation/{itemId}` | 计算过程与历史版本 |
| `GET/POST` | `/api/balance-settlements` | 实例结差来源单列表、生成 |
| `GET` | `/api/balance-settlements/available` | 待生成实例结差候选 |
| `POST` | `/api/balance-settlements/manual` | 生成非实例费用结差草稿 |
| `GET` | `/api/balance-settlements/{settlementNo}` | 来源单详情 |
| `POST` | `/api/balance-settlements/{settlementNo}/confirm` | 确认来源单 |
| `POST` | `/api/balance-settlements/{settlementNo}/void` | 作废草稿 |
| `GET/POST` | `/api/balance-settlements/finals` | 最终结算单列表、新建 |
| `GET` | `/api/balance-settlements/finals/available` | 可汇总来源单 |
| `GET` | `/api/balance-settlements/finals/{finalSettlementNo}` | 最终结算单详情 |
| `POST` | `/api/balance-settlements/finals/{finalSettlementNo}/confirm` | 确认最终结算单 |
| `POST` | `/api/balance-settlements/finals/{finalSettlementNo}/void` | 作废草稿 |

### 8.9 文档与导入中心

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/documents/tree` | 文件夹树 |
| `GET` | `/api/documents/items` | 当前文件夹内容 |
| `POST` | `/api/documents/folders` | 新建文件夹 |
| `PUT/DELETE` | `/api/documents/folders/{folderId}` | 重命名、删除文件夹 |
| `POST` | `/api/documents/files/upload` | 批量上传文件 |
| `PUT/DELETE` | `/api/documents/files/{fileId}` | 重命名、删除文件 |
| `GET` | `/api/documents/files/{fileId}/download` | 下载文件 |
| `GET` | `/api/import-center` | 导入历史分页 |
| `GET` | `/api/import-center/template` | 下载中心模板 |
| `POST` | `/api/import-center/preview` | 解析文件并返回预览 |
| `POST` | `/api/import-center/confirm` | 事务执行导入 |
| `GET` | `/api/import-center/jobs/{jobId}/errors` | 导入错误报告 |

### 8.10 集采、华为云与公共基础资料

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/po/customer-pos` | 客户 PO 列表、分页、排序和筛选 |
| `GET` | `/api/po/customer-pos/{poId}` | 客户 PO 主从详情 |
| `POST` | `/api/po/customer-pos/{poId}/confirm` | 确认客户 PO |
| `POST` | `/api/po/customer-pos/{poId}/match` | 批量匹配 PO 明细产品主档 |
| `POST` | `/api/po/customer-pos/items/import` | 导入当前 PO 的产品明细 |
| `GET` | `/api/po/customer-pos/items/template` | 下载客户 PO 明细导入模板 |
| `POST` | `/api/po/quotations/from-customer-po` | 根据客户 PO 创建报价单 |
| `GET` | `/api/po/quotations/{quotationId}/items/export` | 导出报价明细 |
| `POST` | `/api/po/quotations/{quotationId}/items/import` | 导入报价明细及不含税单价 |
| `GET` | `/api/po/settlement-projects` | 项目结算列表、分页和筛选 |
| `GET` | `/api/po/settlement-projects/{id}` | 项目结算主从详情 |
| `GET` | `/api/po/invoices` | 集采项目发票汇总 |
| `GET/POST` | `/api/cloud/rows` | 华为云对账单列表和手动新增 |
| `PUT/DELETE` | `/api/cloud/rows/{id}` | 修改或删除未确认华为云对账单 |
| `POST` | `/api/cloud/rows/{id}/confirm` | 确认华为云对账单 |
| `POST` | `/api/cloud/import` | 导入华为云账单 |
| `GET/POST` | `/api/cloud/supplier-payments` | 华为云供应商付款汇总和登记 |
| `GET` | `/api/cloud/master-data` | 华为云下拉数据和业务伙伴匹配数据 |
| `GET/POST` | `/api/entities/{entity}` | 公共业务伙伴实体的列表和新增 |
| `GET/PUT/DELETE` | `/api/entities/{entity}/{id}` | 供应商、客户、承接单位及子表详情维护 |
| `POST` | `/api/common/attachments/{ownerType}/{ownerId}` | 公共附件上传 |

客户 PO 的日期与可选金额字段约定：`poDate`、`deliveryDate` 只传 `YYYY-MM-DD`；明细 `targetUnitPrice` 为空转为 `0`，`currency` 为空转为 `USD`，`matchStatus` 为空转为 `unmatched`。该归一化在前端请求体、导入接口和 CRUD 最终入库参数层均有兜底。

## 9. 认证与权限

- `middleware.ts` 保护业务页面和 API，未登录请求跳转登录页或返回 401。
- 登录账号来自 `common_users`，密码保存为加盐哈希；权限数据来自 `common_user_permissions`。
- Cookie 使用 HttpOnly，会话值由 `AUTH_SESSION_TOKEN` 控制。
- `admin` 角色绕过业务模块权限检查；普通用户按一级目录、二级目录、模块和查看/新增/修改/删除/导入/导出/确认权限检查。
- 用户管理和功能启用仅管理员可见；停用账号不能登录。
- 模块停用同时隐藏导航，并拦截对应页面和 API；不会删除数据。

## 10. 前端工作区

- 外层 `AppShell` 维护左侧目录和内部标签页。
- 每个标签页通过嵌入路由保留筛选、分页和未提交表单状态。
- 标签页状态保存在 `sessionStorage`，关闭标签或浏览器会话结束后清除。
- 左侧一级目录顺序保存在 `common_user_preferences`，按登录账号独立保存。
- 左侧一级目录当前为：算力系统、集采系统、华为云业务、业务伙伴、用户管理、文档管理。业务伙伴下直接放供应商管理、客户管理和承接单位管理；文档管理是公共一级目录。
- 主从详情使用 `/api/order-details/{type}/{id}` 按业务单号读取，返回列表时通过标签状态保留原分页。

## 11. 导入、导出与格式

- 导入中心流程：模板下载、上传预览、错误报告、确认写入、历史记录；客户 PO、报价明细、项目结算未采购商品、华为云账单等专用导入入口也必须提供对应模板。
- 主从导入按业务单号分组，同一主单可包含多条明细。
- 日期在服务端按本地年月日解析，前端只显示 `YYYY-MM-DD`。
- 金额统一显示千分位和两位小数；数据库可保留更高精度用于计算。
- 百分比字段前端按百分比展示，数据库保存小数值，例如 `16%` 保存为 `0.16`。
- 导出使用当前筛选条件，但不受当前分页限制。
- 三横列菜单支持升序、降序、筛选和锁定；筛选选项应从当前筛选条件下的全量候选数据生成，不能只从当前页生成。

## 12. 开发与验证

```bash
npm ci
npm run dev:hot
```

提交前至少执行：

```bash
npm test
npm run build
git diff --check
```

数据库脚本：

| 命令 | 用途 | 注意事项 |
| --- | --- | --- |
| `npm run schema:init-merge` | 初始化或补齐 `merge` 数据库结构和必要系统数据 | 只在确认目标库和备份后执行；不要用于清空业务数据 |
| `npm run migrate` | 本地开发库增量迁移 | 不得直接用于生产 |
| `npm run schema:deploy` | 生成无业务数据的部署结构 | 先检查输出文件 |
| `npm run schema:verify` | 只读验证关键表 | 独立 Node 脚本需显式注入环境变量 |
| `npm run schema:compare-remote` | 比较本地与目标库字段 | 需要 `TARGET_DB_*` |
| `npm run schema:sync-remote` | 同步远程结构 | 需要显式应用开关和审批 |
| `npm run internal-service-fees:sync` | 补算内部服务费 | 会写业务数据，只能维护窗口执行 |

## 13. 扩展检查清单

新增或修改模块时检查：

1. `schema.sql`、`scripts/migrate.ts` 和 `LOGICAL_TABLE_NAMES` 是否同步。
2. 主单、明细、导入、导出、删除和确认状态是否形成完整闭环。
3. 列表是否在数据库分页、筛选和排序，导出是否获取全部筛选结果。
4. 主从详情是否按业务单号读取，返回后是否保留原标签状态。
5. 客户、供应商、承接单位、国家和批次是否沿下游链路传递。
6. 金额、百分比、日期和月份是否使用统一格式。
7. 调整单、确认单和快照是否遵守冻结及优先级规则。
8. 新模块是否加入导航、模块启停定义和 API 路由保护。
9. 是否补充纯算法测试、服务测试和关键页面测试。
10. 是否更新部署文档、开发者文档和系统操作手册。
