# cloud_power 开发者文档

## 1. 系统架构

系统采用 Next.js App Router、React、TypeScript 和 MySQL。页面、API 与服务端业务逻辑位于同一项目中：

- 页面路由：`src/app/**/page.tsx`
- API 路由：`src/app/api/**/route.ts`
- 业务服务：`src/lib/*-service.ts`
- 数据库访问与表名映射：`src/lib/db.ts`
- 通用实体定义：`src/lib/modules.ts`
- 前端组件：`src/components/`

## 2. 数据库约定

部署数据库为 `cloud`，应用通过环境变量连接：

```env
DB_HOST=139.9.122.9
DB_PORT=3306
DB_USER=cloud_wenjian
DB_PASSWORD=<数据库密码>
DB_NAME=cloud
DB_CONNECTION_LIMIT=5
```

代码中使用逻辑表名，例如 `requests`、`purchaseorders`。`src/lib/db.ts` 会自动加上 `power_` 前缀，实际访问 `power_requests`、`power_purchaseorders`。不要在业务 SQL 中手工写 `power_` 前缀，除非调用原始数据库接口。

系统不使用数据库外键；主从关系、状态流转和数据一致性由服务端代码维护。索引和唯一约束仍用于保证关键数据唯一性。

## 3. 表结构

| 分类 | 物理表 | 用途 |
| --- | --- | --- |
| 基础信息 | `power_countries`、`power_deliverylocations`、`power_deliverycontacts`、`power_datacenters`、`power_instancemodels`、`power_suppliers`、`power_undertakingunits` | 国家、交付地点、联系人、机房、实例型号、供应商、承接单位 |
| 合同 | `power_instancecontracts`、`power_contractitems`、`power_billingadjustments`、`power_billingadjustmentitems` | 实例合同及月账单实例合同调整 |
| 需求采购 | `power_requests`、`power_requestitems`、`power_purchaseorders`、`power_purchaseorderitems` | 需求单、采购订单及明细 |
| 要货计划 | `power_purchaseordersnitems`、`power_purchaseorderplanitems` | 要货计划 SN 码和要货计划子 |
| 物流 | `power_shipments` | 按采购实例生成的物流记录和交付快照 |
| 预付款 | `power_prepaymentcontracts`、`power_prepaymentcontractitems`、`power_monthlyprepaymentwriteoffs`、`power_prepaymentwriteoffadjustments`、`power_prepaymentwriteoffadjustmentitems` | 预付款合同、月度核销与指定月份调整 |
| 月账单 | `power_billinginstanceledgers`、`power_monthlybillingwriteoffs`、`power_billingstatementsnapshots`、`power_billingstatementsnapshotitems` | 月账单台账、60 个月明细及对账单快照 |
| 服务费 | `power_servicefeesnapshots`、`power_servicefeesnapshotitems` | 服务费核算快照 |
| 文档导入 | `power_documentfolders`、`power_documentfiles`、`power_importjobs` | 文档管理与导入历史 |
| 认证 | `power_appusers` | 登录账号、加盐密码哈希和状态 |

完整的字段、DDL 与索引以 `schema.sql` 和 `scripts/migrate.ts` 为准。

## 4. 核心业务规则

1. 需求单保存后为草稿；确认需求后生成采购草稿；采购确认后需求单更新为已下单并生成实例物流记录。
2. 采购订单可以包含多个需求单号，业务展示优先使用用户填写的 PO 单号。
3. 月账单台账按实例合同生成 60 个月明细。实例合同调整单优先于原实例合同，按确认时间与生效月份覆盖后续月份。
4. 预付款实例以 `purchaseOrderItemId` 作为占用来源。同一采购明细不能重复进入多个预付款合同；草稿、确认、导入均进行服务端校验。
5. 已确认预付款合同回退草稿会清除其月度预付款核销明细；存在已确认预付款调整单时不可回退。
6. 服务费实时计算为月账单总额减预付款核销金额；服务费快照一经确认不随之后续核销变更而修改。

## 5. API 接口

### 认证与首页

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/auth/login` | 登录 |
| `POST` | `/api/auth/logout` | 退出登录 |
| `GET` | `/api/dashboard/overview` | 首页服务费与新增实例图表 |

### 通用实体接口

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/entities/{entity}` | 分页列表与筛选 |
| `POST` | `/api/entities/{entity}` | 新建实体 |
| `GET` | `/api/entities/{entity}/{id}` | 获取详情 |
| `PUT` | `/api/entities/{entity}/{id}` | 修改实体 |
| `DELETE` | `/api/entities/{entity}/{id}` | 删除实体 |
| `GET` | `/api/entities/{entity}/export` | 导出筛选后的全部数据 |
| `GET` | `/api/entities/{entity}/template` | 下载导入模板 |
| `POST` | `/api/entities/{entity}/import` | 导入实体数据 |

`entity` 的有效值定义在 `src/lib/modules.ts`，包括 `countries`、`requests`、`purchase-orders`、`shipments`、`purchase-order-sn-items`、`purchase-order-plan-items` 等。

### 采购与需求

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/procurement/from-request` | 从需求单生成采购草稿 |
| `POST` | `/api/procurement/{poNo}/confirm` | 确认采购订单并生成物流 |
| `POST` | `/api/purchase/product-lines/export` | 导出采购明细一览 |

### 月账单、预付款与服务费

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/billing/available` | 待生成月账单实例 |
| `POST` | `/api/billing/confirm` | 确认月账单台账并生成月度明细 |
| `GET` | `/api/billing/monthly-writeoffs` | 月账单每月明细 |
| `GET`、`POST` | `/api/billing-statements` | 对账单快照列表与创建 |
| `GET` | `/api/billing-statements/{snapshotNo}/export` | 导出对账单 Excel |
| `GET` | `/api/prepayments/available` | 待生成预付款实例 |
| `POST` | `/api/prepayments/drafts` | 生成预付款合同草稿 |
| `GET`、`PUT`、`DELETE` | `/api/prepayments/contracts/{contractNo}` | 预付款合同查询、保存、删除草稿 |
| `POST` | `/api/prepayments/contracts/{contractNo}/confirm` | 确认预付款合同 |
| `POST` | `/api/prepayments/contracts/{contractNo}/rollback` | 已确认合同回退草稿 |
| `GET` | `/api/prepayments/monthly-writeoffs` | 预付款每月核销明细 |
| `GET` | `/api/service-fees/calculate` | 实时服务费核算 |
| `POST` | `/api/service-fees/snapshots` | 保存服务费核算快照 |

### 文档与导入中心

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET`、`POST` | `/api/documents/folders` | 创建及查询文件夹 |
| `PUT`、`DELETE` | `/api/documents/folders/{folderId}` | 重命名、删除文件夹 |
| `POST` | `/api/documents/files/upload` | 批量上传文件 |
| `GET`、`PUT`、`DELETE` | `/api/documents/files/{fileId}` | 文件读取、重命名、删除 |
| `GET` | `/api/documents/files/{fileId}/download` | 下载文件 |
| `GET` | `/api/import-center` | 导入历史分页列表 |
| `POST` | `/api/import-center/preview` | 上传并生成导入预览 |
| `POST` | `/api/import-center/confirm` | 确认执行导入 |

## 6. 本地开发

```bash
npm install
npm run dev:hot
```

常用校验：

```bash
npm test
npm run build
```

本地数据库结构升级使用 `npm run migrate`。远程 `cloud` 数据库已完成初始化，生产部署不要自行执行迁移；结构变更须先备份并单独评估。

## 7. 扩展原则

1. 新增表时同时更新 `schema.sql`、`scripts/migrate.ts`、`src/lib/db.ts` 的逻辑表清单和相关服务代码。
2. 新增通用模块时优先通过 `src/lib/modules.ts` 配置字段、筛选和导航，再补充必要的专用页面。
3. 新增导入能力时须支持模板、预览、错误行提示、成功数量和失败原因。
4. 任何金额页面与导出使用 `formatDisplayValue(value, "money")`，统一两位小数和千分位。
5. 修改数据状态流转时必须同步检查物流、月账单、预付款、服务费和快照的下游影响。
