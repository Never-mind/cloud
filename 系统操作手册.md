# cloud_power 合并业务系统操作手册

> 本手册以当前 `main` 分支为准，适用于算力系统、集采系统、华为云业务和公共基础功能。文档更新日期：2026-08-31。日期统一按年月日显示，金额统一显示千分位和两位小数。

## 1. 登录、首页与工作区

### 1.1 登录

1. 打开部署人员提供的系统地址，例如 `http://127.0.0.1:3000/`；本地开发验证通常使用 `http://127.0.0.1:5174/`。
2. 未登录时系统自动进入登录页。
3. 初始管理员账号：`admin@luzcorp.com`。
4. 初始管理员密码：`admin@luzcorp.com`。首次登录后应由管理员修改密码。

生产系统应由管理员妥善保管账号，不要在聊天、Excel 或公共文档中传播密码。

### 1.2 首页

首页展示：

- 按国家、月份和币种汇总的服务费图表。
- 按国家、月份统计的新增实例数量。
- 当前已启用模块的快捷入口。

附加功能被管理员停用后，不会出现在首页和左侧目录，但历史数据不会被删除。

### 1.3 内部标签页

- 点击左侧功能会在系统内部打开标签页，不覆盖当前页面。
- 未关闭的标签页会保留筛选条件、页码和表单输入。
- 从主单打开明细时，明细会使用独立标签或更新当前明细标签。
- 关闭标签后，该标签中的临时页面状态会清除。
- 浏览器刷新后系统会恢复当前会话中的标签；关闭浏览器会话后不保证继续保留。

### 1.4 左侧目录

当前默认一级目录为：算力系统、集采系统、华为云业务、业务伙伴、用户管理、文档管理；首页固定在最上方。算力系统和集采系统内部再按业务分组显示。

- 点击一级目录可展开或收起。
- 业务伙伴下直接显示供应商管理、客户管理、承接单位管理。
- 算力系统包括客户需求、采购管理、合同管理、物流管理、财务管理、基础信息和数据工具。
- 集采系统包括客户 PO、项目结算、财务管理、产品管理和采购管理。
- 华为云业务下为华为云对账。
- 将鼠标移到目录拖动位置，可拖动调整一级目录顺序。
- 顺序按当前登录账号保存。
- 点击左上角“恢复默认目录顺序”可恢复默认排列。
- 左侧目录可滚动，屏幕较小时仍可访问底部模块。

## 2. 基础信息

基础信息是需求、采购、物流和财务取数的来源。建议先维护基础信息，再导入业务单据。

### 2.1 国家管理

字段包括国家代码、中英文名称、当地语言名称和增值税税率。

- 税率按百分比录入和显示，例如输入 `16` 表示 `16%`。
- 月账单对账单、服务费未税金额和内部服务费会读取国家税率。
- 修改税率会影响之后重新计算的数据，不会覆盖已确认对账单快照。

### 2.2 交付地址、收件人和机房

- 交付地址维护地点 ID、国家、类型、名称和详细地址。
- 收件人维护联系人 ID、所属地点、姓名、电话和邮箱。
- 机房维护机房 ID、地址、中文名称和英文名称。
- 物流编辑和导入时，可通过 ID 或关键字搜索选择。
- 选择后物流记录保存地址、姓名和电话快照，便于保留当时交付信息。

### 2.3 实例型号

维护设备编码、机型、xxll 编码、中英文名称和默认 B6 类型。

规则：

- 设备编码必须唯一。
- 机型、xxll 编码、中文名称和英文名称允许重复。
- 需求单、实例合同、物流和锚定价格可通过设备编码自动带出型号资料。

### 2.4 供应商管理、承接单位管理和客户管理

供应商、客户和承接单位是三个业务系统共用的公共基础信息，维护编码、简称、中文全称、英文名称、国家、城市、地址、税务信息、合作状态、联系人和银行账户等资料。三者业务数据仍分别保存，不能因为字段名称相同而共用业务单据表。

- 供应商主表维护供应商类型、主要供应品类和品牌等供应商专属信息。
- 客户主表维护合作业务，可选择算力业务、集采业务、华为云业务，也可以添加其他业务类型。
- 承接单位保留承接单位编码和承接单位全称（中文），不增加供应商类型字段。
- 进入详情后，已有档案点击“修改”编辑；未保存档案也可以直接新增联系人和银行账户，保存时系统会提示先补齐基础资料。
- 银行账户必填账户名称、银行名称、银行账号和币种；币种支持下拉选项，也允许手工输入其他币种。银行行号、Swift code 和银行地址为可选项。
- 多个联系人或银行账户时，可直接切换默认联系人或默认账户；主表显示当前默认联系人。
- 主表默认按编码升序排列，简称用于列表和业务单据展示，数据库保存业务编码或 ID。

- 需求明细可模糊搜索设备编码、供应商、承接单位和客户。
- 财务和物流列表优先显示业务代码，不显示冗长内部 ID。
- 服务费回款登记中的收款单位来自承接单位，付款单位来自客户。

### 2.5 基础信息通用操作

1. 进入对应模块。
2. 使用新建、编辑或删除维护单条数据。
3. 使用“下载模板”取得当前模块模板。
4. 导入前查看必填字段说明。
5. 导入完成后查看成功数量和失败原因。
6. 导出时系统导出当前筛选条件下的全部数据，不只导出当前页。

删除基础信息前应确认没有业务单据正在引用该记录。

## 3. 合同管理

### 3.1 实例合同

用途：维护国家、设备编码和分阶段实例合同含税价格。

操作：

1. 进入“合同管理 - 实例合同”。
2. 新建或导入合同。
3. 填写合同号、国家、设备编码、币种、前 24 个月含税单价和后 36 个月含税单价。
4. 机型和实例型号英文名称可按设备编码自动带出。
5. 保存后，在待生成月账单实例中可搜索选择该合同。

唯一规则：合同号、国家和设备编码三项不完全相同即可同时存在，因此一个合同号可以包含多种设备。

### 3.2 实例合同调整单

用途：在月账单核销开始后，从指定月份起调整实例合同号、币种和分阶段价格。

操作：

1. 新建调整单，填写调整单号、实例合同号和生效月份。
2. 在主从单据明细区域搜索并添加特定实例，或使用明细导入。
3. 输入设备编码后，系统自动带出机型和英文名称。
4. 填写新币种、前 24 个月价格和后 36 个月价格。
5. 点击“保存草稿”后，按钮变为“修改”，页面进入只读状态。
6. 再次修改时点击“修改”。
7. 检查无误后确认调整单。

取值规则：

- 只有已确认调整单参与月账单计算。
- 对每个月，系统只考虑生效月份不晚于该月的调整单。
- 多张调整单同时有效时，确认时间最近的调整单优先。
- 最新确认调整单即使生效月份更早，也会覆盖其生效月以后较早确认的调整单。
- 月账单台账删除后重新生成，或修改起始月份后重新生成，仍会重新应用调整单。
- 调整来源的月账单明细显示调整单中的实例合同号和币种。

## 4. 客户需求

### 4.1 新建需求单

1. 进入“客户需求 - 需求单”，点击新建。
2. 填写需求单号、国家、合同号、批次、需求类型和计划交付日期。
3. 新增需求明细。
4. 通过模糊搜索选择设备编码、供应商、承接单位和客户。
5. 输入节点数量和需求日期。
6. 点击“保存草稿”。

保存草稿后页面进入只读状态，按钮显示“修改”。只有点击“修改”后才能继续编辑。

### 4.2 确认需求

点击“确认需求单”后：

- 按钮显示“已确认”。
- 需求单状态变为“待下单”。
- 系统自动生成一张采购草稿。
- 页面保留在需求单，不强制跳转采购订单。

未生成月账单和预付款前，符合条件的需求单可以删除。删除前系统会检查采购和财务下游数据。

### 4.3 需求单列表与需求明细一览

- 支持国家、状态和关键字筛选。
- 默认按批次末尾数字从大到小排序，例如 `CL-5` 排在 `CL-1` 前。
- 点击需求单号进入该主单对应明细。
- 从第三页等页面进入详情并返回时，原页码和筛选条件保留。
- 需求明细一览是只读汇总，数据来源于需求单主从明细。
- 导出包含前端展示的全部字段，并导出全部筛选结果。

## 5. 采购管理

### 5.1 新建采购订单

采购订单可来自自动生成的采购草稿，也可人工新建并组合多个需求单明细。

1. 进入“采购管理 - 采购订单”，点击新建或打开草稿。
2. 系统采购 ID 可自动生成。
3. PO 订单号由用户填写，后续业务关联和展示优先使用 PO 订单号。
4. 选择一个或多个需求单的明细。
5. 选择币种：CNY、MXN、CLP、USD、BRL。
6. 上方币种变化后，下方未保存明细立即同步。
7. 填写数量和价格。
8. 保存草稿或确认采购。

采购价格字段：

```text
含税单价 = 不含税单价 + 税费加成
含税总价 = 数量 × 含税单价
```

采购 CAPEX 单价和采购 OPEX 单价用于实例结差，不影响原有含税采购金额计算。

数字输入框中的默认 `0` 会在输入有效数字时自动移除。

### 5.2 确认采购

确认后：

- 按钮显示“已确认”。
- 采购订单锁定为已确认状态。
- 关联需求单更新为“已下单”。
- 系统按采购明细生成或补齐物流记录。

数据导入中心导入采购订单时，同样会同步物流记录。未生成月账单和预付款前，符合条件的采购订单可以删除。

### 5.3 采购列表与采购明细一览

- 采购列表显示 PO 单号，不在主列表展示过长的系统采购 ID。
- 进入详情后仍可查看系统采购 ID 和 PO 单号。
- 支持国家筛选、批次数字倒序、分页、每页条数和页码跳转。
- 金额使用千分位和两位小数。
- 导出包含当前前端字段和全部筛选数据。

### 5.4 要货计划 SN 码与要货计划子

两个子模块位于采购订单详情中：

- 模板字段按业务 Excel 定义保存为文本、日期或数字。
- SN、编码等可能以 `0` 开头的长串值按文本保存，不丢失前导零。
- 导入按钮位于各自明细区域。
- 在采购订单详情内导入时，系统自动关联当前采购订单，不要求模板重复填写 PO 单号。
- 一台设备可以存在几十或上百条 SN 明细，不同步扩展物流行数。

### 5.5 产品管理

产品管理与采购管理同级，包含“产品类别”和“产品主档”。产品主档是集采报价和项目结算的产品参考资料；客户 PO 明细仍允许录入尚未建档的产品。

产品类别操作：

1. 进入“集采系统 - 产品管理 - 产品类别”。
2. 新增或编辑品类名称、墨西哥 HS 编码、是否需要 NOM 认证及其他类别规则字段。
3. 保存后，产品主档选择该品类时自动带出 HS 编码和 NOM 规则。

产品主档操作：

1. 进入“集采系统 - 产品管理 - 产品主档”。
2. 新建主档，填写产品编码、产品名称、产品英文名称、规格、品牌、品类、单位、建议采购价（CNY）、长（cm）、宽（cm）、高（cm）和毛重（kg）。
3. 产品编码用于报价历史匹配；同一品牌、规格应使用独立编码。
4. 选择品类后，系统从产品类别带出墨西哥 HS 编码和 NOM 认证规则，并保存到产品主档。
5. 列表默认按产品编码排序；支持关键字、三横菜单筛选、升序/降序、锁定列、分页和导出。
6. 进入产品主档详情后，可查看或维护品牌型号和规格扩展资料。新增集采 PO 时不要求必须先建立产品主档。

### 5.6 客户 PO

客户 PO 是集采系统的主从单据。主表保存项目名称、PO 单号、PO 日期、交付日期、承接单位、客户和状态；明细保存客户提供的产品行。

新建和编辑：

1. 进入“集采系统 - 客户 PO”，点击“新建客户 PO”。
2. 填写项目名称、PO 单号、PO 日期、交付日期，并从业务伙伴搜索选择承接单位和客户。选择框支持编码、简称关键字匹配。
3. 在下方明细新增行或下载明细模板导入。行号由系统自动生成。
4. 明细必填只有产品名称、品牌、规格、数量；产品编码、币种、目标单价、产品主档匹配等字段可以留空。
5. 保存草稿时，空目标单价按 `0` 保存，空币种按 `USD` 保存，空匹配状态按“未匹配”保存。
6. PO 日期和交付日期按日期填写，系统按 `YYYY-MM-DD` 保存，不要把时间或时区写入日期单元格。
7. PO 明细可以在创建时使用导入；导入模板中的产品名称、品牌、规格、数量必须有值。

确认和后续：

- 草稿可修改或删除；确认后主单和明细锁定，不能再修改。
- PO 主单可点击“生成报价单”，项目名称、客户、承接单位和明细复制到报价单。
- 产品明细可以后续匹配产品主档；匹配后按产品编码带出名称、品牌、规格、品类、关税和 NOM 信息。
- 主单底部显示创建/更新/确认时间和对应人员；明细不重复记录人员。

### 5.7 报价单

报价单采用主从结构。报价列表和客户 PO 列表使用相同的列表交互：分页、关键字、三横菜单筛选、升序/降序、锁定列、导出和图标操作。

报价参数：

- 汇率、资金成本率、账期（月）、清关手续费率、加价率等参数在主单顶部维护。
- 海外增值税率和坏账率可以作为参考参数显示，但不加入最终报价成本。
- 公共费用默认使用 USD；尾程费、清关手续费等公共费用继续按 CIF 比例分摊。
- 空运体积 = 长 × 宽 × 高 ÷ 6000；海运尺寸以厘米计算，再 ÷ 1,000,000 换算立方米。

明细计算：

```text
不含税总价 = 采购数量 × 原币不含税单价，并按报价汇率折算 USD
CIF = 不含税总价 + 头程运费
到仓总价 = CIF + 关税 + 资金成本 + 清关手续费 + NOM认证费 + 分摊公共费用
到仓单价 = 到仓总价 ÷ 采购数量
DDP不含税单价 = 到仓单价 × (1 + 加价率)
DDP不含税总价 = DDP不含税单价 × 采购数量
项目毛利 = DDP不含税总价 - 到仓总价
毛利率 = 项目毛利 ÷ DDP不含税总价
```

- 运输方式可选空运、海运或无运输；是否清关决定是否计算关税；是否 NOM 认证决定 NOM 认证费是否为固定金额。
- 加价率和毛利率是不同指标。用户可以手工改 DDP 不含税单价，系统按 `DDP不含税单价 / 到仓单价 - 1` 反算加价率；也可以直接填写加价率让系统反算 DDP 单价。
- 报价明细导入/导出用于批量维护产品编码、采购数量、币种、原币不含税单价和其他明细参数。导入前先下载当前版本模板。
- 历史报价只按“当前报价之前最近一条”匹配客户 ID 和产品编码，用于参考展示，不自动覆盖当前报价。
- 报价单未确认前可以编辑；确认后锁定。项目名称沿客户 PO 传递到项目结算。

### 5.8 项目结算

项目结算由报价单生成，项目名称、客户、承接单位、报价明细和报价汇率自动带入。主表显示采购成本、销售收入、项目毛利和状态，详情页维护主从明细。

操作顺序：

1. 在报价单详情确认报价后，点击“生成项目结算”。
2. 在“未采购商品”中查看报价数量；新增或导入采购信息时，采购数量默认等于报价数量，导入不得修改报价数量。
3. 填写采购币种、采购单价、价格方式和税率。非 USD 金额按报价单汇率折算为 USD。
4. 将已采购商品转入已采购商品区域，核对采购总价、未税采购金额（USD）和含税采购金额（USD）。
5. 在其他费用成本和销售收入明细中新增记录，填写币种、金额、价格方式和税率；系统计算对应 USD 金额和汇总。
6. 新增发票记录时，分别维护成本/收入发票、发票号、币种、金额、税率、支付和开票状态；附件上传位置在对应发票记录内。
7. 明细保存后为只读状态，需要点击“修改”才进入编辑，保存后恢复只读。
8. 未确认项目可以删除或调整；已完成项目只读。

项目结算主单的创建时间、创建人、更新时间、更新人、确认时间和确认人放在详情页底部，不在顶部占用基础信息空间；明细不重复显示这些人员信息。

### 5.9 集采发票汇总

进入“集采系统 - 财务管理 - 发票汇总”，系统集中展示各项目结算中的发票记录。

- 支持按项目、发票类型、发票号、客户、承接单位、币种、支付状态和开票状态筛选。
- 列表按发票记录汇总，不改变项目结算原始发票数据。
- 点击项目或发票可回到项目结算详情；项目结算中修改发票后，汇总列表重新查询即可看到最新数据。
- 支持三横菜单排序/筛选、锁定列、分页和导出全部筛选结果。

## 6. 物流管理

物流列表按采购实例维度展示，同一需求单包含多个实例时，每个实例一行。

主要能力：

- 国家、签收状态、运输方式和关键字筛选。
- 默认按批次末尾数字从大到小排序。
- PO 单号可点击进入对应采购订单。
- 机房 ID、目的地点 ID、联系人 ID 支持 ID 或关键字搜索。
- 页面显示机房中文名称、详细地址、收件人姓名和电话。
- 实例英文名称按当前设备编码实时读取实例型号资料。

签收状态规则：

- 派送日期为空：未签收。
- 派送日期有值：已签收。

物流导入是更新模式：

1. 使用物流 ID 定位已有记录。
2. Excel 空白字段表示“不修改”，不会清空数据库已有值。
3. 目的地点 ID 自动带出详细地址。
4. 联系人 ID 自动带出姓名和电话。
5. 机房 ID 自动带出机房中文名称。
6. 批次、采购明细 ID、设备编码等已有数据在模板留空时继续保留。

## 7. 财务管理：月账单

### 7.1 待生成月账单实例

展示已确认采购、尚未建立月账单台账的实例。

1. 按国家、批次、需求单、PO 或设备编码筛选。
2. 搜索实例合同号。
3. 系统按合同号、国家和设备编码带出合同币种及前 24、后 36 个月价格，不允许手工改价。
4. 选择起始核销月份。
5. 确认生成月账单台账和 60 个月明细。

页面同时显示含税自算单价和结差差额：

```text
24个月实例单价（含税自算）
  = (采购不含税单价 / 88495.58 × 3978.4 + 税费加成 / 24)
    × (1 + 国家增值税税率)
结差差额单价 = 24个月实例合同价 - 含税自算单价
结差差额总价 = 数量 × 结差差额单价
```

负数差额以红色显示。

### 7.2 月账单台账

- 台账保存合同、币种、价格、数量和起始月份。
- 编辑时只能搜索选择有效实例合同；币种和价格由合同自动带出。
- 修改起始核销月份后，系统删除并重新生成该台账的 60 个月明细。
- 编辑和删除都会弹出确认提示。
- 重新生成时仍会按规则应用已确认实例合同调整单。

### 7.3 月账单每月明细

- 前 24 个月使用前 24 个月合同价。
- 后 36 个月使用后 36 个月合同价。
- 月账单金额（含税）= 数量 × 月账单实例价格（含税）。
- 同时显示承接单位、供应商、客户、结差差额单价和差额总价。
- 来源为调整单时显示调整单号、新实例合同号和新币种。
- 支持国家、批次、月份和关键字筛选，使用后端分页。
- 导出当前筛选条件下全部明细。

### 7.4 月账单对账单

1. 选择国家、起始日期和终止日期。
2. 点击预览核对合同号、产品类型、数量、含税及未税单价。
3. 输入或使用系统生成的对账单号。
4. 生成未确认对账单草稿。
5. 未确认对账单可以删除。
6. 确认后金额和明细冻结，不能删除或退回。
7. 点击对账单号在新标签查看明细，或导出格式化 Excel。

导出规则：

- 税率读取国家管理，不在代码中固定。
- 不含税单价 = 含税单价 / (1 + 国家增值税税率)。
- 同一国家同一期间存在多个币种时，按币种拆分区域。
- 同一实例合同号按模板要求合并显示。
- 保存后的对账单不受后续台账或调整单变化影响。

## 8. 财务管理：预付款

### 8.1 待生成预付款实例

- 展示已确认采购、尚未被任何预付款合同占用的采购明细。
- 支持国家、批次、需求单、PO 和设备编码筛选。
- 勾选实例后显示已选数量、实际总价和拟定合同总价。
- 填写预付款合同号与生效日期后生成草稿。

同一采购明细只能进入一张预付款合同。只要存在于草稿或已确认合同，就不会再次出现在待生成列表。

### 8.2 预付款合同

1. 打开合同号查看详情。
2. 草稿状态点击“修改”进入编辑。
3. 实例行可修改预付款币种、金额和起始核销月份。
4. 非实例费用行可填写费用名称、批次、国家、供应商、承接单位和客户。
5. 点击“保存草稿”后页面变为只读，按钮恢复为“修改”。
6. 点击“确认合同”后生成每条明细的 24 个月核销数据。

确认后系统可询问是否在新标签打开核销明细；选择不跳转时继续停留在合同页面。

合同管理规则：

- 草稿可以删除，删除后实例释放回待生成列表。
- 已确认合同可点击“回退草稿”。
- 回退会删除该合同生成的月度核销明细。
- 若该合同存在已确认预付款核销调整单，系统阻止回退。
- 合同总金额按千分位和两位小数显示。

### 8.3 预付款每月核销明细

- 每条确认合同明细均摊到 24 个月。
- 最后一个月吸收四舍五入差额，保证 24 个月合计等于合同总价。
- 实例费用和非实例费用同时显示。
- 承接单位、供应商和客户从需求明细或合同费用行传递。
- 支持国家、批次、月份、关键字、分页和全部筛选结果导出。

### 8.4 预付款核销调整单

用途：只调整某个实例或费用在特定月份的核销金额。

1. 新建调整单。
2. 通过搜索添加目标实例月份，不加载全部数据到页面。
3. 输入调整后金额并保存草稿。
4. 确认调整单。

确认后仅覆盖指定月份，其余月份不变。

## 9. 财务管理：实际服务费

### 9.1 服务费核算

```text
月度服务费（含税） = 月账单金额（含税） - 预付款核销金额（含税）
月度服务费（未税） = 月度服务费（含税） / (1 + 国家增值税税率)
```

操作：

1. 在页面上方选择起始月份、结束月份、国家、批次和明细类型。
2. 点击查询查看分页明细与汇总。
3. 服务费对账单必须按单一国家、单一月份生成，因此生成时起始月份和结束月份要选择同一个月。
4. 点击“生成服务费对账单”形成未确认草稿。

规则：

- 月账单取“月账单金额（含税）”，不是实例单价。
- 预付款非实例费用单独显示，月账单金额为 `0`。
- 页面分别显示月账单币种与预付款币种。
- 承接单位、供应商和客户按需求明细及预付款费用行传递。
- 服务费核算是实时结果，源数据变化后重新查询会变化。

### 9.2 服务费对账单

对账单主表显示核销月份、国家、各项汇总、确认状态、回款和开票信息，不显示批次范围。

确认规则：

- 未确认：可以删除和确认。
- 已确认：金额与明细冻结，不能删除或退回。
- 点击对账单号在新标签打开服务费对账单明细。

开票操作：

- 在“开票状态”列直接横向切换“未开票/已开票”，手动切换不弹窗。
- 在“发票附件”列上传 PDF、Word、Excel 或图片，单个文件最大 25 MB。
- 上传附件时，系统询问是否同时改为“已开票”，但不强制绑定。
- 删除附件时，系统询问是否同时改为“未开票”，用户也可以保持原状态。
- 每张对账单独立保存附件和状态，不会影响其他记录。

回款操作：

1. 点击记录后的金币“回款”按钮。
2. 使用按钮切换“已回款/未回款”。
3. 收款单位默认带出承接单位。
4. 付款单位默认带出客户。
5. 回款币种和回款金额默认按对账单数据带出。
6. 填写回款日期。
7. 所有默认值都可以手工调整后保存。

### 9.3 服务费对账单明细

明细保存生成对账单当时的国家、批次、需求单、PO、设备、客户、供应商、承接单位、金额、币种和来源 ID。确认后的明细不会因后续月账单或预付款调整而改变。

## 10. 华为云业务

入口：“华为云业务 - 华为云对账”。华为云对账数据可以通过导入批量建立，也可以手动新增。系统按账期、客户和华为 ID 保存每一行；同一客户存在多个华为 ID 时分别建立多行。

### 10.1 华为云对账

基础字段包括账期、客户名称、华为 ID、华为对账人、目录价、伙伴结算金额、客户/供应商代金券、供应商应付、客户应收、万众理论毛利、万众结算毛利、客户折扣和备注。

- 供应商应付按“承接单位 -> 供应商”显示供应商应付不含税金额、税率、税金和含税金额。
- 客户应收按“客户 -> 承接单位”显示客户应收不含税金额、客户承担税率、税金和含税金额。
- 客户实收和客户开票位于客户应收之后，分别维护币种、汇率、未税金额、税率、税金、含税金额、日期及状态。
- 发票、回款等金额的未税/含税值可根据税率自动换算，也可以在编辑时调整后保存。
- 主表显示创建日期、更新日期、确认日期；未确认记录可以修改，确认后锁定。
- 操作列使用图标按钮；附件已放在客户开票相关区域时不重复显示无关附件按钮。

操作：

1. 通过“下载导入模板”取得当前字段模板。
2. 填写账期、客户名称和华为 ID 等必填字段，上传并预览；预览确认无误后执行导入。
3. 手动新增时填写同样的基础字段，保存后可继续编辑或确认。
4. 在服务映射中维护华为 ID/账号与供应商、承接单位、客户和华为对账人的关系。匹配成功后，系统自动显示简称和单位链路。
5. 打开客户实收或客户开票的编辑按钮，维护付款单位和收款单位。付款单位从客户档案选择，收款单位从承接单位档案选择，均支持编码或简称搜索。
6. 供应商付款按账期和供应商汇总；点击箭头查看各华为账号明细，在汇总层维护供应商实付和供应商开票。
7. 汇总和明细均支持关键字、三横菜单升序/降序、筛选、锁定列、分页和导出。

### 10.2 华为云服务映射与供应商付款

- 服务映射用于解决同一客户多个华为 ID、多个账号的归属问题。每个账号应明确绑定客户、承接单位、供应商和对账人。
- 如果账号无法匹配，不要直接确认对账；先在“服务映射”补齐账号关系，再重新加载或重新导入。
- 供应商付款的明细只展示账号对应的供应商应付数据，供应商汇总值在汇总行维护。
- 供应商实付和供应商开票分别记录币种、汇率、未税金额、税率、税金、含税金额、日期和状态。

## 11. 附加功能：成本、锚定价格与结差

此组模块默认可停用。看不到入口时，请管理员到“数据工具 - 功能模块管理”启用。

### 11.1 B6类型规则

维护 B6 类型、别名、适用范围、是否包含资金成本、是否包含备件成本、默认资金月数、备件占用月数、备件费率、结算方式和说明。

- 排序值只控制规则显示顺序，不参与价格计算。
- 实例型号可维护默认 B6 类型。
- CAPEX/OPEX 价格明细选择 B6 类型后可点击“套用规则”，将默认参数填入当前行。
- 套用规则后仍可人工调整该价格版本草稿中的参数。

### 11.2 CAPEX/OPEX锚定价格

1. 新建价格版本，填写版本号、国家、生效日期和来源文件。
2. 新增或导入设备明细。
3. 选择设备编码和 B6 类型。
4. 点击“套用规则”取得默认资金月数、备件参数等。
5. 填写整机不含税价格、币种、汇率和适用费率。
6. 保存草稿并检查完整明细。
7. 点击“查看计算逻辑”核对参数、每一步结果和历史版本。
8. 确认版本后参数与结果锁定。

历史版本不会被新版本覆盖。需要沿用旧参数时使用“复制为新版本”。

### 11.3 实例结差

前提：

- 采购明细已维护采购 CAPEX、采购 OPEX。
- 已存在相同国家和设备编码的已确认锚定价格版本。
- 非 USD 采购币种已填写采购币种兑 USD 的结差汇率。

操作：

1. 选择国家和已确认锚定价格版本。
2. 查询待生成实例。
3. 补充结差汇率并检查缺失原因。
4. 勾选可生成实例，生成结差来源单草稿。
5. 打开来源单核对 CAPEX、OPEX 差额。
6. 确认后进入最终结差候选池。

### 11.4 非实例费用结差

支持类型：

- 备件结差。
- 清关费 OPEX。
- 跨境业务金融税。
- 人力及行政成本。
- 其他非实例 OPEX。

操作：

1. 选择费用类型、国家、币种和结差期间。
2. 下载当前类型模板或在横向表格新增明细。
3. 填写白底输入字段。
4. 灰底字段由系统实时计算，不需要手工填写。
5. 导入只把成功行填入当前页面；确认无误后点击“保存草稿”才写入数据库。
6. 到实例结差来源单列表确认该非实例来源单。

主要自动计算：

- 备件：不含税总价 USD = 结算数量 × 设备不含税单价；CNY 总价再乘支付汇率。
- 清关费：理论税费 = 设备总额 × 当地税率；实际结算 USD = 原币费用 ÷ USD 兑换汇率。
- 金融税：金融税金额 = 计税基数 × 金融税率；扣除 ISS 后再换算人民币。
- 人力及其他费用：实际支付 USD = 原币金额 ÷ 兑 USD 汇率。

### 11.5 结差结算单

结差结算单用于汇总已确认的实例结差和非实例费用来源单。

1. 选择兼容的国家、币种和期间。
2. 勾选已确认来源单。
3. 生成结算单草稿。
4. 核对来源单号、名称、类型、明细数和金额。
5. 确认后冻结；不使用的草稿可以作废。

## 12. 附加功能：内部服务费

内部服务费用于公司内部五年分摊，不改变实际月账单、预付款和服务费对账单。

### 12.1 待初始化内部服务费实例

月账单台账早于内部服务费功能创建时，不会自动拥有内部服务费台账。进入“待初始化内部服务费实例”：

1. 按国家、批次或关键字筛选。
2. 勾选目标实例，点击“初始化已选”。
3. 也可点击“初始化本页”。

### 12.2 内部服务费分摊

```text
60个月合同未税收入 = 60个月月账单含税总收入 / (1 + VAT)
采购总成本 = 数量 × (采购不含税单价 + 税费加成)
内部服务费总额 = 合同未税收入 - 采购总成本
```

正常分摊：

- 总额的 50% 分摊到前 24 个月。
- 剩余 50% 分摊到后 36 个月。
- 每个阶段最后一个月吸收四舍五入差额。

点击“生成/重算未归档”时：

- 已归档月份金额保持不变。
- 人工区间调整月份保持指定金额。
- 实例合同价格调整会修订 60 个月收入计划。
- 修订后的剩余总额只在其他未归档月份重新分摊。

### 12.3 内部服务费调整单与区间调整

1. 在某条内部服务费后点击“区间调整”。
2. 选择起始月和结束月。
3. 输入该区间每月固定内部服务费和调整原因。
4. 保存后系统固定该区间，并重算其余未归档月份。

调整单列表可查看或撤销调整。只要调整涉及已归档月份，就不能撤销。

### 12.4 内部服务费归档快照

1. 选择归档月份和国家筛选条件。
2. 点击“归档当月”。
3. 系统生成内部服务费归档快照及明细。

归档后该月不再被自动重算。后续合同或人工调整只影响剩余未归档月份。

## 13. 数据导入中心

入口：“数据工具 - 数据导入中心”。

通用流程：

1. 选择导入类型。
2. 下载当前版本模板。
3. 按模板第二行说明填写数据，不要修改表头。
4. 上传文件并查看预览。
5. 检查新增、更新、成功和失败数量。
6. 下载失败明细并修正。
7. 确认导入后才正式写入数据库。
8. 在导入历史下方使用分页、每页条数和页码跳转查看记录。

预览中的“新增记录数”是系统将执行的主单、明细和下游记录操作数，不一定等于 Excel 数据行数。

### 13.1 需求单主从导入

必填：需求单号、国家、合同号、批次、需求类型、设备编码、供应商 ID、承接单位 ID、节点数量。客户 ID 可选。

- 相同需求单号的多行数据生成一张主单和多条明细。
- 状态为空默认草稿。
- 已有主单会按导入规则更新，不应生成孤立明细。

### 13.2 采购订单主从导入

必填：PO 订单号、来源需求单号、币种、含税单价；产品编码和需求明细 ID 二选一。

- 系统采购 ID 可为空，导入时自动生成。
- 一个 PO 可以包含多个需求单号。
- 产品编码按“来源需求单号 + 设备编码”匹配需求明细；匹配多条时必须填写需求明细 ID。
- 不含税单价、税费加成和含税单价分别保存，不会把含税单价覆盖到不含税单价。
- 确认状态采购导入后同步生成或更新物流。

### 13.3 实例合同导入

必填：合同号、国家、设备编码、币种、前 24 个月含税单价、后 36 个月含税单价。

机型和实例型号英文名称可为空，系统按设备编码从实例型号匹配。

### 13.4 月账单台账初始化导入

必填：需求单号、PO 订单号、实例编码、实例合同号、起始核销月份。

- 台账 ID 和采购明细 ID 可为空。
- 系统按需求单号、PO 和设备编码匹配采购明细。
- 国家、批次、数量、实际价格等空白字段自动带出。
- 合同币种及前 24、后 36 个月价格强制以实例合同为准，模板中的不一致值不会覆盖合同。
- 导入成功后生成或刷新 60 个月明细，并重新应用已确认实例合同调整单。

### 13.5 预付款合同初始化导入

必填：预付款合同号、合同币种、生效日期、需求单号、PO 订单号、实例编码、预付款币种、预付款总价、起始核销月份。

- 导入后统一生成草稿，不直接生成月度核销。
- 国家、批次、机型、英文名称、数量、实际币种和实际价格可从采购明细带出。
- 导入草稿占用的实例会从待生成预付款列表移除。
- 确认合同后才生成 24 个月核销明细。

物流导入和要货计划导入位于各自业务模块，不属于数据导入中心的五类初始化模板。

## 14. 文档管理

文档库支持类似资源管理器的多级目录：

- 新建文件夹。
- 双击文件夹进入。
- 在文件夹内继续创建子文件夹。
- 右键重命名或删除文件夹。
- 批量上传 PDF、Word、Excel 和图片。
- 下载、重命名或删除文件。

推荐目录：

```text
MX/
  合同/
  发票/
CL/
  合同/
  发票/
BR/
  合同/
  发票/
```

部署到远程服务器后，文件保存在远程服务器 `uploads/`，不会自动保存在用户电脑。数据库只保存文件元数据，因此数据库和文件目录必须一起备份。

## 15. 用户与功能管理

### 15.1 用户管理

“用户管理”仅管理员登录后显示。管理员初始账号为 `admin@luzcorp.com`，初始密码同为 `admin@luzcorp.com`，首次登录后应立即修改。

管理员可以新增、启用/停用和编辑用户，维护用户名称、登录账号、密码和系统权限。普通用户不能看到用户管理入口，也不能修改其他用户权限。

权限按目录和功能操作配置：

- 一级目录勾选后，默认允许使用该目录下的子目录和页面。
- 可以继续按二级目录或功能细分“查看、新增、修改、删除”等权限。
- 停用账号不能登录；停用用户或功能不会删除历史业务数据。
- 创建人、更新人和确认人由当前登录账号自动写入主单据，用户不能在表单中手工冒充其他账号。

### 15.2 功能启用

进入“用户管理 - 功能启用”。管理员可以控制尚未正式启用的模块是否对用户显示和访问；普通用户只能看到允许公开的功能。

- 停用模块后，左侧目录和首页入口隐藏，直接访问页面或 API 也会被拦截。
- 启用模块后，历史数据和原有权限继续保留。
- 功能启停与用户目录排序、列筛选设置相互独立。

## 16. 分页、筛选与导出

- 大数据列表使用后端分页，数据库只返回当前页，不会先把全部数据加载到浏览器。
- 可选择每页条数、上一页、下一页和跳转页码。
- 页面筛选后，分页总数按筛选条件重新计算。
- 导出不受当前页限制，导出当前筛选条件命中的全部数据。
- 明细导出包含前端可见业务字段，包括客户、供应商、承接单位、币种和金额。
- 批次按国家代码和末尾数字排序，数字大的批次优先。

## 17. 常见问题

### 17.1 页面显示“加载中”不结束

先等待当前请求完成。若接口返回无记录，页面应显示“无数据”；持续加载通常表示接口异常或数据库连接失败。刷新前记录当前模块和筛选条件，交给开发人员检查浏览器网络请求和服务日志。

### 17.2 页面刷新后仍是旧版本

服务端代码修改后需要重新执行生产构建并重启：

```bash
npm run build
npm run start
```

生产环境由部署人员使用 PM2 或服务管理器重启。

### 17.3 为什么明细价格不是原实例合同价格

检查是否存在已确认实例合同调整单。有效调整单优先级高于原合同，且同一月份按确认时间最近的调整单取值。

### 17.4 为什么待生成预付款看不到实例

实例可能已被预付款合同草稿或已确认合同占用。删除草稿可释放；已确认合同需满足条件后回退草稿。

### 17.5 为什么物流导入后空白字段没有清空

物流导入是更新模式，空白表示“不修改”。如需清空字段，应在网页编辑中明确操作，不要用空白 Excel 单元格覆盖。

### 17.6 为什么对账单不随后续数据变化

月账单对账单、服务费对账单和内部服务费归档属于快照。确认或归档后保存当时数据，后续源数据调整不会覆盖历史快照。

### 17.7 为什么某些财务模块看不到

进入“数据工具 - 功能模块管理”检查模块是否停用。只有管理员可以修改启停状态。

### 17.8 导入失败如何处理

1. 查看预览失败条数和错误原因。
2. 下载错误报告。
3. 修正日期、数字、必填字段或业务匹配字段。
4. 重新上传预览。
5. 确认预览正确后再执行导入。
