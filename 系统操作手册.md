# cloud_power 算力交付管理系统操作手册

## 文档更新说明

- 系统支持内部标签页；标签页未关闭时会保留筛选、分页和表单操作状态。
- 采购订单详情页中的“要货计划 SN 码”和“要货计划子”导入会自动关联当前采购订单，无需在模板中填写 PO 单号。
- 预付款合同已确认后可点击“回退草稿”。回退会删除该合同生成的预付款每月核销明细；若存在已确认的预付款核销调整单，系统会阻止回退。
- 金额字段统一按千分位和两位小数显示，例如 `10,000.00`。

## 1. 登录与首页

1. 打开系统地址：`http://127.0.0.1:3000/`。
2. 如未登录，进入登录页。
3. 初始账号：`admin@luzcorp.com`。
4. 初始密码：`Luz@#789789`。

首页展示：

- 按国家、月份、币种汇总的服务费图表。
- 按国家、月份统计的新增实例数量。
- 各模块入口。

系统支持内部标签页。打开模块后会生成页签，只要不关闭页签，筛选、分页、表单输入等状态会保留。

## 2. 基础信息

基础信息包括：

- 国家
- 交付地址
- 收件人信息
- 机房
- 供应商
- 实例型号

常用操作：

1. 点击左侧菜单进入对应基础信息模块。
2. 点击新建录入数据。
3. 使用导入模板批量导入。
4. 使用导出功能下载当前数据。
5. 修改基础信息后，后续业务会按新基础资料进行匹配；历史快照字段不会自动被覆盖。

注意：

- 交付地址用于物流目的地。
- 收件人信息用于物流联系人。
- 机房ID可在物流导入时自动带出机房中文名称。
- 实例型号中的设备编码用于需求、采购、合同、月账单、预付款等模块的自动匹配。

## 3. 合同管理

### 3.1 实例合同

用途：维护国家、设备编码、机型、实例英文名称、币种、前24个月价格、后36个月价格。

操作：

1. 进入“合同管理 - 实例合同”。
2. 点击新建。
3. 输入合同号、国家、设备编码。
4. 设备编码填写后，系统自动带出机型和实例型号英文名称。
5. 填写币种、前24个月价格、后36个月价格。
6. 保存。

规则：

- 一个合同号可以包含多个实例设备。
- 只要合同号、国家、设备编码不完全一致，就允许同时存在。

### 3.2 实例合同调整单

用途：针对特定批次、特定实例、指定生效月份后的月账单价格进行调整。

操作：

1. 进入“合同管理 - 实例合同调整单”。
2. 新建调整单。
3. 输入调整单号、实例合同号、币种、生效月份等主单信息。
4. 在明细中搜索添加需要调整的实例。
5. 输入调整后的前24个月价格和后36个月价格。
6. 保存草稿。
7. 确认调整单。

规则：

- 调整单确认后会影响月账单每月核销明细。
- 同一批次、同一实例存在多张调整单时，优先取最近确认的调整单。
- 调整单优先级高于原实例合同。

## 4. 客户需求

### 4.1 需求单列表

展示每个需求单的国家、批次、状态、总数量、计划交付日期、创建日期、更新日期等。

操作：

1. 进入“客户需求 - 需求单”。
2. 点击新建进入需求单详情页。
3. 填写主单信息：需求单号、国家、批次、合同号、需求类型、计划交付日期等。
4. 添加需求明细。
5. 设备编码支持搜索和手输。
6. 点击保存草稿后，状态为草稿。
7. 点击确认需求后，状态进入待下单或采购中。

注意：

- 草稿状态下可修改。
- 保存后按钮会变为“修改”，未进入修改状态时明细不可编辑。
- 确认需求后不会自动跳转到采购单页面。

### 4.2 需求明细一览

展示已确认需求单中的明细。

主要字段：

- 国家
- 批次
- 需求单号
- 设备编码
- 机型
- 英文名称
- 供应商
- 节点数量
- 交付时间
- 创建时间
- 修改时间

规则：

- 需求明细一览不支持直接新建、编辑、删除。
- 数据来自需求单详情中的明细维护。

## 5. 采购管理

### 5.1 采购订单列表

采购订单支持系统采购ID和用户填写的 PO 订单号。

操作：

1. 进入“采购管理 - 采购订单”。
2. 可从已确认需求单生成采购草稿。
3. 可新建采购订单。
4. 采购订单号由用户手动输入，也支持系统生成采购ID。
5. 采购订单可包含多个需求单的明细。
6. 点击确认采购后，采购订单状态变为已确认。

规则：

- 业务展示和后续关联优先使用用户填写的 PO 订单号。
- 采购确认后，需求单状态更新为已下单。
- 采购确认后，系统按采购明细生成物流记录。
- 未生成月账单和预付款前，需求单和采购单允许删除。

### 5.2 采购订单详情

操作：

1. 点击采购订单列表中的单号进入详情页。
2. 填写币种：CNY、MXN、CLP、USD、BRL。
3. 上方币种切换后，下方明细币种同步更新。
4. 填写单价、数量、系数等。
5. 确认采购后按钮显示为已确认。

### 5.3 采购明细一览

展示已确认采购订单中的实例明细。

主要字段：

- 系统采购ID
- PO订单号
- 批次号
- 实例编码
- 实例名称
- 数量
- 单价
- 总价

规则：

- 不支持直接新建、导入、下载模板。
- 数据来自采购订单详情。
- 支持分页、每页条数选择、跳转页码和导出。

## 6. 物流管理

物流列表按实例维度展示。同一个需求单如包含多个实例，会生成多行物流数据。

主要字段：

- 物流ID
- PO订单号
- 批次号
- 采购明细ID
- 实例编码
- 实例英文名称
- 机房ID
- 机房中文名称
- 目的地点
- 收件人
- 地址快照
- 电话快照
- 各物流节点日期

操作：

1. 进入“物流管理 - 物流列表”。
2. 可手动编辑物流节点。
3. 可通过字段设置表单选择显示/隐藏字段。
4. 可导入物流数据更新已有物流单。

物流导入规则：

- 使用物流ID定位并更新已有物流记录。
- Excel 空白字段不会覆盖数据库已有值。
- `目的地点ID` 可自动带出交付地址快照。
- `收件联系人ID` 可自动带出收件人姓名和电话。
- `机房ID` 可自动带出机房中文名称。
- 批次号、采购明细ID、实例编码、英文名称等已有数据在导入空白时会保留。

## 7. 月账单管理

### 7.1 待生成月账单实例

展示已确认下单、尚未生成月账单台账的实例。

操作：

1. 进入“财务管理 - 月账单管理 - 待生成月账单实例”。
2. 系统根据国家和实例编码匹配实例合同。
3. 可手动搜索选择实例合同号。
4. 合同币种、前24个月价格、后36个月价格由合同自动带出，不可手动填写。
5. 选择起始核销月份。
6. 确认生成月账单台账。

### 7.2 月账单台账

用途：保存实例对应的合同价格、起始核销月份等。

操作：

1. 进入“月账单台账”。
2. 点击编辑。
3. 可选择实例合同号。
4. 币种、前24个月价格、后36个月价格自动带出。
5. 修改起始核销月份后，系统重新生成每月核销明细。
6. 编辑或删除时会弹窗确认。

### 7.3 月账单每月核销明细

规则：

- 每个实例生成60个月核销明细。
- 前24个月使用前24个月合同价。
- 后36个月使用后36个月合同价。
- 显示月核销金额和月账单核销总金额。
- 总金额 = 数量 × 月核销金额。
- 实例合同调整单确认后，相关月份金额会按调整单更新。

### 7.4 月账单对账单

操作：

1. 进入“月账单对账单”。
2. 选择国家、开始日期、结束日期。
3. 预览对账单。
4. 保存快照。
5. 导出 Excel。

规则：

- 导出格式按国家模板美化。
- 同一个国家当月存在多个币种时，按币种拆分区域。
- 不含税单价 = 含税价 / (1 + 税率)。
- 税率：MX 16%，CL 19%，BR 2.9%。
- 保存快照后，后续源数据调整不影响已保存快照。

## 8. 预付款管理

### 8.1 待生成预付款实例

展示已下单、尚未被预付款合同占用的实例。

操作：

1. 进入“待生成预付款实例”。
2. 可按国家、批次、需求单、PO、实例编码搜索。
3. 勾选实例。
4. 底部悬浮窗显示勾选数量、实际总价、合同总价。
5. 点击生成预付款合同草稿。

规则：

- 生成草稿后，被选中的实例不再显示在待生成列表。
- 删除草稿后，对应实例释放回待生成列表。

### 8.2 预付款合同

操作：

1. 进入“预付款合同”。
2. 点击合同号进入详情。
3. 草稿状态下点击“修改”进入编辑。
4. 编辑后点击“保存草稿”，按钮恢复为“修改”，页面不可编辑。
5. 点击“确认合同”后按钮变为“已确认”。

规则：

- 预付款合同确认后，按每条实例的合同总价均摊到24个月。
- 费用明细支持手动输入批次号，并参与后续计算。
- 非实例费用也可纳入预付款合同。

### 8.3 预付款每月核销明细

展示确认后的预付款合同每月核销数据。

支持：

- 国家筛选
- 批次筛选
- 时间段筛选
- 分页
- 导出

### 8.4 预付款核销调整单

用途：调整特定实例特定月份的预付款核销金额。

操作：

1. 新建调整单。
2. 通过搜索添加需要调整的实例月份。
3. 输入调整后金额。
4. 保存草稿。
5. 确认调整单。

规则：

- 未选择的数据不会显示在调整单下方。
- 调整只影响指定月份，其余月份不变。

## 9. 服务费核算

服务费计算公式：

`月度服务费 = 月账单核销总额 - 预付款核销金额`

操作：

1. 进入“服务费核算”。
2. 选择时间段、国家、批次等筛选条件。
3. 查看服务费明细。
4. 可保存服务费快照。

规则：

- 月账单金额取月账单核销总额。
- 预付款金额取预付款每月核销金额。
- 预付款中的非实例费用也会显示，月账单对应金额为0。
- 币种分为月账单币种和预付款币种。
- 快照保存后不受后续源数据调整影响。

## 10. 数据导入中心

入口：`数据工具 - 数据导入中心`

通用流程：

1. 选择导入类型。
2. 下载模板。
3. 按模板填写数据。
4. 上传文件预览。
5. 查看总行数、成功行数、失败行数、生成主单数、生成明细数。
6. 如有失败，下载错误报告并修正。
7. 无错误后点击确认导入。
8. 在导入历史中查看记录。

导入历史支持：

- 分页
- 每页条数选择
- 跳转页码
- 错误报告下载

### 10.1 需求单主从导入

按需求单号分组生成需求主单和需求明细。

必填通常包括：

- 需求单号
- 国家
- 合同号
- 批次
- 需求类型
- 设备编码
- 供应商
- 数量

### 10.2 采购订单主从导入

规则：

- 系统采购ID可为空，系统自动生成。
- PO订单号必填。
- 可通过需求单号 + 产品编码匹配需求明细。
- 支持一个采购订单包含多个需求单明细。

### 10.3 月账单台账初始化导入

规则：

- 月账单台账ID非必填，导入成功后系统自动生成。
- 采购明细ID非必填。
- 用户填写需求单号、PO单号、实例编码后，系统自动匹配采购明细。
- 系统自动带出数量、实际币种、实际单价、机型、英文名称等信息。

### 10.4 预付款合同初始化导入

规则：

- 导入后统一生成草稿。
- 采购明细ID非必填。
- 用户填写需求单号、PO单号、实例编码后，系统自动匹配采购明细。
- 系统自动带出国家、批次、机型、名称、数量、实际币种、实际价格等信息。
- 写入预付款明细后，对应实例会从待生成预付款实例中移除。

### 10.5 物流导入

规则：

- 按物流ID更新已有物流记录。
- 空白字段不覆盖已有字段。
- 目的地点ID自动带出详细地址。
- 联系人ID自动带出姓名和电话。
- 机房ID自动带出机房中文名称。

## 11. 文档管理

入口：`文档管理`

支持：

- 新建文件夹
- 右键重命名文件夹
- 双击进入文件夹
- 文件夹内继续创建子文件夹
- 批量上传文件
- 下载文件
- 删除文件

建议目录：

```text
MX
  合同
  发票
CL
  合同
  发票
BR
  合同
  发票
```

远程服务器部署时，上传文件会保存在远程服务器的文件存储目录中。

## 12. 常见问题

### 12.1 页面刷新后数据没变化

如果修改了服务端代码，需要重新构建并重启：

```bash
npm run build
npm run start
```

### 12.2 导入失败如何处理

1. 查看导入预览中的失败行数。
2. 下载错误报告。
3. 按错误提示修正 Excel。
4. 重新上传预览。
5. 确认导入。

### 12.3 为什么待生成预付款实例中看不到某实例

该实例可能已经存在于预付款合同草稿或已确认合同中。删除草稿可释放实例；已确认合同不可直接释放。

### 12.4 为什么月账单价格不是实例合同原价

如果存在已确认的实例合同调整单，系统会优先使用最近确认的调整单价格。

### 12.5 为什么导入物流后空白字段没有清空

物流导入采用更新模式。空白字段表示“不修改”，不会覆盖已有值。
