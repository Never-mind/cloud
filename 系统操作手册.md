# 算力交付管理系统操作手册

## 1. 系统简介

本系统用于管理算力交付业务，从基础资料维护、客户需求录入、采购下单、物流跟踪，到月账单核销、预付款核销、服务费核算和对账单导出。

系统支持内部标签页。用户点击左侧功能模块后，会在系统内部打开一个标签页；只要不关闭该标签，页面内的筛选条件、分页、表单输入和滚动位置会尽量保留。

## 2. 登录与访问

1. 打开浏览器。
2. 访问系统地址：

```text
http://127.0.0.1:3000/
```

3. 进入首页后，通过左侧菜单进入各功能模块。

## 3. 通用界面操作

### 3.1 内部标签页

1. 点击左侧菜单中的任意功能模块。
2. 系统会在顶部打开一个新的标签。
3. 如果该模块已经打开，再次点击会切换到已有标签。
4. 点击标签右侧的 `×` 可关闭标签。
5. 首页标签固定，不能关闭。
6. 未关闭的标签会保留当前页面操作状态。

### 3.2 查询与筛选

1. 进入列表页面。
2. 在顶部查询框输入关键字，或选择国家、批次、月份等筛选项。
3. 点击查询或等待页面刷新结果。
4. 切换到其他标签后再回来，未关闭标签时筛选结果会保留。

### 3.3 分页

1. 列表底部可选择每页显示条数。
2. 可点击上一页、下一页。
3. 可输入页码跳转到指定页。

### 3.4 新建、编辑、删除

1. 点击 `新建` 进入新增页面或打开新增弹窗。
2. 填写必填字段。
3. 点击保存草稿、保存或确认。
4. 对于允许删除的数据，点击删除时系统会弹窗确认。
5. 已进入核销或已生成后续单据的数据，通常不允许删除。

### 3.5 导入与导出

1. 点击 `下载模板` 获取 Excel 模板。
2. 按模板填写数据。
3. 点击 `导入` 上传 Excel。
4. 系统会提示导入条数、成功条数和失败原因。
5. 点击 `导出` 可导出当前模块数据。

## 4. 首页

首页用于查看系统总览数据。

### 4.1 服务费统计

1. 首页顶部显示各国家每月服务费合计。
2. 如果同一国家存在多个币种，会按币种分开显示。
3. 可按国家筛选查看。
4. 服务费趋势以图形方式展示。

### 4.2 新增实例数量

1. 首页显示各国家每月新增实例数量。
2. 用于观察新增资源趋势。

## 5. 基础信息

基础信息是后续业务单据的基础，请优先维护。

### 5.1 国家

用于维护国家代码和国家名称。

操作：
1. 打开 `基础信息 > 国家`。
2. 点击新建。
3. 填写国家代码、中文名、英文名和当地语言名称。
4. 保存。

### 5.2 交付地址

用于维护机房、仓库、办公室、Broker 等交付地址。

操作：
1. 打开 `基础信息 > 交付地址`。
2. 新建交付地址。
3. 填写地点 ID、国家、地点类型、中文名、英文名、详细地址。
4. 保存。

### 5.3 收件人信息

用于维护交付地点对应的收件人。

操作：
1. 打开 `基础信息 > 收件人信息`。
2. 新建联系人。
3. 填写联系人 ID、所属地点、姓名、电话、邮箱。
4. 保存。

### 5.4 机房

用于维护机房与物理交付地址的绑定关系。

操作：
1. 打开 `基础信息 > 机房`。
2. 新建机房。
3. 填写机房 ID、绑定地点 ID、中文名称、英文名称。
4. 保存。

### 5.5 供应商

用于维护 ODM 供应商信息。

操作：
1. 打开 `基础信息 > 供应商`。
2. 新建供应商。
3. 填写供应商 ID、供应商代码、名称。
4. 保存。

### 5.6 实例型号

用于维护设备编码、机型、XXLL 编码、中文名和英文名。

操作：
1. 打开 `基础信息 > 实例型号`。
2. 新建实例型号。
3. 填写设备编码、机型、XXLL 编码、中文名、英文名。
4. 保存。

## 6. 合同管理

### 6.1 实例合同

实例合同用于维护各国家、各设备编码对应的合同价格。

字段包括：
- 合同号
- 国家
- 设备编码
- 机型
- 实例型号英文名
- 币种
- 前 24 个月含税单价
- 后 36 个月含税单价

操作：
1. 打开 `合同管理 > 实例合同`。
2. 点击新建。
3. 输入合同号、国家和设备编码。
4. 系统根据设备编码自动带出机型和英文名称。
5. 选择币种。
6. 填写前 24 个月和后 36 个月含税单价。
7. 保存。

说明：
- 一个合同号可以包含多个设备。
- 只要合同号、国家、设备编码不完全一致，即可同时存在。

### 6.2 实例合同调整单

用于调整已开始核销实例在特定生效月份后的月账单合同价格。

操作：
1. 打开 `合同管理 > 实例合同调整单`。
2. 点击新建。
3. 填写调整单号、实例合同号、国家、批次、设备编码、生效月份、币种、调整价格。
4. 可在明细区导入多条实例调整明细。
5. 保存草稿后页面进入只读状态。
6. 点击修改可重新编辑。
7. 点击确认后，系统会按调整单更新对应月份之后的月账单核销明细。

说明：
- 调整单优先级高于原实例合同。
- 同一批次、同一实例存在多张调整单时，以最近确认的调整单为准。

## 7. 客户需求

### 7.1 需求单列表

需求单列表展示每个需求单号及其状态、国家、批次、总数量、计划交付日期、创建日期、更新日期。

操作：
1. 打开 `客户需求 > 需求单`。
2. 查看草稿和已确认需求单。
3. 点击需求单号进入详情。
4. 点击新建进入新的需求单明细表。

### 7.2 新建需求单

操作：
1. 点击 `新建`。
2. 填写主单信息：需求单号、国家、合同号、批次、类型、计划交付日期等。
3. 点击 `新增明细` 添加设备。
4. 设备编码支持搜索和手动输入。
5. 填写供应商、节点数量。
6. 点击 `保存草稿` 保存为草稿。
7. 点击 `确认需求单` 后，需求单状态进入待下单/采购中，并在采购清单生成草稿。

说明：
- 草稿状态下可修改。
- 保存草稿后按钮会变为修改/确认。
- 未进入修改状态时，明细不可编辑。
- 需求确认后不会自动跳转到采购页面。

### 7.3 需求明细一览

用于集中查看已确认需求单的明细。

显示字段包括：
- 国家
- 批次
- 需求单号
- 设备编码
- 机型
- 英文名称
- 供应商
- 节点数量
- 交付时间
- 创建时间
- 修改时间

说明：
- 需求明细一览的数据来自需求单，不在该页面单独维护。
- 页面不提供新建、导入、编辑、删除。

## 8. 采购管理

### 8.1 采购订单列表

采购订单列表分为草稿和已确认。

字段包括：
- PO 订单号
- 来源需求单号
- 状态
- 币种
- 采购总金额
- 下发日期
- 创建时间

操作：
1. 打开 `采购管理 > 采购订单`。
2. 查看草稿或已确认订单。
3. 点击 PO 单号进入详情。
4. 点击新建进入采购订单明细页。

### 8.2 新建/编辑采购订单

操作：
1. 点击新建。
2. 手动输入采购订单号。
3. 选择币种，支持 CNY、MXN、CLP、USD、BRL。
4. 添加或确认采购明细。
5. 填写各实例单价。
6. 点击保存草稿。
7. 点击确认采购后，按钮变为已确认。

说明：
- 上方币种选择后，下方实例币种会同步更新。
- 数量和价格输入时，前导 0 会自动去掉。
- 采购订单确认后，会同步更新需求单为已下单，并生成物流信息。

### 8.3 采购明细一览

用于集中查看已确认采购订单中的实例明细。

字段包括：
- 国家
- 批次
- PO 单号
- 实例编码
- 名称
- 数量
- 单价
- 总价
- 币种

说明：
- 采购明细一览只显示已确认采购订单。
- 数据由采购订单维护同步。
- 页面不提供新建、导入、编辑、删除。
- 支持分页、跳转页码和导出。

## 9. 物流管理

### 9.1 物流列表

采购订单确认后，系统会按采购明细生成物流记录。同一需求单下多个实例会分别生成多行物流数据。

字段包括：
- 物流 ID
- PO 单号
- 采购明细 ID
- 实例编码
- 实例英文名称
- 目的地点
- 收件人
- 运输方式
- 是否签收
- CRD、APD、ASD、起飞/开船、到港、清关完成、派送等时间节点

### 9.2 字段显示设置

物流字段较多，页面支持字段显示/隐藏。

操作：
1. 打开物流列表。
2. 打开字段设置表单。
3. 勾选需要显示的字段。
4. 取消勾选需要隐藏的字段。
5. 页面会显示当前隐藏字段有哪些。

## 10. 财务管理 - 月账单管理

### 10.1 待生成月账单实例

展示已确认下单但尚未生成月账单台账的实例。

字段包括：
- 国家
- 批次
- 需求单号
- 实例编码
- 机型
- 英文名称
- 数量
- 币种
- 实际单价
- 实例合同号
- 合同币种
- 前 24 个月价格
- 后 36 个月价格
- 起始核销月份

操作：
1. 打开 `财务管理 > 月账单管理 > 待生成月账单实例`。
2. 检查系统自动匹配的实例合同。
3. 如需手动指定合同号，可搜索合同号。
4. 系统根据合同号、国家、设备编码带出币种和价格。
5. 选择起始核销月份。
6. 确认生成月账单台账。

### 10.2 月账单台账

记录已生成月账单核销的实例。

操作：
1. 打开月账单台账。
2. 可编辑实例合同号和起始核销月份。
3. 币种、前 24 个月价格、后 36 个月价格只能由合同号带出，不可手动填写。
4. 修改起始核销月份后，系统重新生成核销明细。
5. 编辑或删除前系统会弹窗确认。

### 10.3 月账单每月核销明细

展示每个实例每个月的核销金额。

说明：
- 前 24 个月按前 24 个月价格。
- 后 36 个月按后 36 个月价格。
- 月账单核销总金额 = 数量 × 月核销金额。
- 如果存在实例合同调整单，调整单优先级高于实例合同价格。

### 10.4 月账单对账单

用于按时间区间生成对账单快照并导出 Excel。

操作：
1. 打开 `财务管理 > 月账单管理 > 月账单对账单`。
2. 选择国家。
3. 选择起始日期和终止日期。
4. 点击预览。
5. 确认无误后保存快照。
6. 点击导出，系统按模板导出 Excel。

说明：
- 税率：MX 16%、CL 19%、BR 2.9%。
- 不含税单价 = 含税单价 / (1 + 税率)。
- 同一币种单独生成一个对账区块。
- 如果同月存在 CNY 和 USD，会拆分成上下两部分。
- 同实例合同号会合并居中显示。
- 快照保存后，不受后续月账单调整影响。

## 11. 财务管理 - 预付款管理

### 11.1 待生成预付款实例

展示已下单但未勾选生成预付款合同的实例。

字段包括：
- 国家
- 批次
- 需求单号
- 实例编码
- 机型
- 英文名称
- 数量
- 币种
- 实际单价
- 实际总价

操作：
1. 勾选需要生成预付款合同的实例。
2. 页面下方悬浮窗显示勾选数量、实际总价和预付款合同金额。
3. 点击生成预付款合同草稿。

说明：
- 未勾选实例继续保留。
- 如果删除草稿，已勾选实例会释放回待生成列表。

### 11.2 预付款合同

用于维护预付款合同草稿和确认。

操作：
1. 打开预付款合同草稿。
2. 填写预付款合同号、币种、生效日期等。
3. 在实例明细中填写或调整合同币种、合同单价、合同总价、起始核销月份。
4. 可新增费用明细，填写费用名称、费用说明、批次号、金额。
5. 点击保存草稿后，按钮变为修改，页面进入只读。
6. 点击修改后可继续编辑。
7. 点击确认合同后，按钮变为已确认，并生成 24 个月核销明细。

### 11.3 预付款每月核销明细

展示预付款合同确认后生成的每月核销金额。

说明：
- 实例和费用明细均可生成核销数据。
- 默认按合同金额平均分摊 24 个月。
- 支持国家、批次、时间等筛选。
- 支持分页和导出。

### 11.4 预付款核销调整单

用于调整特定实例、特定月份的预付款核销金额。

操作：
1. 打开预付款核销调整单。
2. 新建调整单。
3. 通过搜索添加需要调整的实例或月份明细。
4. 输入调整后的月核销金额。
5. 保存草稿。
6. 确认后，系统更新对应月份核销金额，其余月份不变。

## 12. 财务管理 - 服务费核算

### 12.1 服务费计算

服务费计算公式：

```text
月度服务费 = 月账单核销总金额 - 预付款核销金额
```

操作：
1. 打开 `财务管理 > 服务费核算 > 服务费核算`。
2. 选择月份、国家、批次等筛选条件。
3. 点击计算。
4. 查看每月服务费金额。
5. 确认后保存服务费快照。

说明：
- 月账单取月账单核销总金额。
- 预付款取预付款核销金额。
- 预付款中的非实例费用也会显示；对应月账单金额为 0。
- 金额前显示对应币种，区分月账单币种和预付款币种。

### 12.2 服务费快照

用于查看已保存的服务费核算结果。

说明：
- 快照保存后，即使后续月账单或预付款调整，快照明细也不受影响。
- 可用于历史对账和归档。

### 12.3 服务费快照明细

展示服务费快照中的逐行明细。

字段包括：
- 月份
- 国家
- 批次
- 需求单号
- PO 单号
- 实例编码
- 数量
- 明细类型
- 月账单币种与金额
- 预付款币种与金额
- 服务费金额

## 13. 常见业务流程

### 13.1 从需求到采购

1. 维护基础信息。
2. 维护实例合同。
3. 新建需求单。
4. 添加需求明细。
5. 保存草稿。
6. 确认需求单。
7. 系统生成采购订单草稿。
8. 进入采购订单填写采购单号、币种、单价。
9. 确认采购订单。
10. 系统更新需求单为已下单，并生成物流记录。

### 13.2 从采购到月账单

1. 确认采购订单。
2. 打开待生成月账单实例。
3. 匹配或手动选择实例合同。
4. 选择起始核销月份。
5. 确认生成月账单台账。
6. 查看每月核销明细。
7. 如价格需要调整，创建实例合同调整单。
8. 生成月账单对账单快照并导出。

### 13.3 从采购到预付款

1. 确认采购订单。
2. 打开待生成预付款实例。
3. 勾选实例生成预付款合同草稿。
4. 填写合同号、币种、起始核销月份和费用明细。
5. 确认合同。
6. 系统生成 24 个月预付款核销明细。
7. 如某月金额需调整，创建预付款核销调整单。

### 13.4 服务费核算

1. 确保月账单每月核销明细已生成。
2. 确保预付款每月核销明细已生成。
3. 打开服务费核算。
4. 选择月份、国家、批次。
5. 计算服务费。
6. 保存服务费快照。
7. 查看服务费快照明细。

## 14. 注意事项

1. 基础信息是后续单据自动带出的来源，建议先维护完整。
2. 需求单确认后会影响采购流程，确认前请检查明细。
3. 采购订单确认后会生成物流和财务待处理数据，确认前请检查单价、数量和币种。
4. 月账单起始核销月份请选择每月 1 日，避免月份歧义。
5. 预付款起始核销月份同样建议选择每月 1 日。
6. 删除草稿单据可能释放或影响关联实例，请确认后操作。
7. 快照类数据用于归档，保存后不随源数据变化。
8. 如果页面筛选结果需要保留，请不要关闭该模块标签页。
