# 列表排序与筛选功能

## 目标

为需求、采购、物流、月账单、预付款、服务费和结差等列表增加统一的列头升序、降序、多选筛选和候选值搜索，并保持真实的后端分页。

## 进度

- [x] 检查现有通用实体列表、订单列表和专用财务列表
- [x] 实现可复用列头菜单和多选筛选浮层
- [x] 为通用实体接口增加安全排序与多值筛选
- [x] 接入通用实体列表及主要专用列表
- [x] 验证筛选、排序、分页、导出和标签页状态

## 本次补充

- 修复筛选 URL 参数前缀与 SQL 参数前缀混用导致的内部服务费明细筛选失效问题。
- 结差结算来源列表使用数据库 `COUNT(*)` 计算总数，列表仍使用 `LIMIT/OFFSET`，不再先查询全部来源单。
- CAPEX/OPEX 价格版本主列表接入列头升序、降序、多选筛选和候选值搜索。
- 实例结差待生成列表补充承接单位、供应商、客户、采购价格和锚定价格的后端排序筛选。

## 本轮问题修复验证（2026-08-27）

- 日期字段统一按 `YYYY-MM-DD` 输出；需求单日期候选和月账单、预付款、服务费核销月份候选均已验证可返回并可筛选。
- 国家字段统一标准化为 `BR`、`CL`、`MX`；需求单和采购订单国家候选已验证不再为空。
- 已验证“先筛选再排序”以及国家 + 批次号等多列组合筛选，查询使用 `AND` 条件且接口返回正常。
- 批次号排序按字母前缀和数字部分拆分自然排序，避免 `1、10、2` 的字典序问题。
- 通过 `npm.cmd test`：46 个测试文件、188 个测试全部通过。
- 通过 `npx.cmd tsc --noEmit` 和 `npm.cmd run build`。
- 本轮未修改数据库结构或数据，未提交、未推送仓库。

## 查询约定

- `sortField` 和 `sortOrder` 只接受后端白名单字段。
- 多选筛选使用重复参数，例如 `filter.requestNo=value1&filter.requestNo=value2`。
- 筛选确认后回到第 1 页，查询条件写入 URL。
- 导出复用相同的筛选和排序条件，但不受当前分页限制。

## 固定表头补充

- 新增 `src/components/sticky-table.tsx`，为页面级横向表格提供固定表头；页面纵向滚动仍由当前页面/标签页负责，表格只负责横向滚动。
- 物流列表、需求单列表和采购订单列表已接入固定表头；基础信息等复用 `EntityPage` 的列表也会自动获得该能力。
- 固定表头与原表头横向滚动同步，筛选/排序菜单继续可用，列锁定状态通过同一列表标识同步。
- 固定表头按原数据表各列实际宽度渲染，避免长字段导致固定表头与数据列错位。
- 已通过 `npx.cmd tsc --noEmit`、`npm.cmd test`（46 个测试文件、188 个测试）和 `npm.cmd run build`。
- 已使用最新构建启动本地服务并确认 3000 端口监听；浏览器端因服务重启后登录会话失效跳转登录页，未继续执行需要登录的滚动截图验证。
- 本次已将固定表头推广到结差、内部服务费、价格版本、合同详情、订单表单明细、需求/采购明细、非实例结差、导入中心和功能模块管理等页面级表格；同一页面的多个表格使用独立 `tableKey`。
- `StickyTable` 现在允许表格旁保留 `datalist` 等辅助节点，只对其中的 `<table>` 复制固定表头；弹窗内的局部滚动表格继续使用自身滚动和原有表头。
- 本次未修改数据库结构、业务计算或接口查询逻辑。
- 本次验证：`npx.cmd tsc --noEmit`、`npm.cmd test`（46 个测试文件、188 个测试）和 `npm.cmd run build` 均通过。
